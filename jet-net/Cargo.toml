[package]
name = "jet-net"
version = "1.0.0"
edition = "2021"
description = "Jet HTTP client bootstrap — D-NETDEP1=A; native-ize obligation in Epoch 3"

[lib]
name = "jet_net"
path = "src/lib.rs"

[dependencies]
# D-NETDEP1=A: owner-approved pure-Rust blocking HTTP client.
# Carries native-ize obligation: replace with first-party impl by end of Epoch 3.
ureq = { version = "2", default-features = false }
