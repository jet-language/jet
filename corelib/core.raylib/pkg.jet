// core.raylib ring package - official raylib graphics bridge (D-RAYLIB1=A).
//
// Stage 1 is a typed compiler skeleton plus display-gated example coverage.
// The native raylib bridge, dependency sourcing, and lock hashing land in the
// next slice.

name: "core.raylib"
version: "0.1.0"
packages: {
    raylib: library
}
