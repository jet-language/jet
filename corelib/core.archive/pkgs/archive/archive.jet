// core.archive — zip/tar containers (D-CORE-COMPRESS1=A / D-DEP-ARCHIVE1=A).
//
// This is the module declaration file for the core.archive ring package.
// The actual implementation is in the sibling Cargo crate (`Cargo.toml` +
// `src/lib.rs`), which is compiled by `CoreProvider::realize()` (D-BFS1)
// and linked into consumer programs via the FFI bridge (Source/FFI.rs).

module archive { }
