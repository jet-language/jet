// core.archive — zip/tar containers (D-CORE-COMPRESS1=A / D-DEP-ARCHIVE1=A).
//
// This Jet package declaration is the source authority and package boundary.
// Its public ABI is implemented only by the sibling audited kernel in
// `src/lib.rs`; the compiler has no copied archive implementation to use as a
// fallback. `CoreProvider::realize()` compiles that source tree (D-BFS1), and
// the same bytes are used by the consumer FFI bridge.

module archive { }
