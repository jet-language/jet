[package]
name = "jet_archive"
version = "0.2.0"
edition = "2021"
description = "core.archive dependency-free ABI package — zip/tar containers (D-CORE-COMPRESS1=A)"

[workspace]

[lib]
name = "jet_archive"
crate-type = ["rlib"]
