[package]
name = "jet_archive"
version = "0.2.0"
edition = "2021"
description = "core.archive ring package — zip/tar containers (D-CORE-COMPRESS1=A)"

[workspace]

[lib]
name = "jet_archive"
crate-type = ["rlib"]

[dependencies]
tar = "0"
zip = "2"
