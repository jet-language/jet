# Jet orchestration compatibility

When available, use `codeflow` for systematic, multi-part, ambiguous, long-running, or larger-scope work. Codeflow
owns the workflow; this prompt is only a Jet-specific compatibility layer. If Codeflow is unavailable, use the active
harness without creating a durable competing planner, phase model, delegation scheme, checkpoint protocol, or
resumption system here.

For bounded work, stay inline. In either mode:

- follow `AGENTS.md` authority, invariants, command environment, ownership guards, and done conditions;
- use GPT-5.6 Sol with only low, medium, or high effort; Terra needs a recorded task-specific advantage and is never
  the standard reviewer; Luna needs an owner request or measured stable mechanical advantage;
- use `ponytail:ponytail` for the smallest complete implementation without cutting safety, ratified scope, proof, or
  end-to-end behavior;
- let Tower skills own board mechanics and the verification skill own proof mechanics;
- keep one implementer per coherent patch, stage only owned paths, and integrate/remove task worktrees promptly;
- require exactly one fresh Sol reviewer, implementer fixes, and Sol recheck of material fixes.

Finish with the integrated result, exact proof, review outcome, owner gates, durable state changes, and confirmation
that no task worktree or temporary branch remains.
