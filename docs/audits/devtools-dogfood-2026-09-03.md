# Devtools dogfood friction ledger — 2026-09-03

<!-- generated by scripts/agent/devtools-dogfood.mjs; do not edit -->

Card: #2497 (Devtools built in Jet: dogfood anchor 3 friction ledger).
Campaign portfolio: #217. Five-anchor pattern: #2386.

Program under test: `DevtoolsDogfood`, package `examples/features/devtools/`.
The package is Jet-authored. It uses `core.ui` for the panel tree and
`core.devtools` for explicit event publication. It has no external frontend
runtime or fallback implementation.

## Ledger contract

Each row names the exact command and program, the expected and actual result,
the source or missing seam, a replayable evidence receipt, severity, domain,
action, owner, status, and any visible workaround. A workaround is not
closure: the row keeps the underlying seam named until its owning card proves it.

| ID | Command / program | Expected | Actual | Source / missing seam | Evidence receipt | Severity | Owner | Anchor links | Visible workaround | Disposition | Domain | Action | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| DEV-2497-01 | `jet dev examples/features/devtools/run.jet` / `DevtoolsDogfood` | The host can render the panel before the first event arrives. | A package-defined State must provide defaults so the first host render has a typed value and count. | examples/features/devtools/run.jet:13-16; state construction is the package-to-host render seam. | Source receipt: State declares selected: String{"none"} and event_count: Int{0}; replay with the command above. | medium | #2497 | #217 portfolio; #2386 five-anchor pattern | Keep defaults on every panel state field. Do not invent an ambient global state or an untyped fallback. | absorbed in dogfood source; host proof remains with #2424 | panel-state | absorbed in dogfood source; host proof remains with #2424 | open |
| DEV-2497-02 | `jet dev examples/features/devtools/run.jet` / `DevtoolsDogfood` | Only a deliberately selected value leaves the process, while the event remains typed. | Publication uses explicit field: and value: labels for each typed state value. | examples/features/devtools/run.jet:33-47; explicit-value gates are the core.devtools seam. | Source receipt: publish_selection uses devtools.publish(field: .selected, value: selected_value), and run publishes devtools.publish(field: .event_count, value: 1); replay with the command above and inspect the jet.devtools.v1 stream. | high | #2497 | #217 portfolio; #2386 five-anchor pattern | Preserve the value: and field: labels. Never replace them with a bare value or a stringly global event store. | absorbed in dogfood source; protocol proof remains with #2424 | event-publication | absorbed in dogfood source; protocol proof remains with #2424 | open |
| DEV-2497-03 | `jet dev examples/features/devtools/run.jet` / `DevtoolsDogfood` | A public panel is discoverable from the package without a startup registry call. | The canonical marker is #DevPanel on pub fn panel(state: State) UiNode; no startup registry call is present. | examples/features/devtools/run.jet:28-31; marker discovery is the panel registration seam. | Source receipt: #DevPanel and the typed panel signature are adjacent; replay with the command above and inspect the host panel catalog. | medium | #2497 | #217 portfolio; #2386 five-anchor pattern | Use only ratified #DevPanel; do not add a runtime registration call or compatibility alias. | resolved by canonical source; catalog proof remains with #2424 | panel-discovery | resolved by canonical source; catalog proof remains with #2424 | open |
| DEV-2497-04 | `jet test examples/features/devtools/run.jet` / `DevtoolsDogfood` | The package's own checks exercise its state contract without an external test runner. | Checks assert the pre-event defaults and the bounded selected-value state separately from host rendering. | examples/features/devtools/run.jet:54-68; package #Test reachability is the self-hosting seam. | Source receipt: two named #Test blocks assert State{} and exercise panel; replay with the command above. | low | #2497 | #217 portfolio; #2386 five-anchor pattern | Keep checks in the Jet package and use assert_eq; do not mirror them in a foreign frontend harness. | absorbed in dogfood source; integrated test proof remains with #2497 | package-checks | absorbed in dogfood source; integrated test proof remains with #2497 | open |

## Cross-anchor disposition

The package exercises the five-anchor rule from #2386: a real source program,
its public API calls, a host-visible panel, an own-check path, and a concrete
friction receipt. Every row remains linked to the #217 portfolio and the
#2386 pattern through this report header. The package records the workaround
beside the friction instead of silently promoting it to a second mechanism.

The integrated gate is `scripts/agent/jet-env cargo test --test
devtools_dogfood`. This lane records source receipts only; the parent
integration lane owns that command and any host or tier failure it exposes.

<!-- audit-dispositions:v1 -->
| finding | disposition | target or reason |
|---|---|---|
| DEV-2497-01 | card | #2497 owns this panel-state action: absorbed in dogfood source; host proof remains with #2424. |
| DEV-2497-02 | card | #2497 owns this event-publication action: absorbed in dogfood source; protocol proof remains with #2424. |
| DEV-2497-03 | card | #2497 owns this panel-discovery action: resolved by canonical source; catalog proof remains with #2424. |
| DEV-2497-04 | card | #2497 owns this package-checks action: absorbed in dogfood source; integrated test proof remains with #2497. |
<!-- /audit-dispositions -->
