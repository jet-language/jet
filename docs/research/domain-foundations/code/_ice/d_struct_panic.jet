pub struct M { value: Float }
pub fn checked(x: Float) M !Err -> {
    if !x.is_finite() -> return Err("not finite")
    return Ok(M{ value: x })
}
fn run() {
    v :: checked(1.0) ?? panic("failed")
    print("{v.value}")
}
