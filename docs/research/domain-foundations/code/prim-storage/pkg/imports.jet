use "log" as log
use "kv" as kv

fn run() {
    root :: "/home/nate/.cache/jet-luna/dx3/prim-storage"
    p :: Path.from("{root}/imports.txt")
    a :: log.append_record(~p, "a") ?? false
    b :: log.append_record(Path.from("{root}/imports.txt"), "b") ?? false
    print("{a} {b}")
}
