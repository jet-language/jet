use core.files as fs

fn run() {
    p :: Path.from("/home/nate/.cache/jet-luna/dx3/prim-storage/direct.txt")
    fs.append_all(~p, "a") ?? panic("a")
    fs.append_all(~p, "b") ?? panic("b")
    print(fs.read(~p) ?? panic("read"))
}
