use "log" as log

fn run() {
    p :: Path.from("/home/nate/.cache/jet-luna/dx3/prim-storage/local.txt")
    a :: log.append_record(~p, "a") ?? false
    b :: log.append_record(~p, "b") ?? false
    print("{a} {b}")
}
