use core.compute as compute

fn run() {
    values :: compute.zeros([2, 3]) ?? panic("zeros")
    array :: (values: values, dims: ["row", "col"])
    print("shape:{compute.shape(array.values)} dims:{array.dims}")
}
