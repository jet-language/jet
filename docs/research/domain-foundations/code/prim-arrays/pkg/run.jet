use core.compute as compute

fn run() {
    t :: compute.full([2, 3], 2.0) ?? panic("full")
    print("shape:{compute.shape(t)} rank:{compute.rank(t)} numel:{compute.numel(t)}")
    print("values:{compute.to_list(t)}")
}
