use core.compute as compute

struct BoxedTensor {
    tensor: Tensor
}

fn run() {
    values :: compute.zeros([2]) ?? panic("zeros")
    boxed :: BoxedTensor{tensor: values}
    print("wrapped:{compute.shape(boxed.tensor)}")
}
