use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 240, "Atlas gap")
    atlas :: rl.load_texture_atlas("assets/player.atlas")
    rl.draw_sprite(atlas, "hero", 10, 10)
    rl.close_window(window)
}
