fn as_meter(value: Meter) Float -> value.raw()

fn run() {
    source :: 300centimeter
    implicit :: as_meter(source)
    explicit :: Meter.from_centimeter(source)
    print(implicit)
    print(explicit)
    print(explicit.raw())
}
