module fixed<T>(capacity: Int) {
    pub struct Buffer {
        items: [T#capacity]
    }

    pub fn echo(values: [T#capacity]) [T#capacity] -> { return ~values }
}

module three :: fixed<Int>(3)

fn run() {
    input :: [Int#3]{1, 2, 3}
    output :: three.echo(input)
    print(output[2])
}
