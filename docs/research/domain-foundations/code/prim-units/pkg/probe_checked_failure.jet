use "units" as units

fn run() {
    result :: units.checked_meters_add(units.meter(Float.INFINITY), units.meter(1.0))
    if result == {
        .Err(_) -> print("rejected")
        .Ok(_) -> print("accepted")
    }
}
