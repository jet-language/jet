use core.files as fs

fn run() {
    inside :: fs.read("/home/nate/.cache/jet-luna/dx3/prim-capabilities/inputs/allowed.txt") ?? "inside-failed"
    outside :: fs.read("/home/nate/.cache/jet-luna/dx3/prim-capabilities/inputs-outside.txt") ?? "outside-failed"
    print("inside={inside.trim()}")
    print("outside={outside.trim()}")
}
