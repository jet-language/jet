// Deliberately probes whether a ProcessChild exposes a typed message endpoint.
use core.process as process

fn run() {
    child :: process.cmd(["echo", "ready"]).spawn() ?? panic("spawn failed")
    child.send(7)
    child.wait() ?? panic("wait failed")
}
