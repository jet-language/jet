use core.regex as re

fn run() {
    slash :: "\\"
    pattern :: "(a){slash}1"
    result :: re.compile(pattern)
    if result == {
        .Ok(_) -> print("accepted")
        .Err(message) -> print("error={message}")
    }
}
