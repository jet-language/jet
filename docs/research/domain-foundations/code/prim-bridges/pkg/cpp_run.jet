use cpp.reader as cpp

fn run() {
    reader := cpp.new_reader() ?? panic("constructor")
    print(reader.next_line_length() ?? -1)
    print(reader.next_line_length() ?? -1)
    cpp.close_reader(^reader)
}
