#Import module c.z {
    fn zlibVersion() String = "zlibVersion"
}
use c.z as zl
use c.zreader as z

fn run() {
    handle := z.jet_z_open("sample.txt.gz")
    print(z.jet_z_read_count(handle))
    print(zl.zlibVersion())
    print(z.jet_z_read_count(handle))
    z.jet_z_close(handle)
}
