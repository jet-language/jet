use core.mem

fn fill(&buffer: [Float#4]) -[!Mem.Alloc]> {
    loop i in 0..<buffer.len() {
        buffer[i] = 0.25
    }
}

fn run() {
    buffer := [Float#4]{ uninit }
    fill(&buffer)
    print("buffer={buffer[0]},{buffer[1]},{buffer[2]},{buffer[3]}")
}
