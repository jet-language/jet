package {
    name: "prim-realtime-audio-missing"
    version: "0.1.0"
    authority: { holds: { allow: [IO, Time, Mem.Alloc] } }
}

use core.audio as audio

fn run() {
    print("audio-api-present")
}
