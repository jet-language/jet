use core.interrupt as interrupt
use core.dma as dma
use core.flash as flash
use core.wcet as wcet
use core.ota as ota

fn run() {
    print("interrupt {interrupt}")
    print("dma {dma}")
    print("flash {flash}")
    print("wcet {wcet}")
    print("ota {ota}")
}
