enum Kind {
    Pair(left: Int, right: Int)
}
fn describe(k: Kind) String -> {
    if k == .Pair(left, right) -> return "{left}+{right}"
    return "one"
}
fn run() {
    print(describe(Kind.Pair{left: 1, right: 2}))
}
