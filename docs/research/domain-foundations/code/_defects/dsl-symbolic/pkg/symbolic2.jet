pub enum Kind {
    One
    Pair(left: Int, right: Int)
}

pub fn get() Kind -> Kind.Pair{left: 1, right: 2}

pub fn describe(k: Kind) String -> {
    if k == .Pair(left, right) -> return "{left}+{right}"
    return "one"
}
