use core.compute as compute

struct TensorBox {
    values: Tensor
}

fn run() {
    values :: compute.from_list([1.0, 2.0]) ?? panic("tensor")
    box :: TensorBox{values: values}
    print(compute.shape(box.values))
}
