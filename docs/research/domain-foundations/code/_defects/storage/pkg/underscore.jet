use core.files as fs
use "log" as log

fn run() {
    p :: Path.from("/home/nate/.cache/jet-luna/dx3/prim-storage/under.txt")
    _a :: log.append_record(~p, "a") ?? false
    fs.append_all(~p, "b") ?? panic("b")
    print("ok")
}
