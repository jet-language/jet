# Open language-shape work

This file lists open work only. `docs/spec/syntax-decisions.md` is the source
for ratified language law. Tower is the source for current work state.

## Conformance

- Card #560 must enforce the atomic language-shape laws across compiler,
  formatter, tools, examples, and docs. Its scope includes D-SHAPE-PIPE1=C.
- Card #560 must make each inferred fact inspectable and pinnable without a
  second source form.

## Package graph

- Card #532 must settle the remaining Package and jetos graph boundary.
- Its dependent cards must settle composition, overrides, file layout,
  aliases, and output links before #560 can finish its package conformance.
