{
  "name": "tower-board",
  "version": "1.0.0",
  "description": "File-backed project board for a human owner + AI agents. Zero dependencies.",
  "private": true,
  "bin": { "tower": "./tower.mjs" },
  "scripts": {
    "start": "node tower.mjs serve",
    "test": "node --test test/*.test.mjs"
  },
  "engines": { "node": ">=18" }
}
