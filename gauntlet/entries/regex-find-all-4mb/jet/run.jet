use core.files as files
use core.regex as re

#CLI struct Args { path: String }
fn run(args: Args) !IOError -> {
    text :: files.read(args.path)
    matches :: re.find_all(Regex{"struct"}, text)
    print("matches {matches.len()}")
}
