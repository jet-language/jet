use core.files
#CLI struct Args { input: String }
fn run(args: Args) {
    cleaned := (files.read(args.input) ?? panic("read failed"))
        .lines()
        .map(line -> line.trim())
        .filter(line -> line != "")
    cleaned.sort()
    print("lines {cleaned.len()}\n{cleaned.join("\n")}")
}
