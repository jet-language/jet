use core.files
#CLI struct Args { input: String }
fn run(args: Args) {
lines:=(files.read(args.input)??panic("read failed")).lines().filter(line->line.trim()!="").map(line->line.trim())
lines.sort()
print("lines {lines.len()}\n{lines.join("\n")}")
}
