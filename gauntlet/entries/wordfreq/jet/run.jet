#CLI struct Args { path: String }

fn run(args: Args) {
counts::Regex{"[^\t-\r ]+"}.find_all(read_file(args.path)??panic("read")).counts()
loop item in counts.top_n(20)->print("{item.value} {item.key}")
print("distinct {counts.len()} total {counts.values().sum()}")
}
