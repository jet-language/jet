use core.ui as ui

#[Target(Web), HTML("index.html")]
fn run() {
    backend :: ui.null_backend()
    ui.mount(backend, ui.node("reload-before", 180.0, 40.0), ui.constraint(0.0, 0.0, 240.0, 100.0))
}
