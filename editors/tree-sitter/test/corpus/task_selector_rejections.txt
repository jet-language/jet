========================================
task.race named branch is rejected
:error
========================================

fn run() {
    task.race { first: one() }
}

---

=======================================
task.any named branch is rejected
:error
=======================================

fn run() {
    task.any { first: one() }
}

---
