interface:
  display_name: "Tower"
  short_description: "Act on Jet's Tower project board"
  default_prompt: "Use $tower to process the Tower board and advance every unblocked agent-lane card."

policy:
  allow_implicit_invocation: true
