[package]
name = "jet-env-model"
version = "1.0.0"
edition = "2021"

[lib]
name = "jet_env_model"
path = "src/lib.rs"

[dependencies]
jet-codegen = { path = "../jet-codegen" }
jet-pkg-model = { path = "../jet-pkg-model" }
