[package]
name = "jet-store"
version = "1.0.0"
edition = "2021"
description = "Jet machine-wide verified artifact store"

[lib]
name = "jet_store"
path = "src/lib.rs"

[dependencies]
jet-foundation = { path = "../jet-foundation" }
