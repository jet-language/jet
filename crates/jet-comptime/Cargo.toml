[package]
name = "jet-comptime"
version = "1.0.0"
edition = "2021"

[lib]
name = "jet_comptime"
path = "src/lib.rs"

[dependencies]
jet-foundation = { path = "../jet-foundation" }
jet-net = { path = "../jet-net" }
# D-REGEX1: REPL/dev interpreter regex surface (same bootstrap posture as the FFI bridge).
regex = "1"
