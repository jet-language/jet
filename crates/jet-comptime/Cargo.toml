[package]
name = "jet-comptime"
version = "1.0.0"
edition = "2021"

[lib]
name = "jet_comptime"
path = "src/lib.rs"

[dependencies]
jet-foundation = { path = "../jet-foundation" }
jet-parser = { path = "../jet-parser" }
jet-net = { path = "../jet-net" }
