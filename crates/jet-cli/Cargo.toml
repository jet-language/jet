[package]
name = "jet-cli"
version = "1.0.0"
edition = "2021"

[lib]
name = "jet_cli"
path = "src/lib.rs"

[dependencies]
jet-foundation = { path = "../jet-foundation" }
jet-repl = { path = "../jet-repl" }
