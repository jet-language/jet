[package]
name = "jet-rt"
version = "1.0.0"
edition = "2021"
description = "Jet shared runtime helpers for AOT and dev/JIT tiers"

[lib]
name = "jet_rt"
path = "src/lib.rs"

[dependencies]
jet-foundation = { path = "../jet-foundation" }
