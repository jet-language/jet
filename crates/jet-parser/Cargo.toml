[package]
name = "jet-parser"
version = "1.0.0"
edition = "2021"

[lib]
name = "jet_parser"
path = "src/lib.rs"

[dependencies]
jet-foundation = { path = "../jet-foundation" }
jet-lexer = { path = "../jet-lexer" }
