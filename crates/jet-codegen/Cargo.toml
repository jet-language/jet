[package]
name = "jet-codegen"
version = "1.0.0"
edition = "2021"

[lib]
name = "jet_codegen"
path = "src/lib.rs"

[dependencies]
jet-foundation = { path = "../jet-foundation" }
jet-sema = { path = "../jet-sema" }
jet-rt = { path = "../jet-rt" }
jet-pkg-model = { path = "../jet-pkg-model" }
[features]
default = ["jet_native_io"]
jet_native_io = []
