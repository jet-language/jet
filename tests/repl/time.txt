# D-TYPE2-TIME1: REPL uses the registered Time plane for literals, points, and algebra.
> use core.time as time
ok
> wait :: 500ms
> wait.in(.Milliseconds)
500 : Int
> #FX(abilities: Time) { task.timeout(wait); time.sleep(0ms); origin :: time.instant(); later :: origin + 5min; print((later - origin).in(.Minutes) ?? panic("delta")); speed :: 12meter / 2s; print(speed) }
5
6 meter/ns
