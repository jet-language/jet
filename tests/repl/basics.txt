# E2-M18 REPL transcript fixture — basics
# Format: `> input` lines are user inputs; other lines are expected output.
# Lines beginning with `#` are comments.
# Each `> input` is run through run_transcript; output is compared line by line.

# --- arithmetic expression (D-REPL16=B echo) ---
> 1 + 2
3 : Int

# --- binding (no echo for declaration) ---
> x :: 10
# no output expected

# --- expression using binding ---
> x * 2
20 : Int

# --- :type meta-command ---
> :type x
x : Int

# --- print (core.io preload, D-REPL-PRELOAD=A) ---
> print("hello, world")
hello, world

# --- string expression ---
> "abc"
"abc" : String

# --- bool expression ---
> 2 > 1
true : Bool

# --- binding declaration suppresses echo (D-REPL16=B) ---
> _ignored :: 42
# no output expected (binding declarations don't echo)

# --- :reset clears bindings ---
> :reset
session reset

# --- after reset, x is gone ---
> :type x
note: `x` isn't defined in this session

# --- function declaration ---
> fn double(n: Int) -> Int { return n * 2; }
ok

# --- call declared function ---
> double(5)
10 : Int

# --- hard-reject: @Unsafe ---
> @Unsafe { }
error [E1802]: the REPL interpreter can't run `@Unsafe`

# --- :help shows meta-commands ---
> :help
REPL meta-commands

# --- :quit exits ---
> :quit
bye
