Error [E2211]: Jet JIT has a compiler gap for this checked program.
  --> /tmp/env_gap.jet:2:5
    |
  2 | use core.env as env
    |     ^^^^^^^^^
 Why: jet run uses the JIT lens and does not hide a JIT compiler gap by running AOT. JIT gap in run: reads the environment.
 Fix: run `jet build <file>` and the binary for now, then report E2211 with the detail below.

1 problem found
run `jet explain E2211` to learn more
