use core.time as time

fn run() {
    points := (p: Vec3(1.0, 2.0, 3.0), when: time.parse_rfc3339("2026-01-01T00:00:00Z") ?? panic("parse"))
    p := points.p
    p += Vec3.splat(1.0)
    print("{points}")
}
