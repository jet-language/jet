E3004

What this means:
  task cancelled at a cooperative wait point

Why Jet enforces it:
  The task control plane requested cancellation before this wait completed

How to fix it:
  Handle `TaskFailure.Cancelled`, or use `#Shield` around a cancellation-sensitive wait.

This explanation comes from jet's diagnostics reference.
