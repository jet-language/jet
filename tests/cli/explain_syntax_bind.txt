::

What this means:
  SIGIL_BIND_IMMUT: immutable binding or one-line function-body marker

Why Jet enforces it:
  owner decision D-ONELINE-BODY1

How to fix it:
  use `::` at its registered syntax site

Example:
  answer :: 42

This explanation comes from Syntax.rs.
