Error [E1274]: no package record or build attempt for `definitely_missing`
 Why: `jet explain` reads the Hangar Store and persisted Jetpack build attempts; neither contains this package.
 Fix: run `jet build definitely_missing` first, or use `jet explain E1234` for diagnostic-code help.
