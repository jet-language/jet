E3010

What this means:
  `{msg}` — with Jet file and line.

Why Jet enforces it:
  The operands or position do not produce a valid result, so the safe runtime stops instead of returning corrupted data.

How to fix it:
  Check the operands or bounds before the operation, or use a checked operation that returns an outcome.

This explanation comes from jet's diagnostics reference.
