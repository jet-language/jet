$ jet self doctor --plain
[exit: 0]
--- stdout ---
jet self doctor — checking your environment

toolchain:
  [ ok ] rustc — <version>

cache & store:
  [ ok ] build cache — <path>
  [ ok ] package store — <path>

PATH:
  [ ok ] jet — <path>
  [ ok ] rustc on PATH — <path>
  [ ok ] C compiler on PATH — cc (<path>)

language server:
  [ ok ] language server — front-end pipeline responds (run `jet self lsp doctor` for detail)

FFI (C bridge):
  [ ok ] C compiler (cc) — cc: <version>
  [ ok ] cargo (FFI bridge) — <version>
  [warn] pkg-config — not found
       L2101: install pkg-config if an FFI library resolves its link flags through it.

registry:
  [skip] registry — skipped (offline) — pass `--online` to probe it

healthy, with advisories — see the L2101 lines above (run `jet explain L2101`).
--- stderr ---
