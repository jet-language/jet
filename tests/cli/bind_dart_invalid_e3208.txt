Error [E3208]: could not generate bindings from `BAD.jet`.
 Why: Dart entry point `greet` uses unsupported result type `String`.
 Fix: mark top-level scalar Dart callbacks with `@pragma('vm:entry-point')`, pass a valid Jet plugin source with `--jet`, and rerun inside the provisioned Jet environment.
More: jet-lang.dev/e/E3208
