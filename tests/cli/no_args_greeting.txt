Welcome to Jet! (v1.0.0)

the three commands to know:
  jet run    build, then run a program (or a project)
  jet check  look for problems, build nothing
  jet test   compile and run top-level test blocks

try `jet run hello.jet`, or `jet help` to see everything.
