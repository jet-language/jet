$ jet self completions tcsh
[exit: 2]
--- stdout ---
--- stderr ---
Error: `tcsh` isn't a shell jet can generate completions for.
 Why: completions are hand-written per shell; only bash, zsh, and fish are supported.
 Fix: try `jet self completions bash`, `jet self completions zsh`, or `jet self completions fish`.
