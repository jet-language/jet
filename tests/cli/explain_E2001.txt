E2001

What this means:
  This package needs a newer Jet.

Why Jet enforces it:
  Editions opt a project into a specific era of Jet syntax. A newer edition can use syntax this compiler does not understand.

How to fix it:
  Upgrade with `jet upgrade`, or set `edition: "2026"` in `pkg.jet`.

This explanation comes from jet's diagnostics reference.
