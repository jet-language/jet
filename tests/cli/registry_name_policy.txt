Error [E2608]: registry package name `librewolf-fixed-bin` is blocked: it ends with reserved suffix `-bin`
 Why: Package names are trust labels. Jet compares each new name with existing names, folds common homoglyphs, and reserves misleading suffixes so a lookalike cannot borrow an established package's identity.
 Fix: Choose a different package name than `-bin`, remove the reserved suffix if present, then publish again.
