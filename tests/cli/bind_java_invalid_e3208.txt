Error [E3208]: Could not generate bindings from `BAD.jet`.
 Why: `javac` rejected the JVM binding input: <identifier> expected.
 Fix: use a public Java class with one public long/double constructor and non-overloaded long/double methods, then rerun `jet inspect bind java`.
