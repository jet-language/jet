Error [E1239]: `module workspace` is declared in more than one file: `a.jet`, `b.jet`
 Why: the workspace index is discovered by declaration, so exactly one file may declare `module workspace { … }`
 Fix: keep one declaration (conventionally in `workspace.jet`) and delete the others
