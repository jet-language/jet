Warning [L2608] (registry_name_lookalike): registry package name `cagro` is suspicious: it is edit distance 2 from existing package `cargo`
 Why: A name in the warning band is close enough to an existing registry name to confuse package selection, but this publish remains non-blocking while the owner policy allows it.
 Fix: Choose a distinct package name instead of `cargo` before publishing; the closer block band and reserved suffix rules reject stronger matches.
