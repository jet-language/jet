@

What this means:
  COMPTIME_MARK: compile-time demand mark on a name or block

Why Jet enforces it:
  owner decision S57

How to fix it:
  use `@` at its registered syntax site

Example:
  @limit :: 1000

This explanation comes from Syntax.rs.
