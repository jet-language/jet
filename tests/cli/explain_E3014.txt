E3014

What this means:
  `foreign function failed: {msg}`

Why Jet enforces it:
  The foreign implementation or its ABI boundary rejected the call, so Jet stopped instead of fabricating a result.

How to fix it:
  Fix the foreign function or handle its documented failure before calling it again.

This explanation comes from jet's diagnostics reference.
