#compdef 'commands'
# zsh completion from JetCommandSchema v1
if (( CURRENT == 2 )); then
    _values 'command' '--help[show help]' 'serve' 'import'
else
    case $words[2] in
        'serve') _arguments \
    '--port[value for --port]:port:' \
    '--help[show help]' ;;
        'import') _arguments \
    '--file[value for --file]:file:' \
    '--help[show help]' ;;
    esac
fi
