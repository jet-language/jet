#compdef 'commands'
# zsh completion from JetCommandSchema v3
if (( CURRENT == 2 )); then
    _values 'command' '--help[show help]' 'serve' 'import'
else
    case $words[2] in
        'serve') _arguments \
    '(-p --port){-p,--port}[port to listen on]:port:' \
    '--help[show help]' ;;
        'import') _arguments \
    ':file:file to import' \
    '--file[file to import]:file:' \
    '--help[show help]' ;;
    esac
fi
