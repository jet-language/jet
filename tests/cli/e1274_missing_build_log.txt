Error [E1274]: no build log for `definitely_missing`
 Why: `jet logs` reads persisted Jetpack realization attempts; no attempt is recorded for that package.
 Fix: run `jet build definitely_missing` first.
