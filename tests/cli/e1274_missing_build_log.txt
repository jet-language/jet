
  error[E1274]: no build log for `definitely_missing`
    `jet logs` reads persisted Jetpack build attempts; no attempt is recorded for that package.
    fix: run `jet build definitely_missing` first.

