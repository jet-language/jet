error[E1336]: environment image server cannot project services
the image path has no typed service supervisor; starting shell PIDs would bypass readiness, restart, cancellation, and cleanup policy
fix: run the declared services with `jetpack services`, or build an image without `services:` until the one supervisor owns the image runtime
