E3012

What this means:
  stack overflow in `{fn}` — with Jet file, line, and source context

Why Jet enforces it:
  The call stack kept growing without reaching a safe return.

How to fix it:
  End the recursion or make progress toward a base case.

This explanation comes from jet's diagnostics reference.
