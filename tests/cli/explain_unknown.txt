$ jet explain BOGUS
[exit: 2]
--- stdout ---
--- stderr ---
Error: `BOGUS` isn't a diagnostic code jet knows.
 Why: every code jet reports is one of the `E####`/`L####` codes in its diagnostics reference.
 Fix: copy the `E####` code from an error message and pass it to `jet explain`.
