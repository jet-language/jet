:>

What this means:
  OP_UNIFIED_ARROW: callable, arm, and lambda arrow

Why Jet enforces it:
  owner decision D-ARROW-UNIFY1

How to fix it:
  use `:>` at its registered syntax site

Example:
  fn twice(n: Int) :> Int :: n * 2

This explanation comes from Syntax.rs.
