#Live

What this means:
  MARKER_LIVE: terminal direct-input block marker

Why Jet enforces it:
  owner decision D-BLOCKPLANE1

How to fix it:
  use `#Live` at its registered syntax site

Example:
  #Live { input() }

This explanation comes from Syntax.rs.
