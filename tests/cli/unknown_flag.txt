$ jet check --colour x.jet
[exit: 2]
--- stdout ---
--- stderr ---
Error [E2102]: `--colour` isn't a flag jet understands.
 Why: jet ignores no flags silently, so a typo can't quietly change a build.
 Fix: did you mean `--color`? Run `jet help` to see the flags.
