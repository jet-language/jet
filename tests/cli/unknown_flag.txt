$ jet check --colour x.jet
[exit: 2]
--- stdout ---
--- stderr ---
Error [E2102]: `--colour` isn't a flag jet understands.
 Why: flags before `--` belong to jet; everything after `--` is forwarded to your program
 Fix: did you mean `--color`? (run `jet help` for the flags)
