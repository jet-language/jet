$ jet explain e2101
[exit: 0]
--- stdout ---
E2101

What this means:
  `{cmd}` isn't a jet command.

Why Jet enforces it:
  Every jet run starts with a command like `run`, `check`, or `new`.

How to fix it:
  Did you mean `jet {closest}`? Run `jet help` to see them all.


This explanation comes from jet's diagnostics reference.
--- stderr ---
