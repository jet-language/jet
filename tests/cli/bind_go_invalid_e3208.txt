Error [E3208]: Could not generate bindings from `BAD.jet`.
 Why: `go build -buildmode=c-archive` rejected the source: expected operand, found '}'.
 Fix: export `int64`/`float64` scalars or move-only `uintptr` handles with `//export Name`, then rerun `jet inspect bind go`.
