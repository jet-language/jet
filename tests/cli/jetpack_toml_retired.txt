Error [E1225]: `jetpack.toml` is retired.
 Why: Package identity and named sources now live in `package.jet` and `env.jet`, so dependency resolution uses one Jet grammar (D-ONCE-RETIRE1).
 Fix: Move `[repo]` `name`/`version` to `package.jet`, move `[sources]` entries to `env.jet` `sources: { … }`, then delete `jetpack.toml`.
