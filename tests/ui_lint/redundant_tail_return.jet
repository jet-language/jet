fn label(value: Int) String -> {
    if value == {
        // Preserve arm comments when the fix removes `return`.
        1 -> return "one"
    }
    // Preserve comments around the fallback tail.
    return "other"
}

fn run() {
    print(label(1))
}
