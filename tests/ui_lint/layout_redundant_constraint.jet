// E2934: an exact duplicate constraint line in the same `layout { … }` block
// is a lint (compilation still succeeds) — D-LAYOUT1.
fn main() {
    layout form {
        label.width >= 80.0
        label.width >= 80.0
    }
    print("ok")
}
