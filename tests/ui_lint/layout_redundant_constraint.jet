// E2934: an exact duplicate constraint element in the same `Layout.{ … }`
// body is a lint (compilation still succeeds) — D-LAYOUT1 / D-LAYOUT-CTOR1.
fn run() {
    form :: Layout.{
        label.width >= 80.0,
        label.width >= 80.0
    }
    print("ok")
}
