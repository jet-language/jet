Warning [L0302]: these guards all dispatch on `Choice`
  --> tests/ui_lint/subjectless_guard_enum_dispatch.jet:8:5
    |
  8 |     if {
    |     ^^
 Why: subject dispatch makes one closed enum's cases explicit and exhaustively checked
 Fix: write `if choice == { ... }` and put each variant pattern in an arm
