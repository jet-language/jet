Warning [L0201]: implicit clone of `x`; this value is borrowed, so it is copied
  --> tests/ui_lint/json_text_view_clone.jet:3:20
    |
  3 |     j #= Json.Text(x)
    |                    ^
 Why: `Text` stores its own copy of this value
 Fix: write `x .clone()` to copy explicitly and silence this warning
