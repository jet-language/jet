Warning [L1101]: this task still owes `join`
  --> tests/ui_lint/discarded_task.jet:4:5
    |
  4 |     _ :: tasks.spawn(() => 42)
    |     ^
 Why: discarding it into `_` skips the join before the task finishes
 Fix: join it with `.join()`, or write `.detach()` to let it go free
