Warning [L1101]: a spawned task is discarded without `.join()`
  --> tests/ui_lint/discarded_task.jet:4:5
    |
  4 |     _ :: tasks.spawn(() => 42)
    |     ^
 Why: the program may end before this task finishes
 Fix: bind the task and call `.join()`, or chain `.detach()` for fire-and-forget
