Warning [L0301]: head `Red` is unreachable — an earlier head already handles it
  --> tests/ui_lint/multi_head_unreachable.jet:10:9
    |
 10 | fn mark(Red) {
    |         ^^^
 Why: multi-head declaration order selects the first matching head
 Fix: remove this head or merge it with the earlier head
