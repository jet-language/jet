Warning [L0301] (unreachable_dispatch_arm): Head `Red` is unreachable — an earlier head already handles it
  --> tests/ui_lint/multi_head_unreachable.jet:8:9
    |
  8 | fn mark(Red) { print("duplicate") }
    |         ^^^
 Why: Multi-head declaration order selects the first matching head
 Fix: Remove this head or merge it with the earlier head
More: jet-lang.dev/e/L0301
