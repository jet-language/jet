Warning [L0507]: prefer an ordered arm table for this branch
  --> tests/ui_lint/prefer_arm_table.jet:3:5
    |
  3 |     if true {
    |     ^^
 Why: one ordered arm table is Jet's normal form for multi-line and chained choices
 Fix: write `if { condition -> body else -> body }`

Warning [L0507]: prefer an ordered arm table for this branch
  --> tests/ui_lint/prefer_arm_table.jet:10:5
    |
 10 |     if false { print("a") } else if true { print("b") } else { print("c") }
    |     ^^
 Why: one ordered arm table is Jet's normal form for multi-line and chained choices
 Fix: write `if { condition -> body else -> body }`

Warning [L0507]: prefer an ordered arm table for this branch
  --> tests/ui_lint/prefer_arm_table.jet:13:15
    |
 13 |     answer :: if true -> {
    |               ^^
 Why: one ordered arm table is Jet's normal form for multi-line and chained choices
 Fix: write `if { condition -> body else -> body }`
