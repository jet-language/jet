Warning [L3101]: this `@Unsafe` block has no reason
  --> tests/ui_lint/unsafe_missing_audit.jet:3:5
    |
  3 |     @Unsafe {
    |     ^^^^^^^^^
 Why: every gated region records why it can't break memory safety
 Fix: add the reason: `@Unsafe("why this is safe") { … }`
