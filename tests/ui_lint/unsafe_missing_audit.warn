Warning [L3101]: this `#Unsafe` block has no `#Audit` reason
  --> tests/ui_lint/unsafe_missing_audit.jet:5:5
    |
  5 |     #Unsafe {
    |     ^^^^^^^^^
 Why: every gated region records, in one line, why it can't break memory safety
 Fix: add `#Audit("why this is safe")` on the line above
