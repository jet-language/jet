Warning [L0301] (unreachable_dispatch_arm): arm `Red` is unreachable — that case is already handled
  --> tests/ui_lint/dispatch_unreachable_arm.jet:12:9
    |
 12 |         .Red -> { print("dup") }
    |         ^^^^
 Why: every earlier arm already covers this pattern
 Fix: remove this arm or merge it with the one above
