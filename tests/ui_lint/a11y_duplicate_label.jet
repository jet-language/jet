use core.ui as ui

fn main() {
    backend :: ui.null_backend()
    backend.set_focus_group([
        ui.node_role("Submit", 40.0, 10.0, ui.aria_role_button()),
        ui.node_role("Submit", 40.0, 10.0, ui.aria_role_button()),
    ])
    print(backend.focused_label())
}
