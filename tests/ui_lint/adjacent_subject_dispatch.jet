fn run() {
    key :: "rev"
    if key == "revision" || key == "rev" {
        print("revision")
    }
    if key == "path" {
        print("path")
    }
    if key == "profile" {
        print("profile")
    }

    independent :: "ok"
    if independent == "" {
        print("empty")
    }
    if key == "validation" {
        print("validation")
    }
    if independent == "later" {
        print("later")
    }

    left :: "left"
    right :: "right"
    if left == "left" {
        print(left)
    }
    if right == "right" {
        print(right)
    }
    if left == "other" {
        print(left)
    }

    mutable := "one"
    if mutable == "one" {
        mutable = "two"
    }
    if mutable == "two" {
        print(mutable)
    }
    if mutable == "three" {
        print(mutable)
    }

    interposed := "one"
    if interposed == "one" {
        interposed = "two"
    }
    interposed = "three"
    if interposed == "three" {
        interposed = "four"
    }
    if interposed == "four" {
        interposed = "five"
    }

    between := "one"
    if between == "one" {
        print("one")
    }
    between = "two"
    if between == "two" {
        print("two")
    }
    if between == "three" {
        print("three")
    }
}
