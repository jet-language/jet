Warning [L1101] (unjoined_task): `t` still owes `join`
  --> tests/ui_lint/unjoined_task.jet:2:5
    |
  2 |     t :: task 42
    |     ^
 Why: the program may end before this task finishes
 Fix: join it with `.join()`, or write `.detach()` to let it go free
