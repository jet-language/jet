Warning [L1101]: task `t` is dropped without `.join()`
  --> tests/ui_lint/unjoined_task.jet:3:5
    |
  3 |     t :: tasks.spawn(() => 42)
    |     ^
 Why: the program may end before this task finishes
 Fix: call `.join()` on the task before it goes out of scope
