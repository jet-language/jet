Warning [L0301]: arm `Red` is unreachable — that case is already handled
  --> tests/ui_lint/switch_unreachable_arm.jet:15:9
    |
 15 |         Red -> {
    |         ^^^
 Why: every earlier arm already covers this pattern
 Fix: remove this arm or merge it with the one above
