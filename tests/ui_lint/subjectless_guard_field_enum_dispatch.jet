enum State {
    Ready(Int)
    Waiting(Int)
}

struct Message {
    state: State
}

fn run() {
    msg :: Message.{ state: State.Ready(3) }
    if {
        msg.state == .Ready(n) && n > 0 -> print(n)
        msg.state == .Waiting(n) -> print(n)
    }
}
