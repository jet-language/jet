Warning [L0302]: these guards all dispatch on `State`
  --> tests/ui_lint/subjectless_guard_field_enum_dispatch.jet:12:5
    |
 12 |     if {
    |     ^^
 Why: subject dispatch makes one closed enum's cases explicit and exhaustively checked
 Fix: write `if msg.state == { ... }` and put each variant pattern in an arm
