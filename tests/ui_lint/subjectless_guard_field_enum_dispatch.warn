Warning [L0302] (same_enum_guard_table): These arm heads all dispatch on `State`
  --> tests/ui_lint/subjectless_guard_field_enum_dispatch.jet:12:5
    |
 12 |     if {
    |     ^^
 Why: Naming the subject makes one closed enum's cases explicit and exhaustively checked
 Fix: Write `if msg.state == { ... }` and put each variant pattern in an arm
More: jet-lang.dev/e/L0302
