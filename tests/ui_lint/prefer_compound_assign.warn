Warning [L0503]: prefer `p.hp -= …` instead of repeating the left side
  --> tests/ui_lint/prefer_compound_assign.jet:6:10
    |
  6 |     p.hp = p.hp - amount
    |          ^
 Why: compound assignment updates a place in one step without restating it
 Fix: write `p.hp -= …`

Warning [L0503]: prefer `p.hp += …` instead of repeating the left side
  --> tests/ui_lint/prefer_compound_assign.jet:10:10
    |
 10 |     p.hp = p.hp + amount
    |          ^
 Why: compound assignment updates a place in one step without restating it
 Fix: write `p.hp += …`

Warning [L0503]: prefer `n += …` instead of repeating the left side
  --> tests/ui_lint/prefer_compound_assign.jet:15:7
    |
 15 |     n = n + 1
    |       ^
 Why: compound assignment updates a place in one step without restating it
 Fix: write `n += …`

Warning [L0503]: prefer `n += …` instead of repeating the left side
  --> tests/ui_lint/prefer_compound_assign.jet:19:7
    |
 19 |     n = (n + 1)
    |       ^
 Why: compound assignment updates a place in one step without restating it
 Fix: write `n += …`
