Warning [L3101]: this `#Unsafe` function has no reason
  --> tests/ui_lint/unsafe_fn_missing_audit.jet:3:1
    |
  3 | #Unsafe fn unchecked_answer() -> Int {
    | ^^^^^^^
 Why: every gated function records why callers can rely on its unsafe contract
 Fix: add the reason: `#Unsafe("why this is safe") fn ...`
