name: "project-check-entry"
version: "0.1.0"
outputs: {
    release: .Executable{
        entry: entry.launch
    }
}
defaults: {
    run: release
}
authority: {
    holds: {
        allow: [IO]
    }
}
