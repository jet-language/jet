/// Jetdoc fixture module.

/// A documented public record.
pub struct Widget {
    /// The value carried by the widget.
    pub value: Int
}

/// A documented public choice.
pub enum Choice {
    First
    Second
}

/// A documented public protocol-shaped trait.
pub trait Displayable {
    fn display(self) String
}

impl Widget.Displayable {
    fn display(self) String -> "widget"
}

/// A marker declaration is part of the generated reference surface.
marker Checked(@sites: [.Block]) {}

/// An ordered exchange is part of the generated reference surface.
pub protocol Payment {
    client: Hello(version: Int)
    server: Ack(session: Int)
}

/// Return the fixture answer.
/// Example: `answer()`
/// ```jet
/// 1 + 1 // => 2
/// ```
pub fn answer() Int -> 42

fn run() {}
