enum Light { Red Green Yellow }
fn pick() Light -> {
    return Light.Red
}
fn classify() Int -> {
    light :: pick()
    if light == {
        .Red -> { return 1 }
        .Green -> { return 2 }
        else -> { return 0 }
    }
}
fn run() {
    print(classify())
}
