// D-COV1 fixture: `used` is exercised by the test; `unused` is not. Coverage
// reports one HIT and one MISS.
fn used() -> Int {
    return 1
}
fn unused() -> Int {
    return 2
}
fn run() {
    print("cov")
}
#Test("exercise used") {
    require_eq(used(), 1)
}
