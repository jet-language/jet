use core.files as fs

fn run() {
    // Agent-shaped JSON payload via files (json.parse still HostCall-gaps on jet run).
    path :: "/tmp/jet-script-speed-payload.json"
    _ :: fs.write(path, "{{\"n\":1,\"ok\":true}}") ?? panic("write")
    print(fs.read(path) ?? "missing")
}
