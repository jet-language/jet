Error [E1322]: workspace member `../outside` escapes the workspace root
 Why: Package membership is rooted in the workspace and cannot follow an absolute or `..` path
 Fix: use a relative member path below the workspace root, or use `find("./packages")`
