// D-DOTSCOPE1: a `.timeout` region over its budget fails the test (post-hoc).
fn work(n: Int) -> Int {
    total := 0
    loop i in 0..n {
        total = (total + i)
    }
    return total
}
@Test("timeout exceeded") {
    .timeout(1ns) {
        require((work(100000) >= 0))
    }
}
