// simple_exec — a single-file executable with no package.jet (D-ILE1).
// A top-level `fn run` makes this an executable; no manifest is needed (R9).
fn run() {
    print("simple exec, no manifest")
}
