// D-TEST1 fixture: a property that fails, exercising automatic shrinking.
// `n < 50` is false for some generated input; the runner shrinks it to the
// minimal counterexample `n = 50`.
fn run() {
    print("prop")
}
@Test fn always_small(n: Int) {
    require((n < 50))
}
