// Card #2254: non-corpus fixed-width integer hot-loop witness.
fn sum() I64 -> {
    total := I64{0}
    loop _i in 0..<100000 {
        total += I64{1}
    }
    return total
}

fn run() {
    print(sum())
}
