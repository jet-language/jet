// Card #2254: non-corpus integer hot-loop witness.
fn sum(n: Int) Int -> {
    total := 0
    loop i in 0..<n {
        total += keep(i)
    }
    return total
}

fn run() {
    print(sum(10000000))
}
