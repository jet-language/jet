// D-TEST1 fixture: a property test whose parameter type (a user struct) has no
// automatic value generator, so the runner rejects it with E0613.
struct Point {
    x: Int,
    y: Int,
}
fn main() {
    print("p")
}
#Test fn uses_point(pt: Point) {
    require((pt.x == pt.x))
}
