use core.files as fs

trait Hasher {
    fn hash(self) =[]=> Int;
}

struct Doc {
    path: String,
}

impl Doc.Hasher {
    fn hash(self) => Int {
        body :: fs.read(self.path) ?? "";
        return body.len();
    }
}

fn run() {
    d :: Doc.{ path: "x" };
    print(d.hash());
}
