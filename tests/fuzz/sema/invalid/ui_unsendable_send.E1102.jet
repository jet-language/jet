use core.tasks
fn run() {
    greet :: () => { print("hi") }
    // E1102 fires: a closure captured without `take` can't cross a channel
    // send either — the same rule as a spawned task.
    (sender, ch) :: tasks.channel<fn()>()
    sender.send(greet)
}
