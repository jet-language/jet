// D-QUAL2: `derive X` attaches method impls, so X must be a `trait`. A `tag`
// has no methods, so applying one is E0731 — declare it as a `trait` instead.

tag Reviewed { deny: [Net] }

#Reviewed
struct Point {
    x: Int
}

fn run() {
    print("ok")
}
