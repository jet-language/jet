// D-ALLOC1/D-ALLOC2: arena-backed scratch in a tight loop (views stay in region).
use core.mem

fn run() {
    arena :: mem.Arena.new()
    loop i, 0..<3 {
        v :: arena.alloc(i)
        print(v)
    }
    close(^arena)
}
