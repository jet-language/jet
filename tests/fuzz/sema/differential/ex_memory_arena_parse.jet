// D-ALLOC1/D-ALLOC2: arena-backed scratch in a tight loop (views stay in region).
use core.mem

fn run() {
    arena :: mem.Arena.new()
    loop i := 0; i < 3; i += 1 {
        v :: arena.alloc(i)
        print(v)
    }
    arena.free()
}
