// core-conformance: core.http.server.serve_once
use core.http.server as server

fn run() {
    mux :: server.mux()
    result :: server.serve_once("127.0.0.1:65536", mux)
    print(result == .Err(_))
}
