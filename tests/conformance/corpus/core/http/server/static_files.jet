// core-conformance: core.http.server.static_files
use core.http.server as server

fn run() {
    mux :: server.mux()
    print(server.static_files(mux, "/assets", "/__jet_conformance_missing_static_root__"))
}
