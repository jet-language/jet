// core-conformance: core.http.server.access_log
use core.http.client as client
use core.http.server as server

fn run() {
    request :: client.request("GET", "http://127.0.0.1/")
    result :: server.access_log(request, 200)
    print(result)
}
