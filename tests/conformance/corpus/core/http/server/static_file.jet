// core-conformance: core.http.server.static_file
use core.http.server as server

fn run() {
    result :: server.static_file("/__jet_conformance_missing_static_file__", "text/plain")
    print(result == .Err(_))
}
