// core-conformance: core.http.server.cors
use core.http.server as server

fn run() {
    policy :: server.cors_policy(["https://example.test"]) ?? panic("policy")
    mux :: server.mux()
    print(server.cors(mux, policy))
}
