// core-conformance: core.ui.node_role
use core.ui as ui

fn run() {
    result :: ui.node_role("Submit", 40.0, 10.0, ui.aria_role_button())
    assert((result.label == "Submit" && result.width == 40.0 && result.height == 10.0))
}
