// core-conformance: core.ui.key_event
use core.ui as ui

fn run() {
    backend :: ui.null_backend()
    node :: ui.button("Continue")
    backend.mount(node)
    event :: ui.key_event("Enter")
    print(backend.on_event(event))
}
