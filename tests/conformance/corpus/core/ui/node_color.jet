// core-conformance: core.ui.node_color
use core.ui as ui

fn run() {
    result :: ui.node_color("blue", 4.0, 1.0, "#0000ff")
    assert((result.label == "blue" && result.width == 4.0 && result.height == 1.0))
}
