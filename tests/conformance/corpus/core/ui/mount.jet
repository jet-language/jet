// core-conformance: core.ui.mount
use core.ui as ui

fn run() {
    backend :: ui.null_backend()
    tree :: ui.box([ui.text("Ready"), ui.button("Continue")])
    constraint :: ui.constraint(0.0, 0.0, 80.0, 24.0)
    print(ui.mount(backend, tree, constraint))
    print(backend.commands().len())
}
