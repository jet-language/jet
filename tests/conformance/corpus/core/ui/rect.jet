// core-conformance: core.ui.rect
use core.ui as ui

fn run() {
    result :: ui.rect(1.0, 2.0, 3.0, 4.0)
    assert((result.x == 1.0 && result.y == 2.0 && result.width == 3.0 && result.height == 4.0))
}
