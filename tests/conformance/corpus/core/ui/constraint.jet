// core-conformance: core.ui.constraint
use core.ui as ui

fn run() {
    result :: ui.constraint(0.0, 1.0, 80.0, 24.0)
    assert((result.min_width == 0.0 && result.min_height == 1.0 && result.max_width == 80.0 && result.max_height == 24.0))
}
