// core-conformance: core.data.query.sort_by.plan
use core.data as data

fn run() {
    source :: data.query([String]{"b", "a"})
    ordered :: source.sort_by(value -> value)
    print(ordered.plan()[1])
}
