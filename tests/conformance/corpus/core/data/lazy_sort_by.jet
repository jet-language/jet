// core-conformance: core.data.lazy_sort_by
use core.data as data

fn run() {
    source :: data.lazy(data.table([String]{"b", "a"}))
    ordered :: data.lazy_sort_by(source, value -> value)
    print(data.plan(ordered)[1])
}
