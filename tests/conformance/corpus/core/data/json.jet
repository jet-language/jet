// core-conformance: core.data.json
use core.data as data

#Codable
struct Ticket {
    team: String
    minutes: Float
}

fn run() {
    rows :: data.json<Ticket>("[{{\"team\":\"Core\",\"minutes\":4.0}}]") ?? panic("json")
    print(rows.len())
}
