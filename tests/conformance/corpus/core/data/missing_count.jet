// core-conformance: core.data.missing_count
use core.data as data

fn no_value() ?Float -> None

fn run() {
    series :: data.series([Val(Float{1.0}), no_value(), Val(Float{3.0})])
    print(data.missing_count(series))
}
