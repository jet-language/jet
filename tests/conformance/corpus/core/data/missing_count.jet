// core-conformance: core.data.list.optional
use core.data as data
fn no_value() ?Float -> None

fn run() {
    values :: [?Float]{Val(1.0), no_value(), Val(3.0)}
    missing :: data.query(values).filter(value -> value == None).collect() ?? panic("missing")
    print(missing.len())
}
