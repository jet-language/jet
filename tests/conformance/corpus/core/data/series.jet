// core-conformance: core.data.series
use core.data as data

fn no_value() ?Float -> None

fn run() {
    values :: [Val(Float{1.0}), no_value(), Val(Float{3.0})]
    series :: data.series(values)
    print(data.count(series))
}
