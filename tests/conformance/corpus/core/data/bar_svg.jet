// core-conformance: core.data.bar_svg
use core.data as data

fn run() {
    groups :: data.group_count([String]{"A", "B", "A"}, value -> value) ?? panic("groups")
    result :: data.bar_svg(groups) ?? panic("bar svg")
    print(result.len())
}
