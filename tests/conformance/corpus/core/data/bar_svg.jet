// core-conformance: core.data.bar_svg
use core.data as data

fn run() {
    groups :: data.query([String]{"A", "B", "A"})
        .group_by(value -> value)
        .count()
        .collect() ?? panic("groups")
    result :: data.bar_svg(groups) ?? panic("bar svg")
    print(result.len())
}
