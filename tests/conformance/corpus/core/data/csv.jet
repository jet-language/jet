// core-conformance: core.data.csv
use core.data as data

#Codable
struct Ticket {
    team: String
    minutes: Float
}

fn run() {
    rows :: data.csv<Ticket>("team,minutes\nCore,4.0\nTools,8.0") ?? panic("csv")
    print(rows.len())
}
