// core-conformance: core.data.query.sort_by
use core.data as data

fn run() {
    values :: [String]{"b", "a"}
    sorted :: data.query(values).sort_by(value -> value).collect() ?? panic("sort")
    print(sorted[0])
}
