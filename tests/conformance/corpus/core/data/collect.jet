// core-conformance: core.data.collect
use core.data as data

fn run() {
    table :: data.table([Int]{3, 1})
    lazy :: data.lazy(table)
    result :: data.collect(lazy) ?? panic("collect")
    print(data.count(result))
}
