// core-conformance: core.data.lazy_filter
use core.data as data

fn run() {
    source :: data.lazy(data.table([Int]{1, 2}))
    filtered :: data.lazy_filter(source, value -> value > 1)
    print(data.plan(filtered)[1])
}
