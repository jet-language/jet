// core-conformance: core.data.query.filter.plan
use core.data as data

fn run() {
    source :: data.query([Int]{1, 2})
    filtered :: source.filter(value -> value > 1)
    print(filtered.plan()[1])
}
