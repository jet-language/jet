// core-conformance: core.data.group_count
use core.data as data

fn run() {
    groups :: data.group_count([String]{"Core", "Tools", "Core"}, value -> value) ?? panic("group count")
    print(groups[0].count)
}
