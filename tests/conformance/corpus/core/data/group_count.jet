// core-conformance: core.data.query.group_by.count
use core.data as data

fn run() {
    groups :: data.query([String]{"Core", "Tools", "Core"})
        .group_by(value -> value)
        .count()
        .collect() ?? panic("group count")
    print(groups[0].value)
}
