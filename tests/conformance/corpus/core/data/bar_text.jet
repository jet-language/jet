// core-conformance: core.data.bar_text
use core.data as data

fn run() {
    groups :: data.query([String]{"A", "B", "A"})
        .group_by(value -> value)
        .count()
        .collect() ?? panic("groups")
    result :: data.bar_text(groups) ?? panic("bar text")
    print(result)
}
