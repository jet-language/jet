// core-conformance: core.data.plot.bar_text
use core.data.plot as plot

fn run() {
    groups :: [Group<String, Int>]{Group{key: "A", value: 2}}
    result :: plot.bar_text(groups) ?? panic("bar text")
    print(result.len())
}
