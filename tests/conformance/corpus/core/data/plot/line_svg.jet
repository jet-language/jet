// core-conformance: core.data.plot.line_svg
use core.data.plot as plot

fn run() {
    groups :: [DataGroup]{DataGroup{key: "A", count: 2, sum: 3.0, mean: 1.5}}
    options :: DataLineOptions{
        title: "Values",
        x_label: "x",
        y_label: "y",
        markers: true,
        reference: Val(1.5),
        style: "solid",
        color: "#2f6f73",
        legend: "value"
    }
    result :: plot.line_svg(groups, options) ?? panic("line svg")
    print(result.len())
}
