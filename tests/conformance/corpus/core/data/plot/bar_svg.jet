// core-conformance: core.data.plot.bar_svg
use core.data.plot as plot

fn run() {
    groups :: [Group<String, Int>]{Group{key: "A", value: 2}}
    result :: plot.bar_svg(groups) ?? panic("bar svg")
    print(result.len())
}
