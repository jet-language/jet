// core-conformance: core.data.query.filter
use core.data as data

fn run() {
    selected :: data.query([Int]{1, 2, 3}).filter(value -> value > 1).collect() ?? panic("filter")
    print(selected.len())
}
