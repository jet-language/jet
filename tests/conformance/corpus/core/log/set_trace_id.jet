// core-conformance: core.log.set_trace_id
use core as core
use core.log as log

fn run() {
    _state :: core.service.state_store("trace-state") ?? panic("state")
    print((() -> {
        log.set_trace_id("trace-conformance")
        true
    })())
}
