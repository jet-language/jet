// core-conformance: core.sys.umask
use core.sys as os

fn run() {
    #Unsafe("set a deterministic mask for this short-lived witness") {
        mask :: os.umask(0)
        print(mask >= 0 && mask <= 511)
    }
}
