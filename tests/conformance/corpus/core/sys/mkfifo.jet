// core-conformance: core.sys.mkfifo
use core.sys as os

fn run() {
    #Unsafe("probe FIFO creation with an empty path") {
        print(os.mkfifo(Path.from(""), 420) ? ok -> true ! error -> false)
    }
}
