// core-conformance: core.sys.wait
use core.sys as os

fn run() {
    #Unsafe("wait for a child that this witness does not have") {
        result :: os.wait() ?? -1
        print(result >= 0)
    }
}
