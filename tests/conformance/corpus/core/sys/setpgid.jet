// core-conformance: core.sys.setpgid
use core.sys as os

fn run() {
    #Unsafe("set the current process into its existing process group") {
        result :: os.setpgid(0, 0)
        print(result == .Ok(()))
    }
}
