// core-conformance: core.sys.setpriority
use core.sys as os

fn run() {
    #Unsafe("lower the current process priority for this short-lived witness") {
        print(os.setpriority(0, 19) ? ok -> true ! error -> false)
    }
}
