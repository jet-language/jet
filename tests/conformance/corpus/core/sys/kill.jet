// core-conformance: core.sys.kill
use core.sys as os

fn run() {
    #Unsafe("check the process group without sending a signal") {
        result :: os.kill(0, 0)
        print(result == .Ok(()))
    }
}
