// core-conformance: core.sys.getpriority
use core.sys as os

fn run() {
    #Unsafe("read the current process priority") {
        result :: os.getpriority(0) ?? -21
        print(result >= -20)
    }
}
