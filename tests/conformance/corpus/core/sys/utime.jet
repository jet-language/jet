// core-conformance: core.sys.utime
use core.sys as os

fn run() {
    #Unsafe("probe timestamp update with an empty path") {
        result :: os.utime(Path.from(""), 0, 0)
        print(result == .Ok(()))
    }
}
