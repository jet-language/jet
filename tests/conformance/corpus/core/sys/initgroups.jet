// core-conformance: core.sys.initgroups
use core.sys as os

fn run() {
    #Unsafe("probe initgroups with a stable missing user") {
        result :: os.initgroups("__jet_conformance_missing_user_2286__", -1)
        print(result == .Ok(()))
    }
}
