// core-conformance: core.encoding.csv.decode
use core.encoding.csv as csv

#Codable
struct Row {
    name: String
    score: Int
}

fn run() {
    result :: csv.decode<Row>("name,score\nada,9\n") ?? panic("decode")
    print(result[0].score)
}
