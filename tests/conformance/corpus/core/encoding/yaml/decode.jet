// core-conformance: core.encoding.yaml.decode
use core.encoding.yaml as yaml

#Codable
struct Config {
    title: String
}

fn run() {
    result :: yaml.decode<Config>("title: jet\n") ?? panic("decode")
    print(result.title)
}
