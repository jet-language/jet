// core-conformance: core.encoding.xml.to_string
use core.encoding.xml as xml

fn run() {
    document :: xml.parse("<r>ok</r>") ?? panic("parse")
    result :: xml.to_string(document)
    print(result)
}
