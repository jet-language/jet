// core-conformance: core.encoding.xml.root
use core.encoding.xml as xml

fn run() {
    document :: xml.parse("<r>ok</r>") ?? panic("parse")
    result :: xml.root(document) ?? panic("root")
    print(result)
}
