// core-conformance: core.encoding.xml.content
use core.encoding.xml as xml

fn run() {
    document :: xml.parse("<r>ok</r>") ?? panic("parse")
    root :: xml.root(document) ?? panic("root")
    result :: xml.content(root) ?? panic("content")
    print(result.len())
}
