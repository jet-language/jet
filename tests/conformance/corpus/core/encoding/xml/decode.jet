// core-conformance: core.encoding.xml.decode
use core.encoding.xml as xml

#Codable
struct Item {
    #Rename("$text") value: String
}

fn run() {
    result :: xml.decode<Item>("<item>ok</item>") ?? panic("decode")
    print(result.value)
}
