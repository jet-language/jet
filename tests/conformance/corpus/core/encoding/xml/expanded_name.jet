// core-conformance: core.encoding.xml.expanded_name
use core.encoding.xml as xml

fn run() {
    document :: xml.parse("<r>ok</r>") ?? panic("parse")
    root :: xml.root(document) ?? panic("root")
    result :: xml.expanded_name(root) ?? panic("expanded_name")
    print(result.local)
}
