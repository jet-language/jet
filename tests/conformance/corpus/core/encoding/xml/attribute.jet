// core-conformance: core.encoding.xml.attribute
use core.encoding.xml as xml

fn run() {
    document :: xml.parse("<r id=\"7\">ok</r>") ?? panic("parse")
    root :: xml.root(document) ?? panic("root")
    result :: xml.attribute(root, "id")
    print((result ?? panic("attribute")) ?? "missing")
}
