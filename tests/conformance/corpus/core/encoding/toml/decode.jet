// core-conformance: core.encoding.toml.decode
use core.encoding.toml as toml

#Codable
struct Config {
    title: String
}

fn run() {
    result :: toml.decode<Config>("title = \"jet\"\n") ?? panic("decode")
    print(result.title)
}
