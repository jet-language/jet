// core-conformance: core.encoding.toml.to_string
use core.encoding.toml as toml

fn run() {
    value :: DataTree.Object(["title": DataTree.Text("jet")])
    result :: toml.to_string(value)
    print(result)
}
