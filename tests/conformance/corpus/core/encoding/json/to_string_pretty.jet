// core-conformance: core.encoding.json.to_string_pretty
use core.encoding.json as json

fn run() {
    value :: DataTree.Object(["a": DataTree.Int(1)])
    result :: json.to_string_pretty(value)
    print(result)
}
