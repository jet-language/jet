// core-conformance: core.encoding.json.canonical
use core.encoding.json as json

fn run() {
    value :: DataTree.Object(["b": DataTree.Int(2), "a": DataTree.Int(1)])
    result :: json.canonical(value)
    print(result)
}
