// core-conformance: core.encoding.json.to_string
use core.encoding.json as json

fn run() {
    value :: DataTree.Object(["a": DataTree.Int(1)])
    result :: json.to_string(value)
    print(result)
}
