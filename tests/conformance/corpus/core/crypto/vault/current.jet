// core-conformance: core.crypto.vault.current
use core.crypto as crypto
use core.crypto.vault as vault

fn run() -[Secret]> {
    result :: vault.current<crypto.SigningKey>("__core_conformance_missing_2286")
    print(result == .Err(_))
}
