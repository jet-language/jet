// core-conformance: core.crypto.vault.current
use core.crypto as crypto
use core.crypto.vault as vault

fn run() -[Secret, IO]> {
    result :: vault.current<crypto.SigningKey>("__core_conformance_missing_2286") ?? return
    print(result)
}
