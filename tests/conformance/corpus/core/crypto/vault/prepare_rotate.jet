// core-conformance: core.crypto.vault.prepare_rotate
use core.crypto as crypto
use core.crypto.vault as vault

fn run() -[Secret]> {
    result :: vault.prepare_rotate<crypto.SigningKey>("")
    print(result == .Err(_))
}
