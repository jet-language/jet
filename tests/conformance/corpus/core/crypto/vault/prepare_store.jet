// core-conformance: core.crypto.vault.prepare_store
use core.crypto as crypto
use core.crypto.vault as vault

fn run() -[Secret]> {
    key :: crypto.SigningKey.new_random() ?? return
    result :: vault.prepare_store<crypto.SigningKey>("", ^key)
    print(result == .Err(_))
}
