// core-conformance: core.crypto.vault.versions
use core.crypto as crypto
use core.crypto.vault as vault

fn run() -[Secret, IO]> {
    result :: vault.versions<crypto.SigningKey>("__core_conformance_missing_2286") ?? return
    print(result)
}
