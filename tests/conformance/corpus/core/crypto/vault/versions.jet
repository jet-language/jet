// core-conformance: core.crypto.vault.versions
use core.crypto as crypto
use core.crypto.vault as vault

fn run() -[Secret]> {
    result :: vault.versions<crypto.SigningKey>("__core_conformance_missing_2286")
    print(result == .Err(_))
}
