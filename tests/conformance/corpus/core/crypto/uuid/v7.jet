// core-conformance: core.crypto.uuid.v7
use core.crypto.uuid as uuid

fn run() {
    clock :: Clock.new(1700000000000)
    result :: uuid.v7(clock)
    print(result.len() == 36)
    print(result.slice(14, 14) == "7")
}
