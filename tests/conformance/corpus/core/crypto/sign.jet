// core-conformance: core.crypto.sign
use core.crypto as crypto

fn run() {
    key :: crypto.SigningKey.new_random() ?? panic("signing key")
    message :: [U8]{0x6a, 0x65, 0x74}
    result :: crypto.sign(key, message) ?? panic("sign")
    print(result.bytes().len() == 64)
}
