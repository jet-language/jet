// core-conformance: core.crypto.password_verify
use core.crypto as crypto

fn run() {
    password :: crypto.Secret.from_text("correct horse battery staple")
    stored :: crypto.password_hash(password) ?? return Err("password hash")
    result :: crypto.password_verify(password, stored) ?? return Err("password verify")
    print(result)
}
