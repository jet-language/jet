// core-conformance: core.process.pipeline
use core.process as process

fn run() {
    first :: process.cmd(["printf", "pipeline"])
    second :: process.cmd(["cat"])
    result :: process.pipeline([first, second]) ?? panic("pipeline")
    print(result.output)
}
