// core-conformance: core.tasks.yield_now
use core.tasks as tasks

fn run() {
    before :: tasks.current_task()
    print(tasks.yield_now())
    after :: tasks.current_task()
    print(before == after)
}
