// core-conformance: core.service.runtime
use core.service as service

fn run() {
    retention :: Duration.milliseconds(1000) ?? panic("duration")
    result :: service.runtime("fixture-store", retention: retention)
    print(result)
}
