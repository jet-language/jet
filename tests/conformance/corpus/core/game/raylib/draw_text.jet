// core-conformance: core.game.raylib.draw_text
use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 200, "text")
    color :: rl.color(240, 240, 240, 255)
    print(rl.draw_text("Jet", 8, 8, 16, color))
    rl.close_window(window)
}
