// core-conformance: core.game.raylib.window_should_close
use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 200, "should-close")
    result :: rl.window_should_close(window)
    assert((result == true))
    rl.close_window(window)
}
