// core-conformance: core.game.raylib.play_sound
use core.game.raylib as rl

fn run() {
    sound :: rl.load_sound("assets/jump.wav")
    result :: rl.play_sound(sound)
    assert((result == true))
}
