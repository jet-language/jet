// core-conformance: core.game.raylib.end_drawing
use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 200, "end")
    rl.begin_drawing(window)
    print(rl.end_drawing())
    rl.close_window(window)
}
