// core-conformance: core.game.raylib.begin_drawing
use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 200, "begin")
    print(rl.begin_drawing(window))
    rl.end_drawing()
    rl.close_window(window)
}
