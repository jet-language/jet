// core-conformance: core.game.raylib.color
use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 200, "color")
    result :: rl.color(16, 32, 48, 255)
    print(rl.clear_background(result))
    rl.close_window(window)
}
