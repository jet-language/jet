// core-conformance: core.game.raylib.window_ready
use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 200, "ready")
    result :: rl.window_ready(window)
    assert((result == false))
    rl.close_window(window)
}
