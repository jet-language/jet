// core-conformance: core.game.raylib.window_open
use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 200, "open")
    print(rl.window_ready(window))
    rl.close_window(window)
}
