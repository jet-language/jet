// core-conformance: core.game.raylib.draw_rectangle
use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 200, "rectangle")
    color :: rl.color(240, 200, 80, 255)
    print(rl.draw_rectangle(8, 8, 32, 16, color))
    rl.close_window(window)
}
