// core-conformance: core.game.raylib.clear_background
use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 200, "clear")
    color :: rl.color(16, 32, 48, 255)
    print(rl.clear_background(color))
    rl.close_window(window)
}
