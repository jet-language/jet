// core-conformance: core.time.zoned
use core.time as time

fn run() {
    date_time :: time.from_timestamp(1710502245)
    zone :: time.utc()
    value :: time.zoned(date_time, zone)
    print(value.offset_seconds())
}
