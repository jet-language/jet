// core-conformance: core.time.zoned_local
use core.time as time

fn run() {
    date :: time.new(2024, 3, 15)
    clock :: time.time(12, 30, 45)
    zone :: time.utc()
    value :: time.zoned_local(date, clock, zone)
    print(value.offset_seconds())
}
