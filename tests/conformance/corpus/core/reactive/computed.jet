// core-conformance: core.reactive.computed
use core.reactive as reactive

fn run() {
    source :: reactive.signal(2)
    value :: reactive.computed(() -> source.get() + 1)
    print(value.get())
    source.set(4)
    print(value.get())
}
