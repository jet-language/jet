// core-conformance: core.reactive.effect
use core.reactive as reactive

fn run() {
    source :: reactive.signal(1)
    effect :: reactive.effect(() -> print(source.get()))
    source.set(2)
    print(effect.is_active())
}
