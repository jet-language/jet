// core-conformance: core.reactive.derived
use core.reactive as reactive

fn run() {
    source :: reactive.signal(2)
    value :: reactive.derived(() -> source.get() + 1)
    print(value.get())
    source.set(4)
    print(value.get())
}
