// core-conformance: core.db.policy
use core.db as db

fn run() {
    connection :: db.open_memory()
    policy :: db.policy("items", "true") ?? panic("policy")
    print(db.policy_audit(connection.with_policy(policy, "owner")))
}
