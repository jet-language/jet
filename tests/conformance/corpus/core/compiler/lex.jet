// core-conformance: core.compiler.lex
use core.compiler as compiler

@source :: "fn generated() String -> {{ return \"ok\" }}"
@lexed :: compiler.lex(@source) ?? panic("lex")
@token_count :: @lexed.tokens().len()

fn run() {
    print(@token_count)
}
