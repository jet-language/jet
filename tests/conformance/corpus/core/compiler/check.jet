// core-conformance: core.compiler.check
use core.compiler as compiler

@source :: "fn generated() String -> {{ return \"ok\" }}"
@parsed :: compiler.parse(@source) ?? panic("parse")
@checked :: compiler.check(@parsed) ?? panic("check")
@function_count :: @checked.functions().len()

fn run() {
    print(@function_count)
}
