// core-conformance: core.regex.compile_with
use core.regex as re

fn run() {
    flags :: re.flags(true, false, false)
    result :: re.compile_with("[a-z]+", flags) ?? panic("compile_with")
    print(result.is_match("JET"))
}
