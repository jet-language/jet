// core-conformance: core.regex.replace
use core.regex as re

fn run() {
    result :: re.replace(Regex{"[0-9]+"}, "#", "id=42")
    print(result)
    first :: re.replace(Regex{"[0-9]+"}, "#", "id=42 zip=90210", mode: .First)
    print(first)
}
