// core-conformance: core.auth.oauth_finish
use core.auth as auth

fn run() {
    print(auth.session_id(auth.oauth_finish("state", "alice", 1_700_000_000_000, 3_600_000) ?? panic("oauth rejected")))
}
