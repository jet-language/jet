// core-conformance: core.net.udp_set_timeout
use core.net as net

fn run() {
    socket :: net.udp_bind("127.0.0.1:0") ?? panic("bind")
    print((() -> {
        net.udp_set_timeout(socket, 1000) ?? panic("timeout")
        true
    })())
}
