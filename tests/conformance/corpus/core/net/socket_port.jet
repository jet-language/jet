// core-conformance: core.net.socket_port
use core.net as net

fn run() {
    address :: net.socket_addr("127.0.0.1", 8080) ?? panic("address")
    result :: net.socket_port(address)
    print(result)
}
