// core-conformance: core.net.listener_local_socket_addr
use core.net as net

fn run() {
    listener :: net.tcp_listen("127.0.0.1:0") ?? panic("listen")
    result :: net.listener_local_socket_addr(listener) ?? panic("address")
    print(net.socket_host(result))
}
