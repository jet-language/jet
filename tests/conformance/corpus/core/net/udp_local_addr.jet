// core-conformance: core.net.udp_local_addr
use core.net as net

fn run() {
    socket :: net.udp_bind("127.0.0.1:0") ?? panic("bind")
    result :: net.udp_local_addr(socket) ?? panic("local address")
    print(net.socket_host(result))
}
