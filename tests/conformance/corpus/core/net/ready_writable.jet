// core-conformance: core.net.ready_writable
use core.net as net

fn run() {
    listener :: net.tcp_listen("127.0.0.1:0") ?? panic("listen")
    address :: net.socket_to_string(net.listener_local_socket_addr(listener) ?? panic("address"))
    client :: net.tcp_connect(address) ?? panic("connect")
    _server :: net.tcp_accept(listener) ?? panic("accept")
    ready :: net.tcp_ready(&client, NetReadyInterest.Write, 1000) ?? panic("ready")
    result :: net.ready_writable(ready)
    print(result)
}
