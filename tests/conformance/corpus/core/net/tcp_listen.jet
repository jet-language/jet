// core-conformance: core.net.tcp_listen
use core.net as net

fn run() {
    result :: net.tcp_listen("127.0.0.1:0") ?? panic("listen")
    print(net.socket_host(net.listener_local_socket_addr(result) ?? panic("address")))
}
