// core-conformance: core.net.dns_ptr
use core.net as net

fn run() {
    result :: net.dns_ptr("1.0.0.127.in-addr.arpa", 0)
    print(result == {
        .Ok(_) -> false
        .Err(_) -> true
    })
}
