// core-conformance: core.net.dns_srv
use core.net as net

fn run() {
    result :: net.dns_srv("_jet._tcp.example.test", 0)
    print(result == {
        .Ok(_) -> false
        .Err(_) -> true
    })
}
