// core-conformance: core.net.udp_bind
use core.net as net

fn run() {
    result :: net.udp_bind("127.0.0.1:0") ?? panic("bind")
    address :: net.udp_local_addr(result) ?? panic("address")
    print(net.socket_host(address))
}
