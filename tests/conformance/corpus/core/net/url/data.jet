// core-conformance: core.net.url.data
use core.net.mime as mime
use core.net.url as url

fn run() {
    content_type :: mime.parse("text/plain") ?? panic("mime")
    result :: url.data(content_type, "fixture")
    print(result.scheme())
}
