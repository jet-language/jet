// core-conformance: core.net.url.from_parts
use core.net.url as url

fn run() {
    result :: url.from_parts("https", "example.test", "/a", [["x", "1"]], "frag") ?? panic("parts")
    print(result.to_string())
}
