// core-conformance: core.net.ws.upgrade
use core.http.client as http_client
use core.net.ws as ws

fn run() {
    request :: http_client.request("GET", "http://127.0.0.1/")
    print(ws.upgrade(request) ? ok -> false ! error -> true)
}
