// core-conformance: core.net.ws.connect
use core.net.ws as ws

fn run() {
    result :: ws.connect("http://127.0.0.1/")
    print(result == {
        .Ok(_) -> false
        .Err(_) -> true
    })
}
