// core-conformance: core.net.error_os_code
use core.net as net

fn run() {
    if net.ip_addr("not-an-ip") == {
        .Ok(_) -> panic("invalid address accepted")
        .Err(error) -> print(net.error_os_code(error))
    }
}
