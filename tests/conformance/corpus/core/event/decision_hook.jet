// core-conformance: core.event.decision_hook
use core.event as _event

fn run() {
    hook :: _event.decision_hook<Int, String>(HookPolicy.FirstCancelElseTransform)
    print(hook.listener_count())
    hook.run(4).drop("the witness only needs hook completion")
    print(true)
}
