// core-conformance: core.compute.sparse_nnz
use core.compute as compute

fn run() {
    source :: compute.matrix(2, 2, 1.0) ?? panic("source")
    sparse :: compute.to_sparse(source) ?? panic("sparse")
    result :: compute.sparse_nnz(sparse)
    print(result)
}
