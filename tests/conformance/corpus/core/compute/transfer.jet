// core-conformance: core.compute.transfer
use core.compute as compute

fn run() {
    source :: compute.matrix(2, 2, 1.0) ?? panic("source")
    result :: compute.transfer(source, compute.device_cpu()) ?? panic("transfer")
    print(compute.device(result))
}
