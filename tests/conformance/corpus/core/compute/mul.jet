// core-conformance: core.compute.mul
use core.compute as compute

fn run() {
    left :: compute.from_list([2.0, 3.0]) ?? panic("left")
    right :: compute.from_list([4.0, 5.0]) ?? panic("right")
    result :: compute.mul(left, right) ?? panic("mul")
    print(compute.to_list(result))
}
