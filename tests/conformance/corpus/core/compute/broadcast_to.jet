// core-conformance: core.compute.broadcast_to
use core.compute as compute

fn run() {
    source :: compute.vec(3, 2.0) ?? panic("source")
    result :: compute.broadcast_to(source, [2, 3]) ?? panic("broadcast")
    print(compute.to_list(result))
}
