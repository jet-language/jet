// core-conformance: core.compute.abs
use core.compute as compute

fn run() {
    source :: compute.from_list([-1.0, 2.0]) ?? panic("source")
    result :: compute.abs(source) ?? panic("abs")
    print(compute.to_list(result))
}
