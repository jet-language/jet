// core-conformance: core.compute.sub
use core.compute as compute

fn run() {
    left :: compute.from_list([5.0, 7.0]) ?? panic("left")
    right :: compute.from_list([2.0, 3.0]) ?? panic("right")
    result :: compute.sub(left, right) ?? panic("sub")
    print(compute.to_list(result))
}
