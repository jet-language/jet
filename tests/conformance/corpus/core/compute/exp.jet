// core-conformance: core.compute.exp
use core.compute as compute

fn run() {
    source :: compute.from_list([0.0, 1.0]) ?? panic("source")
    result :: compute.exp(source) ?? panic("exp")
    print(compute.to_list(result))
}
