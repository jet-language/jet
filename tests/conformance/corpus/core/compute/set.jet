// core-conformance: core.compute.set
use core.compute as compute

fn run() {
    tensor := compute.vec(2, 0.0) ?? panic("tensor")
    compute.set(&tensor, [1], 7.0) ?? panic("set")
    print(compute.to_list(tensor))
}
