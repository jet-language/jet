// core-conformance: core.compute.get
use core.compute as compute

fn run() {
    source :: compute.from_list([3.0, 4.0]) ?? panic("source")
    result :: compute.get(source, [1]) ?? panic("get")
    print(result)
}
