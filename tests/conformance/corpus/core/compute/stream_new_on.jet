// core-conformance: core.compute.stream_new_on
use core.compute as compute

fn run() {
    result :: compute.stream_new_on(compute.device_cpu()) ?? panic("stream")
    print(compute.stream_show(result))
}
