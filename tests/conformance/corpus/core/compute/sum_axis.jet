// core-conformance: core.compute.sum_axis
use core.compute as compute

fn run() {
    source :: compute.matrix(2, 3, 1.0) ?? panic("source")
    result :: compute.sum_axis(source, 0) ?? panic("sum axis")
    print(compute.to_list(result))
}
