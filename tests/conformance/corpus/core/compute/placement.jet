// core-conformance: core.compute.placement
use core.compute as compute

fn run() {
    source :: compute.matrix(2, 2, 1.0) ?? panic("source")
    result :: compute.placement(source)
    print(result.len() > 0)
}
