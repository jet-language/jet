// core-conformance: core.compute.transpose
use core.compute as compute

fn run() {
    source :: compute.matrix(2, 3, 1.0) ?? panic("source")
    result :: compute.transpose(source) ?? panic("transpose")
    print(compute.shape(result))
}
