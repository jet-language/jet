// core-conformance: core.compute.div
use core.compute as compute

fn run() {
    left :: compute.from_list([6.0, 8.0]) ?? panic("left")
    right :: compute.from_list([2.0, 4.0]) ?? panic("right")
    result :: compute.div(left, right) ?? panic("div")
    print(compute.to_list(result))
}
