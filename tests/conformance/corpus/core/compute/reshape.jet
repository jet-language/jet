// core-conformance: core.compute.reshape
use core.compute as compute

fn run() {
    source :: compute.from_list([1.0, 2.0, 3.0, 4.0]) ?? panic("source")
    result :: compute.reshape(source, [2, 2]) ?? panic("reshape")
    print(compute.shape(result))
}
