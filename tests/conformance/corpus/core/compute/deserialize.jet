// core-conformance: core.compute.deserialize
use core.compute as compute

fn run() {
    source :: compute.from_list([1.0, 2.0]) ?? panic("source")
    wire :: compute.serialize(source) ?? panic("serialize")
    result :: compute.deserialize(wire) ?? panic("deserialize")
    print(compute.to_list(result))
}
