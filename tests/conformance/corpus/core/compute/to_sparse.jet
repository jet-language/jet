// core-conformance: core.compute.to_sparse
use core.compute as compute

fn run() {
    source :: compute.matrix(2, 2, 1.0) ?? panic("source")
    result :: compute.to_sparse(source) ?? panic("sparse")
    print(compute.sparse_show(result))
}
