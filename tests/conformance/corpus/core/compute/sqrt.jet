// core-conformance: core.compute.sqrt
use core.compute as compute

fn run() {
    source :: compute.from_list([0.0, 4.0]) ?? panic("source")
    result :: compute.sqrt(source) ?? panic("sqrt")
    print(compute.to_list(result))
}
