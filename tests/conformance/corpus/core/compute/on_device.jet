// core-conformance: core.compute.on_device
use core.compute as compute

fn run() {
    source :: compute.matrix(2, 2, 1.0) ?? panic("source")
    result :: compute.on_device(source, compute.device_cpu()) ?? panic("on device")
    print(compute.device(result))
}
