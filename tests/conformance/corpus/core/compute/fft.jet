// core-conformance: core.compute.fft
use core.compute as compute

fn run() {
    signal :: compute.from_list([1.0, 0.0, -1.0, 0.0]) ?? panic("signal")
    result :: compute.fft(signal) ?? panic("fft")
    print(compute.numel(result))
}
