// core-conformance: core.compute.matmul
use core.compute as compute

fn run() {
    left :: compute.matrix(2, 2, 2.0) ?? panic("left")
    right :: compute.matrix(2, 2, 3.0) ?? panic("right")
    result :: compute.matmul(left, right) ?? panic("matmul")
    print(compute.to_list(result))
}
