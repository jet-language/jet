// core-conformance: core.compute.serialize
use core.compute as compute

fn run() {
    source :: compute.from_list([1.0, 2.0]) ?? panic("source")
    result :: compute.serialize(source) ?? panic("serialize")
    print(result.len() > 0)
}
