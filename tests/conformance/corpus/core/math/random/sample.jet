// core-conformance: core.math.random.sample
use core.math.random as random

fn run() {
    random.seed(7)
    items :: [String]{"red", "green", "blue"}
    sample :: random.sample(items, 2)
    print(sample.len() == 2)
}
