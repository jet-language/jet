// core-conformance: core.math.random.weighted_pick
use core.math.random as random

fn run() {
    random.seed(7)
    items :: [String]{"red", "green", "blue"}
    weights :: [Float]{0.0, 1.0, 0.0}
    choice :: random.weighted_pick(items, weights) ?? "none"
    print(choice == "green")
}
