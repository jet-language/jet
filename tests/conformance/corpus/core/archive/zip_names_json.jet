// core-conformance: core.archive.zip_names_json
use core.archive as archive

fn run() {
    data :: [U8]{72, 101, 108, 108, 111}
    zip :: archive.zip_compress("entry.txt", data)
    result :: archive.zip_names_json(zip)
    print(result)
}
