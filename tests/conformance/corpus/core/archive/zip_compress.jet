// core-conformance: core.archive.zip_compress
use core.archive as archive

fn run() {
    data :: [U8]{72, 101, 108, 108, 111}
    result :: archive.zip_compress("entry.txt", data)
    print(result.len())
}
