// core-conformance: core.archive.zip_write
use core.archive as archive

fn run() {
    data :: [U8]{72, 101, 108, 108, 111}
    writer :: archive.zip_open([U8]{})
    result :: archive.zip_write(writer, "entry.txt", data)
    print(result.len())
}
