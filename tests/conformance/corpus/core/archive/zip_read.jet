// core-conformance: core.archive.zip_read
use core.archive as archive

fn run() {
    data :: [U8]{72, 101, 108, 108, 111}
    zip :: archive.zip_compress("entry.txt", data)
    reader :: archive.zip_open(zip)
    result :: archive.zip_read(reader, "entry.txt")
    print(result == data)
}
