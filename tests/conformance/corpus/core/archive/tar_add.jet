// core-conformance: core.archive.tar_add
use core.archive as archive

fn run() {
    data :: [U8]{72, 101, 108, 108, 111}
    result :: archive.tar_add([U8]{}, "entry.txt", data)
    print(result.len())
}
