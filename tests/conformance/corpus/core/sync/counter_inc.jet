// core-conformance: core.sync.counter_inc
use core.sync as sync

fn run() {
    counter :: sync.counter_new("replica-a", 2)
    incremented :: sync.counter_inc(counter, "replica-a", 3)
    print(incremented)
}
