// core-conformance: core.sync.counter_merge
use core.sync as sync

fn run() {
    left :: sync.counter_new("replica-a", 2)
    right :: sync.counter_new("replica-b", 3)
    merged :: sync.counter_merge(left, right)
    print(merged)
}
