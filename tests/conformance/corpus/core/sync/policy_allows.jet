// core-conformance: core.sync.policy_allows
use core.sync as sync

fn run() {
    policy :: sync.policy_new("items", "owner == user") ?? panic("policy")
    print(sync.policy_allows(policy, "alice", "alice"))
}
