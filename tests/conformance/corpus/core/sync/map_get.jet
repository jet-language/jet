// core-conformance: core.sync.map_get
use core.sync as sync

fn run() {
    map :: sync.map_new()
    populated :: sync.map_set(map, "key", "value")
    result :: sync.map_get(populated, "key")
    print(result ?? "missing")
}
