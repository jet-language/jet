// core-conformance: core.sync.text_merge
use core.sync as sync

fn run() {
    left :: sync.text_new("replica-a", "left")
    right :: sync.text_new("replica-b", "right")
    merged :: sync.text_merge(left, right)
    print(merged)
}
