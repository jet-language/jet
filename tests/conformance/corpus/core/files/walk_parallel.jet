// core-conformance: core.files.walk_parallel
use core.files as files
use core.testing as testing

fn run() {
    root :: testing.temp_dir("files-walk-parallel")
    path :: Path.from(root).join("fixture.txt")
    writer :: files.create(path) ?? panic("create")
    writer.write_line("fixture") ?? panic("write")
    writer.flush() ?? panic("flush")
    result :: files.walk_parallel(root) ?? panic("walk")
    print(result.len())
}
