// core-conformance: core.files.read_bytes
use core.files as files
use core.testing as testing

fn run() {
    root :: testing.temp_dir("files-read-bytes")
    path :: Path.from(root).join("fixture.bin")
    files.write_bytes(path, [U8]{65, 66, 67, 68}) ?? panic("seed")
    result :: files.read_bytes(path) ?? panic("read_bytes")
    print(result.len())
    files.remove_all(root) ?? panic("cleanup")
}
