// core-conformance: core.files.is_dir
use core.files as files
use core.testing as testing

fn run() {
    root :: testing.temp_dir("files-is-dir")
    result :: files.is_dir(root)
    assert((result == true))
    files.remove_all(root) ?? panic("cleanup")
}
