// core-conformance: core.files.remove
use core.files as files
use core.testing as testing

fn run() {
    root :: testing.temp_dir("files-remove")
    path :: Path.from(root).join("fixture.txt")
    files.write(path, "fixture") ?? panic("seed")
    files.remove(path).drop("the witness only needs call completion")
    print(true)
    files.remove_all(root) ?? panic("cleanup")
}
