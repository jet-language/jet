// core-conformance: app.transact_invalidate
use app as application

fn run() {
    query :: application.live("users", "alice")
    print(application.live_get(query))
    hit :: application.transact_invalidate("users")
    assert(hit > 0)
}
