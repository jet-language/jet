// core-conformance: app.live_stats
use app as application

fn run() {
    query :: application.live("users", "alice")
    print(application.live_get(query))
    result :: application.live_stats()
    assert(result.contains("limit=1024"))
}
