// core-conformance: app.sync
use app as application

fn run() {
    result :: application.sync("SyncMap(name=jet)", over: "corpus-app-sync")
    assert(result.contains("session=corpus-app-sync") && result.contains("doc=SyncMap(name=jet)"))
}
