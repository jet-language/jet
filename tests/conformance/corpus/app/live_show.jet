// core-conformance: app.live_show
use app as application

fn run() {
    query :: application.live("users", "alice")
    result :: application.live_show(query)
    assert(result.contains("footprint=users"))
}
