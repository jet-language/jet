// core-conformance: app.auth_oauth
use app as application

fn run() {
    config :: application.auth("users")
    configured :: application.auth_oauth(config, "google github")
    print(application.auth_show(configured))
}
