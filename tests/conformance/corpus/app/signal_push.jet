// core-conformance: app.signal_push
use app as application

fn run() {
    query :: application.live("users", "alice")
    updated :: application.signal_push(query, "changed")
    print(application.live_get(updated))
}
