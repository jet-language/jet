// D-QUAL2: a `tag` is a marker with no methods. Declaring a method on one is
// E0732 — the fix is to make it a `trait` (if the method should dispatch) or
// drop the method (to keep it a marker).
tag Reviewed {
    fn review(self) -> Int;
}

fn run() {
    print("ok")
}
