fn fetch(url: String) #(Net) -> String {
    return "data"
}

fn pure_compute(x: Int) #(!Net) -> Int {
    n #= fetch("http://example.com")
    return x + 1
}

fn main() {
    print(pure_compute(42))
}
