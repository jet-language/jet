// E0367: bare unit alt in an or-pattern needs a leading `.`.
enum Light {
    Red
    Green
}

fn show(light: Light) {
    if light == {
        .Red | Green -> { print("warm") }
        else -> { print("other") }
    }
}

fn run() {
    show(Light.Red)
}
