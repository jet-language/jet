use core.event as event

fn run() {
    scope :: event.scope()
    hook :: event.decision_hook<Int, String>(HookPolicy.FirstCancelElseTransform)
    hook.on(scope, (value: Int) => HookDecision.Fail(value))
}
