// Fix: a borrow can't escape through a closure as a `view`. Return the local
// owned (drop `view`); the caller gets its own copy.
fn make() -> String {
local: String :: "hi"
    return local
}

fn run() {
    print(make())
}
