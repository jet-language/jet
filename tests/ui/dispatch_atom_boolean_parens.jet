// E0386: distributed atom group is a direct operand of `&&` or `||`
fn run() {
    n :: 48
    ready :: true
    if n < {
        48 | 45 && ready -> print("hit")
        else -> print("miss")
    }
}
