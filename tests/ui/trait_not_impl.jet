trait Ordered {
    fn rank(self) -> Int;
}

fn largest<T: Ordered>(xs: List<T>) -> T? {
    return null;
}

struct Point {
    x: Int;
}

fn main() {
    val xs: List<Point> = [];
    print(largest(xs));
}
