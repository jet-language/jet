Error [E0740]: `load` uses the effect `Fs`, which its signature doesn't allow
  --> tests/ui/effect_out_of_set.jet:3:25
    |
  3 | fn load(path: String) =[Net]=> String = fs.read(~path) ?? ""
    |                         ^^^
 Why: the signature declares `=[Net]=>`, so the body may only use those; `Fs` is outside that set
 Fix: add `Fs` to the effect list, or stop using it in `load`
