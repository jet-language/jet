fn stopping_sort_key(n: Int) String !Err -> {
    if n == 1 { return Err("stop") }
    return "{n}"
}

fn run() {
    values := [3, 1, 2]
    values.sort_by((n: Int) -> stopping_sort_key(n)) ?? "fallback"
}
