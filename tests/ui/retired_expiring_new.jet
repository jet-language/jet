// D-SHAPE-CTORVERB1=C: generic expiry uses ExpiringValue.new.
use core.time.expiring as expiring

fn run() {
    ttl :: Duration.seconds(1) ?? panic("duration")
    cache :: expiring.new("value", ttl, Clock.system())
}
