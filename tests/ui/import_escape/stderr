Error [E0602]: this import path escapes the project
  --> tests/ui/import_escape/main.jet:1:8
    |
  1 | import "../outside";
    |        ^^^^^^^^^^^^
 Why: file imports stay inside the folder that contains the entry `.jet` file — `..` isn't allowed
 Fix: use a path without `..`, or move the file inside the project tree
