struct Signup {
    email: String
}

fn run() {
    signup :: Signup{email: "ok"}
    if Validate.over(signup).check(true, at: missing, "missing").finish() == {
        .Ok(_) -> { print("ok") }
        .Err(_) -> { print("err") }
    }
}
