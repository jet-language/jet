// D-ITERTOOLS1=A: driving a consumed lazy Iter twice is use-after-move (E0121).
fn run() {
    nums := [1, 2, 3, 4]
    view := nums.filter((n: Int) => n > 1)
    a := view.to_list()
    b := view.to_list()
    print(a)
    print(b)
}
