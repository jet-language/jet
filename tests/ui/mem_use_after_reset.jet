// E3104: alloc into a freed arena (after free() the allocator is gone).
// reset() keeps the arena alive; free() consumes it.
use core.mem
fn main() {
    arena @= mem.Arena.new()
    x @= arena.alloc(1)
    print(x)
    arena.free()
    y @= arena.alloc(2)
    print(y)
}
