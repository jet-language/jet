Error [E1210]: `pkg.jet` lists an unknown target
 Why: package `web` lists target `plugin`, which has no backend yet (reserved for a future increment)
 Fix: use a shipped target: `library`, `executable`, `test`, or `example`
