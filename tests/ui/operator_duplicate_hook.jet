struct Vec2 {
    x: Int
    y: Int
}

impl Vec2.Add {
    fn add(self, rhs: Vec2) => Vec2 { return Vec2.{x: self.x + rhs.x, y: self.y + rhs.y} }
}

impl Vec2.Add {
    fn add(self, rhs: Vec2) => Vec2 { return Vec2.{x: self.x + rhs.x, y: self.y + rhs.y} }
}

fn run() {

}
