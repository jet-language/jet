// E0365: each anonymous-union member can appear in only one match arm.
fn show(value: Int | String) {
    if value == {
        .Int(n) -> print(n)
        .Int(n) -> print(n)
        .String(s) -> print(s)
    }
}

fn run() {
    show(1)
}
