// C25/E0316: inverted arm-head range (`hi..lo`) — lower bound exceeds upper bound.
fn classify(score: Int) -> String {
    if score == {
        100..0 -> { return "impossible" }
        else -> {
            return "ok"
        }
    }
}

fn run() {
    print(classify(50))
}
