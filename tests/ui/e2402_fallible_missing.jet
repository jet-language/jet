enum MyError {
    NotFound
}
fn inner() -> Int ? MyError {
    return Ok(1)
}
// The return type is `Error` (the built-in) but `MyError` has no `Fallible` impl.
fn outer() -> Int ? {
    x :: inner()?
    return Ok(x)
}
fn run() {

}
