// E0412: `private` is not the Jet spelling. The keyword is this fixtures
// subject, so it stays; only the arrows move to the ratified spelling.
#PubFile

private fn secret() Int -> 0

fn run() {
    return
}
