// D-REPLAY1: `#Replayable` rejects ambient nondeterminism reached through the
// call graph. Replay code must take deterministic inputs or mockable handles.
fn record_line() {
    print("tick")
}

#Replayable fn replay_turn() {
    record_line()
}

fn run() {
    replay_turn()
}
