Error [E1212]: `pkg.jet` declares package `hello` but no `module hello` was found
 Why: each `packages:` name must correspond to a `module <name> { … }` declaration in a `.jet` file in the source tree
 Fix: add a `.jet` file containing `module hello { … }`, or remove `hello` from `packages:` in `pkg.jet`
