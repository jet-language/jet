use c.cb as c

@Pure fn can_panic(x: I32) -> I32 {
    panic("callback panic")
    return x
}

@Extern module c.cb {
    fn call(cb: @Pure fn(I32) -> I32, x: I32) -> I32 = "call"
}

fn run() { print(c.call(can_panic, 1)) }
