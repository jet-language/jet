// D-WHEN2: the dropped arm still gets name-resolution.
// `nonexistent_var` is a typo and should be caught even though
// this arm is not type-checked.
$flag :: true

fn run() {
    $if $flag {
        print("selected")
    } else {
        print("{nonexistent_var}")
    }
}
