struct Pool { handles: [Task<Int>] }

fn drain(p: Pool) => Int {
    total := 0
    loop h, p.handles { total += h.join() ?? 0 }
    total
}

fn run() {
    p :: Pool.{ handles: [task 1] }
    print(drain(p))
}
