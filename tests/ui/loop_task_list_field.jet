use core.tasks as tasks

struct Pool { handles: [Task<Int>] }

fn drain(p: Pool) => Int {
    total := 0
    loop h, p.handles { total += h.wait() }
    total
}

fn run() {
    p :: Pool.{ handles: [tasks.spawn(() => 1)] }
    print(drain(p))
}
