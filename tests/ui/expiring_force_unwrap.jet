// E0511: Expiring.force bypasses fallible expiry check.
use core.time as time
use core.time.expiring as expiring

fn main() {
    clk #= time.clock(0)
    item #= expiring.new("x", time.secs(1), clk)
    _ #= item.force(clk)
}
