struct Address {
    text: String
}

struct Email {
    addr: String,
    nested: Address
}

fn shallow(e: Email) -> Data {
    return Data.Text(e.addr)
}

fn nested(e: Email) -> Data {
    return Data.Text(e.nested.text)
}

fn run() {}
