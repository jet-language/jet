// E2306: `#Ref(pool)` names `pool`, but no `String` value named `pool` is in
// scope for the `&String` field to borrow.
struct View {
    #Ref(pool) text: &String
}
fn show(source: String) {
    v: View :: View.{ text: source }
    print(v.text)
}
fn run() {
    show("hi")
}
