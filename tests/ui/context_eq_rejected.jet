// D-CTX1: `=` is reassignment (S17); context fields use `:`.
// E0760 fires on the `=` token.
use core.mem

fn main() {
    arena :: mem.Arena.new()
    #Context(allocator = arena) {
        print("should not reach")
    }
}
