// E0610: `use alias.item` but alias is not a module
fn helper() -> Int {
    return 1
}

// `helper` is a function, not a module — using it as a module alias fires E0610
use helper.something

fn main() {
    print(something())
}
