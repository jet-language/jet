fn use_window(window: View<Int> from packet, packet: [Int]) {
    print(window[0])
}

fn run() {
    packet := [9, 8, 7]
    other := [1, 2, 3]
    window :: other[0..1]
    use_window(window, packet)
}
