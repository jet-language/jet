use core.http as http
use core.http.server as server

fn handle(req: HTTPRequest) => HTTPResponse ? HTTPError = Ok(server.response(200, "ok"))
fn run() {
    router :: http.router()
    id :: "42"
    router.get("/users/{id}", handle)
    doubled :: "/teams/{{name}}"
    router.get(doubled, handle)
}
