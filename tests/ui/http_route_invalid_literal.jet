use core.http as http

fn handle(req: HttpRequest) -> HttpResponse {
    return HttpResponse.{status: "200 OK", body: "ok", headers: []}
}

fn run() {
    router :: http.router()
    id :: "42"
    router.get("/users/{id}", handle)
    doubled :: "/teams/{{name}}"
    router.get(doubled, handle)
}
