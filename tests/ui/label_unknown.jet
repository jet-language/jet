struct Container {
    w: Int
    h: Int
}

impl Container {
    fn make(width: Int, height: Int) -> Container {
        return Container.{w: width, h: height}
    }
}

fn main() {
    _ :: Container.make(width: 5, depth: 3)
}
