// E0928 (D-JPK-TASKRUN1, card #476): `#Task fn` must not reuse a reserved
// lifecycle verb — `run`/`dev`/`build`/`test` already name Jet's built-in
// entry points. (`test` chosen over `build` so E3501 doesn't also fire.)
#Task
fn test() {
    print("nope")
}

fn run() {}
