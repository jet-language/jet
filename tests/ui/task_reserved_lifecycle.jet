// E0928 (D-JPK-TASKRUN1/D-CMD-OVERRIDE1=C, cards #476/#1451): `#Job fn` must not reuse a reserved
// lifecycle verb — `run`/`dev`/`build`/`test`/`bench` already name Jet's built-in
// entry points. (`test` chosen over `build` so E3501 doesn't also fire.)
#Job fn test() {
    print("nope")
}

fn run() {

}
