use core.encoding.csv as csv

#Codable
struct Sale {
    item: String
    qty: Int
}

fn run() {
    query :: csv.query<Sale>("sales.csv", SQL{"SELECT * FROM sales WHERE quntity > 1"})
        ?? panic("query")
    rows :: query.collect() ?? panic("collect")
    print(rows)
}
