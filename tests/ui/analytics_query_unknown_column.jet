use core.encoding.csv as csv

#Codable
struct Sale {
    item: String
    qty: Int
}

fn run() ![FieldError] {
    sales :: csv.query<Sale>("sales.csv", SQL{"SELECT * FROM sales WHERE quntity > 1"})?
    print(sales)
}
