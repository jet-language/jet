// D-EFF2: `#(via f)` must name a callback parameter of the function. `missing`
// is not a parameter of `run`, so the pass-through annotation is E0748.

fn run(n: Int, act: #(Io) fn(Int)) #(via missing) {
    act(n);
}

fn show(n: Int) {
    print("{n}");
}

fn main() {
    run(5, show);
}
