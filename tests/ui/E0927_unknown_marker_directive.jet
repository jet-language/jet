// E0927 (card #518): `#Zorp` isn't a registered directive-plane marker — a
// plain unrecognized name, not close to any real directive marker.
#Zorp
struct Widget {
    label: String
}

fn run() {
    print("ok")
}
