Error [E0366]: pattern arms need `==` — this table uses `<`
  --> tests/ui/dispatch_pattern_needs_eq.jet:11:9
    |
 11 |         .Circle -> "circle"
    |         ^^^^^^^
 Why: structural patterns compare by shape under `if subject == { … }` only
 Fix: write `if subject == { … }` for pattern arms, or use a Bool head
More: jet-lang.dev/e/E0366
