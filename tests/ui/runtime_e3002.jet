fn read_raw() String ? :> Err("file not found")

fn parse_config() String ? {
    raw :: read_raw()? "reading raw config"
    return Ok(raw)
}

fn run() ? { _ :: parse_config()? "loading config" }
// @dev_interpreter
