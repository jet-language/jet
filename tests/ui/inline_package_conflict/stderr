Error [E1363]: this source declares inline Package facts while `package.jet` also exists.
  --> tests/ui/inline_package_conflict/run.jet:1:1
    |
  1 | package { name: "inline-demo" }
    | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 Why: D-ECO-INLINEPACKAGE1 allows one Package context per project; choosing between two identities would make builds, dependencies, outputs, and authority nondeterministic.
 Fix: remove the inline block or remove `package.jet`, then keep the remaining Package declaration canonical.
More: jet-lang.dev/e/E1363
