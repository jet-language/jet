// E0427: the retired `#Ref(owner) name: T` form (plain type, no `&`). The `&`
// on the type is what now marks a stored reference (D-REF-SHORTHAND1).
struct Caption {
    #Ref(src) text: String
}
fn run() {
    print(0)
}
