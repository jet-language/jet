// D-PATW: `_` in payload slot = wildcard (binds nothing, I4 snapshot).
enum Wrapper {
    Some(Int)
    Empty
}
fn run() {
    w :: Wrapper.Some(42)
    if w == .Some(_) {
        print("has value")
    }
}
