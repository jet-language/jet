// D-AUTHORITY-NAME1=A / D-FACTMODEL1=A: the Abilities value is ordinary data, not
// a compile-time effect/type fact that can select a type or dispatch implementation.
fn run() {
    #Caps(Abilities) {
        print("Abilities must remain ordinary data")
    }
}
