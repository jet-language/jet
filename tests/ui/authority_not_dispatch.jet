// D-AUTHORITY-WORD2=E / D-FACTMODEL1=A: Authority is ordinary data, not a
// compile-time fact that can select a type or dispatch implementation.
fn run() {
    #FX(Authority) {
        print("Authority must not dispatch")
    }
}
