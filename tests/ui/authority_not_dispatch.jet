// D-AUTHORITY-NAME1=A / D-FACTMODEL1=A: the Authority value is ordinary data, not
// a compile-time effect/type fact that can select a type or dispatch implementation.
fn run() {
    #Caps(Authority) {
        print("Authority must remain ordinary data")
    }
}
