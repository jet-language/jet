// D-AUTHORITY-WORD2=E / D-FACTMODEL1=A: Abilities is ordinary data, not a
// compile-time fact that can select a type or dispatch implementation.
fn run() {
    #Abilities(Abilities) {
        print("Abilities must not dispatch")
    }
}
