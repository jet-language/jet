Error [E3520]: two build entries for the package: the selected source file and `package.jet`
  --> tests/ui/build_entry_conflict/main.jet:3:4
    |
  3 | fn build(b: BuildContext) => BuildPlan ? { return b.plan() }
    |    ^^^^^
 Why: one unit has exactly one build entry so policy and provenance have one auditable home
 Fix: keep the `fn build` in the selected source file and remove the entry in `package.jet`
