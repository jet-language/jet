// E0063 (D-MARKER-FAMILY1): `#Caps` is a directive — `@Caps` is not a valid spelling.
fn announce(label: String, n: Int) #(Io) {
    print("{label}: {n}")
}

fn run() {
    @Caps(Io) {
        announce("answer", 42)
    }
}
