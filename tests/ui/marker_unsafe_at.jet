// E0063 (D-MARKER-FAMILY1): `#Unsafe` is a directive — `@Unsafe` is not a valid spelling.
use core.mem
fn run() {
    cell: Int :: 1337
    @Unsafe("cell is live on this stack frame and the pointer never escapes") {
        p: *Int :: *cell
        print(p.*)
    }
}
