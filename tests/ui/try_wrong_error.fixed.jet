enum E1 {
    A
}

enum E2 {
    B
}

fn f() -> Int ? E1 {
    return ok(1)
}

fn g() -> Int ? E2 {
    r #= f()
    if r == {
        ok(n) -> {
            return ok(n)
        }
        err(e) -> {
            return err(E2.B)
        }

    }
}

fn main() {

}
