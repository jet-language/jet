// E0927: `#Pure` is retired. The marker is the subject of this fixture, so it
// must stay; the arrows are migrated to the ratified spelling.
#Pure fn old_pure() Int -> { return 2 }

fn run() { print(old_pure()) }
