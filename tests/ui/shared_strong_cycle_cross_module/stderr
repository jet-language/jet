Error [E0221]: Field `child` on `Root` can form a strong `Shared` cycle
  --> tests/ui/shared_strong_cycle_cross_module/run.jet:5:5
    |
  5 |     child: ?Shared<helper.Node>
    |     ^^^^^
 Why: A strong `Shared` edge through `helper.Node` can point back at `Root`, so reference counting alone cannot free the graph
 Fix: Use `Shared.Weak<T>` for intentional back-edges, or store an id instead of a strong `Shared` handle
More: jet-lang.dev/e/E0221

Error [E0221]: Field `owner` on `Node` can form a strong `Shared` cycle
  --> tests/ui/shared_strong_cycle_cross_module/run.jet:2:6
    |
  2 | use "helper"
    |      ^^^^^
 Why: A strong `Shared` edge through `Root` can point back at `Node`, so reference counting alone cannot free the graph
 Fix: Use `Shared.Weak<T>` for intentional back-edges, or store an id instead of a strong `Shared` handle
More: jet-lang.dev/e/E0221
