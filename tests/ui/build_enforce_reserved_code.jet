// @programmable_build
struct Entity {
    id: Int
}

fn build(b: BuildContext) -> BuildPlan ? {
    types :: b.program.types()
    b.error(types[0].span, "E9000", "bad", "bad", "bad")
    return b.plan()
}

fn run() {

}
