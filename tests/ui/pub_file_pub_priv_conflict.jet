// E0417: `priv` and `pub` together. Both keywords are this fixtures subject,
// so they stay; only the arrows move to the ratified spelling.
#PubFile

priv pub fn secret() Int -> 0

fn run() {
    return
}
