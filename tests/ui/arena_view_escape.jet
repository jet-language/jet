// D-ALLOC2 / D-REGION1: E0631 — an arena `view` may not escape its region.
// Returning it would let it outlive the arena and dangle.
use core.mem

fn make() -> Int {
    arena @= mem.Arena.new()
    x @= arena.alloc(42)
    return x
}

fn main() {
    print(make())
}
