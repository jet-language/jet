// E0317: or-pattern alternatives bind different names.
enum Conn {
    Active(Int)
    Reconnecting(Int)
    Idle
}
fn main() {
    c :: Conn.Active(1)
    if c {
        c == Active(id) | Reconnecting(session) -> { }
        c == Idle -> { }
    }
}
