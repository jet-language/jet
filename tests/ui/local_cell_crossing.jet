fn cross_task(cell: Cell<Int>) {
    worker :: task {
        _ :: cell
    }
    worker.join() ?? panic("task failed")
}

fn cross_shared(cell: Cell<Int>) { _ :: shared cell }

struct Cache {
    cell: Cell<Int>
}

fn cross_shared_nested(cache: Cache) { _ :: shared cache }

fn run() {}
