// E2405: two `impl Source -> Target` blocks for the same (Source, Target) pair.
enum ParseErr {
    Empty
}

enum AppErr {
    Parse(ParseErr)
}

impl ParseErr -> AppErr { return AppErr.Parse(self) }

impl ParseErr -> AppErr { return AppErr.Parse(self) }

fn run() {

}
