// E1314: a trait-bounded variadic parameter used outside the one supported
// shape, a direct `loop x, parts { … }` loop (D-ANY-JAI1).
fn log_all(parts: ...Renderable) {
    print(parts.len())
}

fn run() {
    log_all(1, "two")
}
