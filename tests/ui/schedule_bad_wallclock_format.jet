// E0926 (D-SCHEDULE1, card #505): a daily schedule must be a plain
// "HH:MM" string — two digits, a colon, two digits.
#[Task, Every("3:00")] fn prune_sessions() {
    print("pruning")
}

fn run() {
    prune_sessions()
}
