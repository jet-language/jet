Error [E0605]: `y` exists but is private to its file
  --> tests/ui/import_private_field/main.jet:5:13
    |
  5 |     print(p.y)
    |             ^
 Why: only names marked `pub` can be used from another file (S18)
 Fix: add `pub` before `y`, or don't reach across files here
