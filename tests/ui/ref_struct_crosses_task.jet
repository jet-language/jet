use core.tasks as tasks;

struct Holder {
    ref name: String;
}

fn make(src: String) -> Holder {
    return Holder { name: src };
}

fn main() {
    val t = tasks.spawn(() => make("hi"));
    t.join();
}
