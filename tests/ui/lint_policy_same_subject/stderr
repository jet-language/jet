Error [E1293]: Lint `same_subject_dispatch` (`L0514`) is denied by policy: Adjacent categorical guards over `key` can be one ordered subject table.
  --> tests/ui/lint_policy_same_subject/run.jet:3:5
    |
  3 |     if key == "revision" || key == "rev" {
    |     ^^
 Why: Exclusive guards repeat one stable subject; an ordered table keeps the categories together. This team's `policy.lints.deny` in `package.jet` turns this warning into a build failure (D-LINTPOLICY1 — the override law); it stays a warning everywhere `package.jet` doesn't opt in. The listed lint name matches.
 Fix: Use the underlying lint fix (Rewrite as `if key == { … }` and group aliases with `|`; see `examples/suites/dispatch.jet`), or remove `same_subject_dispatch` from `policy.lints.deny` if this team no longer wants the wall.
More: jet-lang.dev/e/E1293
