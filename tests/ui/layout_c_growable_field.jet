// D-REPRC1 (D-REPRC1 = B): `#layout(c)` rejects growable fields.
#layout(c)
struct Packet {
    id: Int
    payload: [Int]   // growable — not C-stable
    label: String    // growable — not C-stable
}
fn main() {
    print(0)
}
