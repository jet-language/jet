// E1104: `#Layout(c)` struct with growable field.
#Layout(c)
struct Packet {
    id: Int
    payload: [Int]   // growable — not C-stable
    label: String    // growable — not C-stable
}
fn main() {
    print(0)
}
