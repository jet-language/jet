// D-UNINIT-SENTINEL2: `uninit` is legal only as the whole `Type.{ }` body.
// A multi-element body is an ordinary typed literal — not the uninit trigger.
use core.mem

fn run() {
    xs := [Int#2].{ uninit, 1 }
    print(xs.len())
}
