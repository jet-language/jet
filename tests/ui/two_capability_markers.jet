// E0029: a parameter's capability is written once. Here `mut` (pre-name) and the
// type-side `~` sigil both mark `x` as writable — that is two markers (D-CAP7).
fn bump(mut x: ~Int) {
    x = x + 1
}

fn run() {
    n := 1
    bump(~n)
}
