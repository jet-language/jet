// E0029: a parameter's capability is written once. Here `&x` (pre-name) and
// the type-side `&` sigil both mark `x` as writable — that is two markers.
fn bump(&x: &Int) {
    x += 1
}

fn run() {
    n := 1
    bump(&n)
}
