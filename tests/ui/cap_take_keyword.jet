// E0057: the `take` capability keyword is retired (D-CAP7). Move is the `^`
// sigil on the type: `name: ^String`, and `^name` at the call site.
fn archive(take name: String) -> String {
    return name
}

fn main() {
    saved: String @= archive(take "vault")
    print(saved)
}
