// E0414: under `#PubFile` every item is already public, so `pub` is
// redundant. The redundant keyword is this fixtures subject and must stay.
#PubFile

pub fn greet() String -> "hi"

fn run() {
    return
}
