Error [E0605]: `secret` exists but is private to its file
  --> tests/ui/module_private/main.jet:4:16
    |
  4 |     print(math.secret());
    |                ^^^^^^
 Why: only names marked `pub` can be used from another file (S18)
 Fix: add `pub` before `secret`, or don't reach across files here
