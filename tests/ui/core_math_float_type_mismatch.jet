use core.math as math

fn run() {
    math.sqrt(1)
    math.is_nan(1)
    math.sign(1)
    math.pow(2.0, F32.from_float(2.0) ?? F32.from_int(0))
    math.hypot(2.0, F32.from_float(2.0) ?? F32.from_int(0))
}
