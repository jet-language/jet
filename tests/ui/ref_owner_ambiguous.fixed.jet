// E0207 fix: name each owner with `#Ref(label)`.
struct Pair {
    #Ref(a) left: &String
    #Ref(b) right: &String
}

fn build(a: String, b: String) {
    p: Pair :: Pair.{ left: a, right: b }
    print(p.left)
}

fn main() {
    build("x", "y")
}
