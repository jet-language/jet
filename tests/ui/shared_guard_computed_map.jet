struct Stats {
    strength: Int
    attack: Int => strength * 2
}

fn run() {
    shared := Shared.new(Stats.{ strength: 10 })
    guard :: shared.guard_read()
    _ :: guard.map(value => value.attack)
}
