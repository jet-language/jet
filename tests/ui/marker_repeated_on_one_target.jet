// E0999 (D-MARK-REPEAT1=A): `#Inline` is not a repeatable rule, so the second
// copy — usually a merge artifact — is an error with a remove-the-repeat fix.
#[Inline, Inline]
fn price(cents: Int) => Int {
    return cents * 3
}

fn run() {
    print(~price(2))
}
