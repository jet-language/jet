// E0058: the `view` return keyword is retired (D-CAP7). A borrowed return is
// the `&` sigil: `-> &String`.
fn headline(text: String) -> view String {
    return text
}

fn main() {
    print(headline("Jet"))
}
