use core.tasks
struct Holder {
    ref name: String
}
fn forward(h: Holder) {
    // E1102 fires: `h` (a ref-holding struct) can't cross task boundary.
    // E1103 fires at `.detach()`: the task has a sendability problem.
    t :: tasks.spawn(() => h)
    t.detach()
}
fn main() {
    print(0)
}
