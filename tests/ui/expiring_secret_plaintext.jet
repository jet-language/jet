// E0112: only the closed zeroizing secret family may enter ExpiringSecret.
use core.time as time
use core.vault as vault

fn run() {
    clock := time.clock(0)
    ttl := Duration.seconds(1) ?? panic("duration")
    _ := vault.ExpiringSecret.new("plaintext", ttl, clock)
}
