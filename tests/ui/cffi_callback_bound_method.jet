use c.cb as c

struct Incrementer { enabled: Bool }
@Pure fn Incrementer.apply(self, x: I32) -> I32 { return x + 1 }

#Extern module c.cb {
    fn call(cb: @Pure fn(I32) -> I32, x: I32) -> I32 = "call"
}

fn run() {
    inc :: Incrementer.{enabled: true}
    print(c.call(inc.apply, 1))
}
