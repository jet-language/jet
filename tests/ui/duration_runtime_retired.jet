// D-SHAPE-DURATION1=A / D-SHAPE-DURATIONCONVERT1=A: legacy duration spellings are retired.
use core.time as time

fn run() {
    old_constructor :: time.seconds(1)
    duration :: Duration.seconds(1) ?? panic("duration")
    old_reader :: duration.millis()
}
