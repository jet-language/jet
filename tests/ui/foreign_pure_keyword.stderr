Error [E0053]: the purity modifier is written `#Pure`, not bare `pure`
  --> tests/ui/foreign_pure_keyword.jet:1:1
    |
  1 | pure fn square(n: Int) -> Int {
    | ^^^^
 Why: `#Pure` is a marker, like every other `#`-tag, so the purity contract draws the eye
 Fix: write: #Pure fn name() { ... }
