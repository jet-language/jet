struct Parcel {
    label: String
}

struct Holder {
    value: String
}

enum Wrapped {
    Val(String)
}

fn check(parcel: Parcel, values: [String]) -> Holder {
    alias :: parcel.label
    wrapped :: Wrapped.Val(values[0])
    owned := "old"
    owned = parcel.label
    print(alias)
    print(wrapped)
    return Holder.{value: parcel.label}
}

fn run() {
    print(0)
}
