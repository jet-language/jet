module util {
    pub fn double(x: Int) -> Int {
        return x * 2
    }
    pub fn triple(x: Int) -> Int {
        return x * 3
    }
}

use util.{double as d, triple as t}

fn main() {
    print(d(5))
    print(t(5))
}
