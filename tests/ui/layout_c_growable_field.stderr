Error [E0003]: `#layout` isn't a known attribute on a const
  --> tests/ui/layout_c_growable_field.jet:2:8
    |
  2 | #layout(c)
    |        ^
 Why: only `#static` and `#inline` are supported on const declarations
 Fix: remove the attribute or use `#static` or `#inline`
