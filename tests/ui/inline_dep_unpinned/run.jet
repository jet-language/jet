// @all_diags
// U11 (D-JPK-SCRIPTDEP1=A): `textkit#1.4` resolves fine (the sibling
// `.jet/inline-deps/textkit/1.4.2/` copy satisfies it) but the selector isn't
// pinned to an exact version — L0203.
use textkit#1.4

fn run() {
    print(textkit.shout("hi"))
}
