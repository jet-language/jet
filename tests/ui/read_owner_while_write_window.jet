use core.mem

struct Node {
    payload: Int
    hops: Int
}

fn run() {
    node := Node.{payload: 41, hops: 0}
    pinned :: mem.pin(&node)
    if node.payload > 0 {
        pinned.hops += 1
    }
    print("{(pinned.hops)}")
}
