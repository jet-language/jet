Error [E1278]: jetos activation proof is incomplete
 Why: D-WD8 requires a plan and proof artifact before `jet os switch` can activate a generation.
 Fix: run `jet os build <host>` again; if the generation is hand-edited, discard it.
