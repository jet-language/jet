// @complexity_cli fail 3
fn tangled(n: Int) Int {
    total := 0
    loop i, 0..<n {
        loop j, 0..<n {
            if i > 0 && j > 0 || i == j -> total += i + j
        }
    }
    return total
}

fn run() {}
