// E2501: calling a write method on a read-only file handle.
use core.files as files;
fn go() -> Int ? IOError {
    val h = files.open("/tmp/x.txt")?;
    h.write_line("hello")?;
    return ok(0);
}
fn main() { go(); }
