Error [E1257]: this sandbox's exported interface changed incompatibly
 Why: a sandbox's frozen exported interface is the load-time contract (D-PLUGIN-VERSION1) — export `old_scale` was removed
 Fix: restore the removed/changed export, or accept this as an intentional breaking change (delete the stale snapshot under `.jet/cache/api/` to re-freeze)
More: jet-lang.dev/e/E1257
