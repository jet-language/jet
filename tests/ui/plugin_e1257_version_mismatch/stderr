Error [E1257]: this plugin's exported interface changed incompatibly
 Why: a plugin's frozen exported interface is the load-time contract (D-PLUGIN-VERSION1) — export `scale` changed from `fn scale(a: Float, b: Float) -> Float` to `fn scale(a: Float, b: Float, c: Float) => Float`
 Fix: restore the removed/changed export, or accept this as an intentional breaking change (delete the stale snapshot under `.jet/cache/api/` to re-freeze)
