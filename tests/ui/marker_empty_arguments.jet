// E0999 (D-MARK-FORM1=A): parentheses appear exactly when arguments are
// written, so an empty pair is a leftover the formatter deletes.
#Inline()
fn double(x: Int) => Int {
    return x * 2
}

fn run() {
    print(~double(21))
}
