// D-NOPANIC1=D: a library panic on bad input cannot cross a denied row.
fn parse_port(raw: String) =[!Panic]=> Int {
    return Int.parse(raw) ?? panic("bad port")
}

fn run() {
    print(parse_port("bad"))
}
