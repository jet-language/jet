enum ExpectedLambdaError {
    Bad
}

enum OtherLambdaError {
    Other
}

fn apply(f: fn(Int) Int ExpectedLambdaError!) Int -> f(1) ?? 0

fn run() {
    print(apply((n: Int) Int OtherLambdaError! -> n))
}
