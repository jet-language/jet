fn is_positive(cents: Int) -> Bool {
    print("checking")
    return cents > 0
}

@Pre(is_positive(cents), "cents must be positive")
fn add_fee(cents: Int) -> Int {
    return cents + 5
}

fn main() {
    print("{(add_fee(1))}")
}
