#Pre(if {
    cents > 0 -> {
        print("checking")
        true
    }
    else -> false
}, "cents must be positive") fn add_fee(cents: Int) => Int {
    return cents + 5
}

fn run() {
    print("{(add_fee(1))}")
}
