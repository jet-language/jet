// E0926 (D-SCHEDULE1, card #505): `#Every(…)`'s duration unit isn't in the
// closed schedule vocabulary (`ns`/`us`/`ms`/`s`/`min`).
#Task
#Every(5px)
fn prune_sessions() {
    print("pruning")
}

fn run() {
    prune_sessions()
}
