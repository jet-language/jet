// E0926 (D-SCHEDULE1, card #505): `#Every(…)`'s duration unit isn't in the
// shared duration plane (`ns`/`us`/`µs`/`ms`/`s`/`sec`/`secs`/`min`/`h`/`d`).
#[Job, Every(5px)] fn prune_sessions() {
    print("pruning")
}

fn run() {
    prune_sessions()
}
