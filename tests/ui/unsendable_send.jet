use core.tasks
struct Logger {
    ref sink: String
}
struct Config {
    logger: Logger
    id: Int
}
fn main() {
     ch@ Channel<Config>  = tasks.channel()
    sender @= ch.sender()
    cfg @= Config.{logger: Logger.{}, id: 1}
    sender.send(^cfg)
}
