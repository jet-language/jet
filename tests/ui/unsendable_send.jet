fn run() {
    greet :: () -> {
        print("hi")
    }
    // E1102 fires: this closure cannot cross a channel send either — the same
    // rule as a spawned task.
    (sender, ch) :: channel<fn()>()
    sender.send(greet)
}
