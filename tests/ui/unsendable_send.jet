use core.tasks
struct Logger {
    sink: &String
}
struct Config {
    logger: Logger
    id: Int
}
fn run() {
(sender, ch) :: tasks.channel<Config>()
    cfg :: Config.{logger: Logger.{}, id: 1}
    sender.send(^cfg)
}
