use core.tasks
struct Logger {
    sink: &String
}
struct Config {
    logger: Logger
    id: Int
}
fn run() {
ch: Channel<Config> :: tasks.channel()
    sender :: ch.sender()
    cfg :: Config.{logger: Logger.{}, id: 1}
    sender.send(^cfg)
}
