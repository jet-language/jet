package { name: "runtime-e3014", authority: { holds: { allow: [Browser, DB, Env, Exec, FFI, FS, GPU, IO, Log, Net, Rand, Secret, Time] } } }

extern rust "std" {
    fn panic_now(value: String) Int = "std::panic::panic_any"
}

fn run() {
    panic_now("foreign failure")
}
// @dev_interpreter
// @runtime_ffi
