Error [E2302]: the `#Ref(src)` field `name` must point into `src`
  --> tests/ui/ref_field_dangles_local.jet:6:22
    |
  6 | h: Holder #= Holder.{name: local}
    |                      ^^^^
 Why: this field stores a view into `src`, but the value comes from `local` instead
 Fix: fill `name` from `src` (or rename the `#Ref` label to match the real owner)
