Error [E1220]: `panicdep` uses the `Panic` effect at `panicdep::parse_port`, which this package's budget doesn't allow
  --> tests/ui/panic_effect_budget/run.jet:2:5
    |
  2 | use panicdep
    |     ^^^^^^^^
 Why: The package denies stops from `panicdep::parse_port`; a dependency that can stop cannot cross this budget boundary
 Fix: Return a fallible result for expected failure, or add facts or a `#Pre`/refinement proof for a programmer-error stop; `Panic` is deny-only and cannot be allowed or granted
More: jet-lang.dev/e/E1220
