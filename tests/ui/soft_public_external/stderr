Warning [L0601]: `_legacy` is a soft-public API
  --> tests/ui/soft_public_external/main.jet:5:13
    |
  5 |     library._legacy()
    |             ^^^^^^^
 Why: a leading underscore allows outside use but carries no compatibility promise across minor releases
 Fix: use a public name without a leading underscore when callers need a stable API
