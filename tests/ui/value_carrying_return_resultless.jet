fn source() Int -> {
    return Ok(7)
}

fn declared() Int -> {
    value :: source() ?? return Err("declared failure")
    return Ok(value)
}

fn run() {
    source() ?? return Err("resultless failure")
}
