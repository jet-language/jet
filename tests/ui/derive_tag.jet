// D-QUAL2: `derive X` attaches method impls, so X must be a `trait`. A `tag`
// has no methods, so deriving one is E0731 — declare it as a `trait` instead.
tag Reviewed;

struct Point {
    x: Int
    derive Reviewed
}

fn run() {
    print("ok")
}
