// D-CONF-PLANE1/D-CONF-NAME1: the retired `payload:` wrapper is a normal
// unknown-field error now — there is no second parser that still accepts it.
payload: {
    name: "myapp",
    version: "0.1.0",
}
