trait Logger {
    fn log(self, msg: String);
}

struct InnerService {
    name: String;
}

// InnerService does NOT implement Logger

struct Proxy {
    inner: InnerService;
}

impl Proxy: Logger using inner;

fn main() {}
