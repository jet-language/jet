// A type and its owning typestate section both exist. A state label is still a
// compile-time fact, not a runtime value exposed as `Type.State`.
struct Machine {
    state { Off, On }
    enabled: Bool
}

fn run() {
    print(Machine.Off)
}
