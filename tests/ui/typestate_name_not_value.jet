// A type and its typestate declaration both exist. A state label is still a
// compile-time fact, not a runtime value exposed as `Type.State`.
state Machine { Off, On }

struct Machine {
    enabled: Bool
}

fn run() {
    print(Machine.Off)
}
