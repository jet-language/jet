// E0927 (card #518): `#Targt` is one letter off `#Target` (D-MARK-TARGET1=A)
// — close enough (edit distance 1) for a "did you mean" suggestion.
#Targt(Wasm)
struct Widget {
    label: String
}

fn run() {
    print("ok")
}
