struct Resource {
    handler: fn(Int) => Bool
}

fn sink(item: Resource) {
    print(item.handler(1))
}

fn run() {
    item :: Resource.{ handler: (value: Int) => value > 0 }
    f :: () => {
        sink(item)
    }
    sink(item)
    f()
}
