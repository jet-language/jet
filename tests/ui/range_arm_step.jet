// C25/E0319: `step` in an arm head is a porting hazard — `step` belongs in a loop.
fn classify(n: Int) -> String {
    if n == {
        0..10 step 2 -> { return "even-ish" }
        else -> { return "other" }
    }
}

fn main() {
    print(classify(4))
}
