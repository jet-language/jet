pub policy audit(topic: String, prefix: String{"audit"}) {
    wrap(call) {
        return call()
    }
}

#Policy(audit("users.load")) fn load() Int -> 7

fn run() {
    selected :: apply(audit("users.load"), load)
    print(selected())
}
