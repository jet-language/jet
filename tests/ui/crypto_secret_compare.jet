// D-CRYPTO-API1=A: secret equality uses constant_time_equal, never ==.
use core.crypto as crypto

fn run() {
    first :: crypto.Secret.from_text("first")
    second :: crypto.Secret.from_text("second")
    print(first == second)
}
