// E0922 (D-MARK-DEBUG1=A): `Debug` auto-derives; writing it as an explicit
// derive is retired — this used to silently no-op.
#Debug
struct Widget {
    label: String
}

fn run() {
    print("ok")
}
