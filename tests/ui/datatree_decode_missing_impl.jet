// D-AUTODERIVE1=E auto-derives `Decode` for a qualifying plain struct, so the
// missing-contract case needs a type that refuses codec generation.
#!Codable
struct SecretHandle {
    raw: Int
}

fn run() {
    tree :: DataTree.Int(1)
    _ :: tree.decode<SecretHandle>()
}
