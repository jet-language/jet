// E2932: a layout constraint can't mix a horizontal value (`left`/`right`/
// `width`) with a vertical one (`top`/`bottom`/`height`) — GATE 1/2
// (D-LAYOUT1 / D-LAYOUT-GATES1).
fn run() {
    layout form {
        label.left >= label.height + 8.0
    }
}
