module bank {
    pub struct Account {
        balance: Int
    }
}

module vis2 {
    pub module bank {
        pub struct Account {
            balance: Int
        }
    }
}

fn bad() bank.Account {
    return vis2.bank.Account{balance: 1}
}

fn run() { }
