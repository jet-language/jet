// D-UNITLIT1: cross-unit arithmetic between two literal-suffixed values reuses
// the existing E0127 distinct-type gate — no implicit conversion.
#UnitFamily(Time) { ms, s }

fn run() {
    a :: 500ms
    b :: 1s
    total :: a + b
    print("{total}")
}
