use core.mem

fn run() {
    first :: 1
    second :: 2
    #Unsafe("two independent pointer operations", obligations: .Track) {
        first_pointer :: *Int.{*first}
        assert no_alias
        second_pointer :: *Int.{*second}
        print(first_pointer.*)
        assert valid_ptr, aligned
    }
}
