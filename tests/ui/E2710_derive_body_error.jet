// E2710: the derive body throws a comptime error (undefined name).
// The error is pinned at the #[Broken] trigger on the struct (D-METADERIVE1=A).

derive Broken for T {
    x @= undefined_name
    emit("impl Color : Broken {{ }}")
}

#[Broken]
struct Color { r: Int }

fn main() { print("ok") }
