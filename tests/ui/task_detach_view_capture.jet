use core.tasks
fn peek(s: String) -> &String { return s }
fn main() {
    // E1102 fires: the task returns a view that can't cross the task boundary.
    // E1106 fires at .detach(): the captured view borrow may outlive the caller.
    t #= tasks.spawn(() => peek("hello"))
    t.detach()
}
