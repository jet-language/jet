// D-META-STAGE1=B: `#Known` is retired. Compile time has one mark, `@`, and the
// mark belongs to the name, so it is written at every mention.
#Known limit :: 1000

fn run() {
    print("{@limit}")
}
