// E0003 (D-IFDIST1): a value-producing comparison table needs a final `else`
// so every path yields a value.
fn band(n: Int) => Int {
    return if n < {
        16 -> 0
        32 -> 1
    }
}

fn run() {
    print("{band(20)}")
}
