// D-DOTCTOR2: an inferred construction stays rejected without context.
// D-LIT-DOT1=B spells it `{name: …}` — the dot dropped, the rule did not.
struct Fighter {
    name: String
    hp: Int
}

fn run() {
    fighters :: [{name: "Ada", hp: 10}, {name: "Bo", hp: 8}]
    print(fighters.len())
}
