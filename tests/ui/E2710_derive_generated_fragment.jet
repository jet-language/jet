// E2710: generated source must re-enter the ordinary parser, and failures
// point back to the marker that requested expansion.
derive T.Broken {
    emit("fn generated( {{")
}

#Broken
struct Color {
    red: Int
}

fn run() {}
