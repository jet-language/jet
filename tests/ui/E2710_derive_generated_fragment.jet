// E2710 / D-SHAPE-DUNDER2=A: user derives re-enter the ordinary lexer and
// cannot use Jet's reserved generated-name lane.
derive T.Broken {
    emit("fn __generated() {{}}")
}

@Broken
struct Color {
    red: Int
}

fn run() {}
