// D-TAINT2: a value tagged `#Credential` reaching `print` (a credential
// sink) is E0722 — credential leakage caught at compile time.
fn run() {
    secret :: #Credential "my-secret-token"
    print(secret)
}
