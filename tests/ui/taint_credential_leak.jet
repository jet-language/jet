// D-TAINT2: a value tagged `@Tainted(Credential)` reaching `print` (a credential
// sink) is E0722 — credential leakage caught at compile time.
fn run() {
    secret :: @Tainted(Credential) "my-secret-token"
    print(secret)
}
