use c.cb as c

fn schedules(x: I32) =[]=> I32 {
    handle :: task x
    return handle.join() ?? 0
}

#Extern module c.cb {
    fn call(cb: fn(I32) =[]=> I32, x: I32) => I32 = "call"
}

fn run() { print(c.call(schedules, 1)) }
