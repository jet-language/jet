// D-ALLOC2: E0632 — an arena `view` may not be used after the arena is reset
// (or freed); reset invalidates everything allocated in the arena.
use core.mem

fn main() {
    arena #= mem.Arena.new()
    x #= arena.alloc(42)
    arena.reset()
    print(x)
}
