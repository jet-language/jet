// E0920 (D-METHODMACRO1=A): `#Inline` and `#InlineAlways` can't both apply
// to the same declaration — pick one.
#Inline #InlineAlways fn quad(x: Int) -> Int {
    return x * 4
}

fn run() {
    print(quad(2))
}
