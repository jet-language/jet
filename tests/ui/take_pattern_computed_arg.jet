// D-SHIFT1: `take_pattern` needs a literal pattern string — a computed
// String has no compile-time holes to check.
fn main() {
    pat :: "inc-42 "
    c :: Cursor.over("inc-42 rest")
    m :: c.take_pattern(pat) ?? panic("no match")
    print("{m}")
}
