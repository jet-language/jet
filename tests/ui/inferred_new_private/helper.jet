pub struct Token {
    pub value: Int
}

impl Token {
    fn new(value: Int) -> Token {
        return Token.{value: value}
    }
}

pub struct Secret<T> {
    pub value: T
}

impl Secret {
    fn new(value: ^T) -> Secret<T> {
        return Secret<T>.{value: value}
    }
}
