Error [E0605]: `new` exists but is private to its file
  --> tests/ui/inferred_new_private/main.jet:4:25
    |
  4 |     inferred: Token :: .new(1)
    |                         ^^^
 Why: only names marked `pub` can be used from another file (S18)
 Fix: add `pub` before `new`, or don't reach across files here

Error [E0605]: `new` exists but is private to its file
  --> tests/ui/inferred_new_private/main.jet:5:30
    |
  5 |     explicit: Token :: Token.new(2)
    |                              ^^^
 Why: only names marked `pub` can be used from another file (S18)
 Fix: add `pub` before `new`, or don't reach across files here

Error [E0605]: `new` exists but is private to its file
  --> tests/ui/inferred_new_private/main.jet:6:23
    |
  6 |     generic :: Secret.new(3)
    |                       ^^^
 Why: only names marked `pub` can be used from another file (S18)
 Fix: add `pub` before `new`, or don't reach across files here
