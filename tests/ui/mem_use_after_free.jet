// E3104: use-after-free — allocating into an arena after it was freed.
use core.mem
fn main() {
    arena #= mem.Arena.new()
    x #= arena.alloc(1)
    print(x)
    arena.free()
    y #= arena.alloc(2)
    print(y)
}
