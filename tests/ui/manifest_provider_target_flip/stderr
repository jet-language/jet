Error [E1206]: `package.jet` has a shape error
 Why: dependency `helpers` uses retired provider-first ref `github@acme/helpers/abc123`; write `acme/helpers/abc123@github`
 Fix: check `package.jet` against docs/spec/syntax-decisions.md (U1)
