struct Box {
    value: Int
}

fn run() {
    frozen :: freeze(Box{value: 1})
    task.group g {
        child :: task ^frozen { frozen.value }
        print(child.join() ?? 0)
    }
    print(frozen.value)
}
