// D-BOUND-SINK1=A: a library-declared head is a typed sink; bare String is E0149.
marker Pattern on [.Text] {
    check @body
    hole @value
}

fn accept(pattern: Pattern) {}

fn run() {
    raw :: "untrusted"
    accept(raw)
}
