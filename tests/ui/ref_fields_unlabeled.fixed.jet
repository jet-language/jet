// D-REF-SHORTHAND1: two unlabeled `&T` fields are fine at the definition —
// which value owns each borrow is only decided when the struct is built.
struct Pair {
    a: &String
    b: &String
}

fn main() {
    print(0)
}
