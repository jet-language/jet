fn main() {
    #grant(Io) { caps ->
        // Aliasing the capability handle to another binding lets it be carried
        // around — the capability is revoked at the grant's end, so it can't be
        // stored, returned, or shared past the block (E0711).
        alias @= caps;
        print("granted");
    }
}
