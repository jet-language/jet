fn run() {
    #FX(authority: IO) {
        // Aliasing the Authority handle to another binding lets it be carried
        // around — the Authority is revoked at the block's end, so it can't be
        // stored, returned, or shared past the block (E0711).
        alias :: authority
        print("granted");
    }
}
