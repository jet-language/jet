Error [E0365]: arm `Int` is unreachable — that case is already handled
  --> tests/ui/union_repeated_member_arm.jet:5:9
    |
  5 |         .Int(n) -> print(n)
    |         ^^^^^^^
 Why: every earlier arm already covers this pattern
 Fix: remove this arm or merge it with the one above
