use core.args as args
use core.process as process

fn run() {
    spec :: args.spec().flag("verbose")
    parsed :: spec.parse(process.argv()) ?? panic("parse failed")
    print(parsed.flag("verbose"))
}
