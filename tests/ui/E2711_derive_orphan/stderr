Error [E2711]: derive orphan rule: `derive Named for T` and `Bar` are both in an imported module
 Why: user-derive expansion can only run in the entry module; both the derive block and the struct are from the same imported file
 Fix: move both `derive Named` and `struct Bar` to your entry module; derive expansion only runs there
