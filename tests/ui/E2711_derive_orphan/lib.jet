// E2711 fixture: neither the UserDerive provider nor its target type is local
// to the entry module, so the generated impl would have no local owner.
derive T.Named {
    fn tag_name(self) => String :: T.@name
}

#Named
pub struct Bar {
    x: Int
}
