use c.cb as c

fn allocates(x: I32) =[]=> I32 {
    values :: [x]
    return x
}

#Extern module c.cb {
    fn call(cb: fn(I32) =[]=> I32, x: I32) => I32 = "call"
}

fn run() { print(c.call(allocates, 1)) }
