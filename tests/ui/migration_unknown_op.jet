// Tests E0911: an unsupported op inside migration { } → staged-to-D-MIGRATE2 teaching error.
struct Foo {
    x: Int
}

migration Foo {
    drop x
}

fn main() {
    f @= Foo { x: 1 }
    print("{f.x}")
}
