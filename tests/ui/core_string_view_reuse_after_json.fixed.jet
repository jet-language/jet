#Policy(copies: .Explicit)

use core.encoding.json as json

fn render(raw: String) String -> {
    view :: raw.after(":")
    node :: JSON.Text(~view)
    print(view.after("v"))
    return json.to_string(node)
}

fn run() {
    print(render("key:value"))
}
