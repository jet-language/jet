// E1313: a trait-bounded variadic call-site argument doesn't implement the
// bound trait (D-ANY-JAI1). `log(Int) -> Int` has no `Renderable` impl.
fn log_all(parts: ...Renderable) {
    loop p in parts {
        print("{p}")
    }
}

fn main() {
    double :: (x: Int) => x * 2
    log_all(1, double)
}
