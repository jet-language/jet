// E2933: every element inside a `Layout.{ … }` body must be a
// `>=`/`<=`/`==` comparison of layout values (D-LAYOUT1 / D-LAYOUT-CTOR1).
fn run() {
    x :: 1
    form :: Layout.{
        x == 1
    }
}
