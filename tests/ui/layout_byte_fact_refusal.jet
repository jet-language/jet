// D-LAYOUT-FACTS1=B: optional byte facts can be read; default layout keeps them absent.
derive T.LayoutBytes {
    layout :: T.reflect().layout
    bytes :: layout.fields[0].offset
}

#LayoutBytes
struct Packet {
    count: Int
}

fn run() {}
