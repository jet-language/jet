// D-LAYOUT-FACTS1=B: byte facts stay absent, but reads must explain why.
derive T.LayoutBytes {
    layout :: T.reflect().layout
    bytes :: layout.fields[0].offset
}

#LayoutBytes
struct Packet {
    count: Int
}

fn run() {}
