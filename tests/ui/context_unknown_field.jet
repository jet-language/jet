// D-CTX1: only `allocator` and `logger` are valid context fields.
// E0761 fires on an unknown field name.
use core.mem

fn main() {
    arena :: mem.Arena.new()
    #Context(scheduler: arena) {
        print("should not reach")
    }
}
