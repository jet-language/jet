// E3401: reading stdin inside a @Pure fn.
use core.io as io
@Pure fn read_first() -> String {
    handle :: io.stdin()
    line :: handle.read_line()
    return ""
}
fn main() {
    print(read_first())
}
