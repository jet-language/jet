use core.args as args
use core.io as io

fn run() {
    spec :: args.spec().option("output", "write to FILE")
    parsed :: spec.parse(io.args()) ?? panic("parse failed")
    print(parsed.option("output") ?? "none")
}
