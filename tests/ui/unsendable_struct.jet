use core.tasks
struct Logger {
    sink: &String
}
struct Config {
    logger: Logger
    id: Int
}
fn run() {
    cfg :: Config.{logger: Logger.{}, id: 1}
    t :: tasks.spawn(take(cfg) () => cfg.id)
    t.join()
}
