use core.tasks as tasks;

struct Logger {
    ref sink: String;
}

struct Config {
    logger: Logger;
    id: Int;
}

fn main() {
    val cfg = Config { logger: Logger { }, id: 1 };
    val t = tasks.spawn(take(cfg) () => cfg.id);
    t.join();
}
