// @programmable_build
// @build_locked
fn build(b: BuildContext) => BuildPlan ? {
    b.generate("locked", "fn generated_locked() => Int {{ return 1 }}")?
    app :: b.add_executable("app", ["build_locked_drift.jet", ".jet/generated/build_locked_drift/locked.jet"], [])?
    return b.plan(app)
}
fn run() {}
