// @programmable_build
// @build_locked
fn build(b: BuildContext) -> BuildPlan ? {
    b.generate("locked", "fn generated_locked() {}")?
    app :: b.add_executable("app", ["build_locked_drift.jet"], [])?
    return b.plan(app)
}
fn run() {}
