Error [E0510]: `core.crypto.vault.get` requires an audited `#Unsafe` region
  --> tests/ui/secret_ungranted.jet:4:16
    |
  4 |     v :: vault.get("db_password")
    |                ^^^
 Why: raw key import may only run inside an explicit expert-tier gate (I1)
 Fix: wrap the call in `#Unsafe("vault key import: …") { … }`

Error [E1264]: `handler` reads a secret but doesn't declare the `Secret` effect
  --> tests/ui/secret_ungranted.jet:3:4
    |
  3 | fn handler() {
    |    ^^^^^^^
 Why: reading a secret (`core.crypto.vault.get`) always requires an explicit grant — unlike other effects, there is no silently-inferred default here, so a function must opt in even with no other explicit effect row at all.
 Fix: add `=[Secret]=>` to `handler`'s signature (or widen an existing effect row to cover it)

Error [E1264]: `run` reads a secret but doesn't declare the `Secret` effect
  --> tests/ui/secret_ungranted.jet:7:4
    |
  7 | fn run() {
    |    ^^^
 Why: reading a secret (`core.crypto.vault.get`) always requires an explicit grant — unlike other effects, there is no silently-inferred default here, so a function must opt in even with no other explicit effect row at all.
 Fix: add `=[Secret]=>` to `run`'s signature (or widen an existing effect row to cover it)
