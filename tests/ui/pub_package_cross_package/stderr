Error [E0605]: `hidden` exists but is private to its file
  --> tests/ui/pub_package_cross_package/main.jet:4:15
    |
  4 |     print(dep.hidden())
    |               ^^^^^^
 Why: only names marked `pub` can be used from another file (S18)
 Fix: add `pub` before `hidden`, or don't reach across files here
