// E0367: bare unit variant arms need a leading `.` (D-ENUMDOT1).
enum Light {
    Red
    Green
}

fn show(light: Light) {
    if light == {
        Red -> { print("bare unit") }
        Green -> { print("ok") }
    }
}

fn run() {
    show(Light.Red)
}
