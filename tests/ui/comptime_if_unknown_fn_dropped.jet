// D-WHEN2: unknown function in the dropped arm must still be caught (E0102).
// Previously, E0107 (unknown var) was kept but E0102 (unknown fn) was silently dropped.
comptime flag = true

fn run() {
    comptime if flag {
        print("selected")
    } else {
        nonexistent_function_xyz()
    }
}
