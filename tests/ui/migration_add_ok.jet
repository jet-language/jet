// Tests that `add verified: Bool =  false` (D-MIGRATE2A) unblocks the added field.
// The sibling .published.snapshot supplies the prior shape (just `name`).
@PublishedSchema
struct Account {
    name: String
    verified: Bool
}

migration Account {
    add verified: Bool  = false
}

fn main() {
    a :: Account.{
        name: "Alice"
        verified: true
    }
    print("{a.name}")
}
