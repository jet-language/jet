// E2712: `$name` used outside any comptime context.
// The `$x` splice is only valid inside derive bodies, comptime {} blocks,
// or comptime binding RHS (D-CTMARKER1=C). At runtime it has no comptime
// scope and is rejected with E2712.

fn main() {
    x #= 42
    print($x)
}
