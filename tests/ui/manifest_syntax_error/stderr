Error [E1206]: `pkg.jet` has a shape error
 Why: dependency `helpers` has value `notaref`, which is neither a quoted version, a `provider@target` ref, nor an inline git struct
 Fix: check `pkg.jet` against docs/spec/syntax-decisions.md (U1)
