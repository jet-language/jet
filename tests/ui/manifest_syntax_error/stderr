Error [E1206]: `jet.toml` has a shape error on line 4
 Why: jet.toml uses a small subset of TOML — tables, strings, and inline tables only
 Fix: check `jet.toml` for unclosed quotes, missing `=`, or unsupported TOML features: string literal must fit on one line
