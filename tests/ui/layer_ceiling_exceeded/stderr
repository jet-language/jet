Error [E1006]: `core.files` needs the `std` runtime layer
  --> tests/ui/layer_ceiling_exceeded/main.jet:1:1
    |
  1 | use core.files as fs
    | ^^^^^^^^^^^^^^^^^^^^
 Why: this package declares `layer: core` in `pkg.jet`, which caps imports at the `core` layer; `core.files` is a `std` module; import chain: `use core.files`
 Fix: remove the import or helper use, raise the ceiling to `layer: std` in `pkg.jet`, or use a `core`-layer alternative
