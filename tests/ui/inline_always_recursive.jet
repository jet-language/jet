// E0917 (D-METHODMACRO1=A): `#InlineAlways` cannot expand a self-recursive call.
#InlineAlways fn factorial(n: Int) -> Int {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}

fn run() {
    print(factorial(5))
}
