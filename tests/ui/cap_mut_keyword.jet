// E0056: the `mut` capability keyword is retired (D-CAP7). Write access is the
// `~` sigil on the type: `n: ~Int`, and `~x` at the call site.
fn bump(mut n: Int) {
    n = n + 1
}

fn main() {
    x: Int = 1
    bump(mut x)
    print(x)
}
