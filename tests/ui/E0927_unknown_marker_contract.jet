// E0927 (card #518): `#Bogus` isn't a registered contract-plane marker and
// isn't a `derive T.Bogus { … }` provider either — a plain unrecognized
// name, not a typo close to anything real.
#Bogus
struct Widget {
    label: String
}

fn run() {
    print("ok")
}
