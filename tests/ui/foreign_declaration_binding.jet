// D-TEACH-FOREIGN1 (ballot open) / #1887's table: a keyword from another
// language is taught, not blamed on the expression grammar.
fun add(left: Int, right: Int) => Int {
    return left + right
}

fn run() {
    var count: Int = 1
    let total = count
    print(total)
}
