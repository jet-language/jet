use core.mem

struct Node { payload: Int }

fn consume(n: ^Node) => Int { return n.payload }

fn run() {
    node := Node.{payload: 7}
    pinned :: mem.pin(&node)
    taken :: consume(^node)
    print("{taken} {(pinned.payload)}")
}
