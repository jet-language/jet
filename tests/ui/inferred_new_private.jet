module hidden {
    struct Token {
        value: Int
    }

    impl Token {
        fn new(value: Int) -> Token {
            return Token.{ value: value }
        }
    }
}

fn run() {
    token: hidden.Token :: .new(1)
}
