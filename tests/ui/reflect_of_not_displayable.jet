// E0112: `reflect.of(x)` requires `x` to be interpolatable (the same gate as
// `"{x}"`, D-ANY-JAI1). A closure has no `Display`/`JetDisplay` impl.
use core.reflect as reflect

fn run() {
    double :: (x: Int) => x * 2
    v :: reflect.of(double)
    print(v.type_name())
}
