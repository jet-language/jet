use core.encoding.cbor as cbor

fn run() {
    value :: U64.{ 1 }
    cbor.to_bytes(value).drop("this fixture only pins E2411")
    cbor.decode<U64>([U8].{ 0x01 }).drop("this fixture only pins E2411")
}
