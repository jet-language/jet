Error [E0121]: `view` was given away earlier, so it can't be used here
  --> tests/ui/iter_double_drive.jet:6:10
    |
  6 |     b := view.to_list()
    |          ^^^^
 Why: after a value moves somewhere else, the old name no longer holds it
 Fix: `view` is one-pass — materialize it first with `view.to_list()`, or create a fresh source for the second drive
