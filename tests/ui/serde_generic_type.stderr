Error [E2413]: `Box` is generic, so it can't derive `Encode`/`Decode` yet
  --> tests/ui/serde_generic_type.jet:4:8
    |
  4 | struct Box<T> {
    |        ^^^
 Why: the built-in serde derive walks concrete fields; generic (de)serialization isn't built yet
 Fix: make `Box` concrete, or hand-write the (de)serialization for now
