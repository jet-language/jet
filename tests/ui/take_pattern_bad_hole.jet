// D-SHIFT1: a `take_pattern` hole binds a bare (optionally typed) name —
// an expression is not a hole.
fn main() {
    c :: Cursor.over("inc-42 rest")
    m :: c.take_pattern("inc-{id + 1} ") ?? panic("no match")
    print("{m}")
}
