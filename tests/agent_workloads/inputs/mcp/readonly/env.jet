module env.dev {
    prompt: $HOME
    imports: [env.agent.codex(mcp: [repo_server])]
    services: {
        fixture: {
            enable: false,
            ports: [8080],
            run: ["fixture", "--port", "8080"],
            ready: "fixture --ready",
            watch: ["src"],
            after: ["database"],
            sockets: ["run/fixture.sock"]
        }
    }
    files: ["config/generated.txt": File{ content: "generated\n", mode: .Copy }]
}
