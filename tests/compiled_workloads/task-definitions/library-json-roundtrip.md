# Library: JSON round trip

Ship a reusable library plus a test executable. Decode a versioned JSON
document into typed values, validate unknown fields and bounds, re-encode a
canonical form, and expose one public error type. Test malformed UTF-8,
duplicate keys, deep nesting, and a missing required field. Compare the
library API and test authoring boundary, not a one-line program.
