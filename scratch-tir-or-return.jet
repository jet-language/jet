fn checked(x: Int) Int !Err -> {
    if x == 0 {
        return Err("zero")
    }
    return Ok((100 /% x))
}
fn safe(x: Int) Int -> {
    return checked(x) ?? return -1
}
fn run() {
    print(safe(4))
    print(safe(0))
}
