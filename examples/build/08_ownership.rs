// Generated by jet — do not edit. Edit the .jet source instead.
// If rustc rejects this file, that is a bug in jet (invariant I2).
#![allow(warnings)]

trait JetShow { fn jet_show(&self) -> String; }
impl JetShow for i64 { fn jet_show(&self) -> String { self.to_string() } }
impl JetShow for f64 { fn jet_show(&self) -> String { format!("{:?}", self) } }
impl JetShow for bool { fn jet_show(&self) -> String { self.to_string() } }
impl JetShow for String { fn jet_show(&self) -> String { self.clone() } }
impl<T: JetShow> JetShow for &T { fn jet_show(&self) -> String { (**self).jet_show() } }

fn user_show(user_msg: &String) {
    println!("{}", ((*user_msg)).jet_show());
}

fn user_bump(user_n: &mut i64) {
    (*user_n) += 1i64;
}

fn user_archive(user_name: String) -> String {
    return user_name;
}

fn user_headline(user_text: &String) -> &String {
    return user_text;
}

fn main() {
    let mut user_score = 41i64;
    user_bump(&mut (user_score));
    println!("{}", (user_score).jet_show());
    let user_greeting = "hello".to_string();
    user_show(&(user_greeting));
    println!("{}", (user_greeting).jet_show());
    let user_saved = user_archive("vault".to_string());
    println!("{}", (user_saved).jet_show());
    println!("{}", (user_headline(&("Jet".to_string()))).jet_show());
}

