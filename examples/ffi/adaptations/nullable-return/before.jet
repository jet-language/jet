#Import module c.names {
    fn find_name(id: U64) String = "find_name"
}

// Unrun expectation: the low-level declaration exposes a nullable C pointer.
pub fn run() String -> {
    return c.names.find_name(7)
}
