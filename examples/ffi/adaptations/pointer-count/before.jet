#Import module c.sums {
    fn sum_bytes(bytes: &[U8], count: U64) U64 = "sum_bytes"
}

// Unrun expectation: the native arity is explicit and the caller supplies the count.
pub fn run() U64 -> {
    bytes := [10, 20, 30]
    return c.sums.sum_bytes(&bytes, 3)
}
