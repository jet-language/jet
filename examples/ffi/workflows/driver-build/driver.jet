// The CMake project is the foreign build owner.  This entry is available for
// an explicit Jet-side smoke after a plan is accepted; it never claims the
// CMake custom action as compiler-owned.
pub fn run() {
    print("foreign driver build")
}
