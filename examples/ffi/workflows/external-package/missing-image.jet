use c.png as png

pub fn run() {
    image :: png.Image.open("missing-or-corrupt.png", format: .RGBA) ?? panic("expected libpng error")
    defer image.close()
    print("unexpected decode: {image.width}x{image.height}")
}
