use cobol.payroll as payroll

fn run() {
    print(payroll.apply_minor(7, 10000) ?? panic("COBOL payroll call failed"))
    print(payroll.apply_minor(8, -10000) ?? panic("COBOL payroll call failed"))
}
