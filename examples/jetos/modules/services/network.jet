// modules/services/network.jet — networking baseline for every machine.
import jetpack as pkg;
import core.json as json;
pub fn contribute() -> [JSON] {
    return [pkg.services(["tailscale", "openssh"]), pkg.option("sys.networking.firewall", "on")];
}
