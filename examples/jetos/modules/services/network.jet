// modules/services/network.jet — networking baseline for every machine.

import jetpack as pkg;
import std.json as json;

pub fn contribute() -> List<Json> {
    return [
        pkg.services(["tailscale", "openssh"]),
        pkg.option("sys.networking.firewall", "on"),
    ];
}
