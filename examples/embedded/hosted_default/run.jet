// Typed hosted-default witness.
// The `hosted` machine keeps its allocator, panic, startup, and byte sink as
// hidden `HostedDefault` facts; this program uses the supplied default output.
fn run() { print("hosted default") }
