// Minimal FFI demo: return process ID as a placeholder.
// std::process::id() returns u32 which doesn't map directly to Jet's Int (i64);
// returning 0 as a stub until the FFI type-bridge layer supports u32.

pub fn pid() -> Int {
    return 0;
}

fn main() {
    print("pid: {pid()}");
}
