// 16 — Wordcount: THE M5 exit example.

fn main() {
    var counts: Map<String, Int> = [:];
    for word in "the quick the brown".split(" ") {
        counts[word] = (counts.get(word) or 0) + 1;
    }
    for key, count in counts {
        print("{key}: {count}");
    }
}
