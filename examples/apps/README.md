Flagship app slices live here. Each app is runnable with `jet run` and has a
deterministic expected output under its own `expected/` directory.

These are not product capstones yet. The capstone gate and current
classification live in `capstone-proof.md`.
