// D-CONF-MERGE1=A: every build fact follows one source-scope and layer law.
// The resolver tests prove the writer choices; this executable spec keeps the
// user-facing vocabulary visible on every execution tier.
fn run() {
    print("source scopes: item > block > function > module > file > package")
    print("effective source: item")
    print("layers: command line > fleet > system > environment > workspace > optimization bundle > declaration")
    print("effective layer: command line")
    print("same-layer conflict: E3521 names both writers")
    print("force winner: system/fleet .Force; CLI is shadowed")
    print(@build.package.name)
    print(@build.profile)
}
