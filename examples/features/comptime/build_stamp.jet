// D-CONF-STAMP1=B: provenance is a locked build input, not a wall-clock read.
fn run() {
    print(@build.stamp.git ?? "none")
    print(@build.stamp.dirty)
    print(@build.stamp.toolchain)
    print(@build.stamp.at)
}
