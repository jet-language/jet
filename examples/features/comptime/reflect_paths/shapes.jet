use core.reflect as reflect

pub struct Point {
    pub value: Int

    impl Display {
        fn display(self) => String :: "Point({self.value})"
    }
}

pub fn show() {
    point :: Point.{value: 7}
    reflected :: reflect.of(point)
    print(reflected.type_name())
    print(reflected.path())
}
