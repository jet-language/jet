// D-CTFIND1/2: `find(glob)` expands a build-time glob, sorts the relative file
// paths, and records each matched file hash for reproducible builds.
comptime paths = find("find_inputs/**/{{alpha,beta}}-[0-9].t?t")

fn run() {
    print(paths.join("|"))
}
