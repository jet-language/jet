// 15 — Lists: literals, methods, slice, join (M5).
fn main() {
    var nums: List<Int> = [3, 1, 2];
    nums.push(4);
    nums.sort();
    print(nums[0]);
    print(nums.get(99) or (-1));
    val mid = nums[1..2];
    print(mid.join(","));
}
