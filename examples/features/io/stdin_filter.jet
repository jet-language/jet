// D-STDIN1=A: streaming line-by-line stdin.
// Filter stdin lines containing "jet" and print matches.
// When stdin is empty (as in golden tests), no output is produced.
use core.io as io

fn main() {
    loop line in io.stdin().lines() {
        if line.contains("jet") {
            print(line)
        }
    }
}
