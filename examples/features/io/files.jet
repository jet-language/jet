use core.fs as fs
fn main() {
    path :: "/tmp/jet_m10_files.txt"
    wrote :: fs.write(path, "hello\njet") ?? panic("write failed")
    text :: fs.read(path) ?? panic("read failed")
    print(text.to_upper())
    print(fs.exists(path))
}
