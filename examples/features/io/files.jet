use core.files as fs

fn run() ? {
    path :: "/tmp/jet_m10_files.txt"
    wrote :: fs.write(path, "hello\njet") ?? return Err("write failed")
    text :: fs.read(path) ?? return Err("read failed")
    print(text.to_upper())
    print(fs.exists(path))
}
