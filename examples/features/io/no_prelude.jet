#NoPrelude

// D-PRELUDEX1=A: `#NoPrelude` disables every readable Core prelude alias.
// An explicit qualified Core import restores the needed operation.
use core.io as io

fn run() { io.print("no-prelude: io.print works") }
