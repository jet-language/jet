@NoPrelude

// D-PRELUDEX1=A: `@NoPrelude` disables ambient `print` / `input`.
// Explicit `use core.io as io` restores access under a qualified name.
use core.io as io

fn run() {
    io.print("no-prelude: io.print works")
}
