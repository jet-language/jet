use core.sys as env

fn run() {
    inherited :: env.get("JET_C2319_INHERITED") ?? "ΔΔ"
    print(inherited.len())
    print(inherited.slice(0, 0))

    env.set("JET_C2319_VALUE", "ΔΔ")
    value :: env.get("JET_C2319_VALUE") ?? ""
    print(value.len())
    print(value.slice(0, 0))
}
