// D-TESTING1: corpus, fixture, and golden helpers all read explicit paths.
use core.testing as testing

fn run() {
    corpus :: testing.corpus("examples/features/io")
    fixture :: testing.fixture("examples/features/expected/io/db.out")
    golden :: testing.golden("examples/features/expected/io/testing_projection.out", "corpus:{corpus.len()}\nfixture:{fixture.len()}\ngolden:true\n")
    print("corpus:{corpus.len()}")
    print("fixture:{fixture.len()}")
    print("golden:{golden}")
}
