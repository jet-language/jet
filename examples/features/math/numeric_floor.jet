// Numeric floor flagship for examples/features/math/ (D-EXAMPLES-SHORTPATH1=A).
// Broader libm + integer-safety families: math_audit.jet.
use core.math as math

fn run() {
    print(math.abs((-7.0)))
    print(math.max(3.0, 9.0))
    print(math.min(3.0, 9.0))
    print(math.clamp(15.0, 0.0, 10.0))
    print(math.sqrt(9.0))
    print(math.hypot(3.0, 4.0))
}
