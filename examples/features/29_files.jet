import core.fs as fs;
fn main() {
    val path = "/tmp/jet_m10_files.txt";
    val wrote = fs.write(path, "hello\njet") ?? panic("write failed");
    val text = fs.read(path) ?? panic("read failed");
    print(text.to_upper());
    print(fs.exists(path));
}
