// M8: function-typed parameters and a stored callback.
fn apply_twice(f: fn(Int) => Int, x: Int) => Int :: f(f(x))

fn double(x: Int) => Int :: (x * 2)

struct Worker {
    step: fn(Int) => Int
}

fn run() {
    double_fn :: double
    print(apply_twice(double_fn, 3))
    print(apply_twice((x: Int) => (x + 1), 5))
    w :: Worker.{step: (n: Int) => (n * n)}
    print(w.step(4))
}
