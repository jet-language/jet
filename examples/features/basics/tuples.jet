// S73 (D-SG7): named-only tuples — literals, types, access, equality, destructure.
fn bounds() -> (max: Int, min: Int) {
    return (min: 0, max: 10)
}

fn main() {
    p :: (x: 1, y: 2)
    q :: (y: 3, x: 4)
    same_shape :: (p == q)
    (a, b) :: p.clone()
    print("{p.x} {p.y} {a} {b} {same_shape}")
    pair :: bounds()
    print("{pair.min} {pair.max}")
}
