// body_rules — D-ONELINE-BODY1=B and D-LOOP-STMT-ARROW1=C.
fn twice(n: Int) => Int :: n * 2

fn run() {
    if true -> print("if arrow")
    loop value, [1, 2] -> print("loop {value}")
    print(twice(3))
}
