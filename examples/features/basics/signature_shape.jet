// D-SIG-SHAPE1=B: a result is bare after the parameters and `:>` starts the body.
fn double(n: Int) Int :> n * 2

fn bump(n: Int) Int :[IO]> {
    print(n)
    n + 1
}

fn run() :[IO]> { print(bump(double(20))) }
