// The canonical beginner workflow uses the same `run.jet` entry that `jet new`
// creates. Check, test, fix, and explain operate on this source.
fn greet(name: String) String -> "hello, {name}"

#CLI
struct GreetingArgs {
    #Doc("name to greet") name: String{"Jet"}
}

#Test("greet says hello") {
    assert_eq(greet("Jet"), "hello, Jet")
}

fn run(args: GreetingArgs) { print(greet(args.name)) }
