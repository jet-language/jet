// D-GO127-STDLIB1=A: typed String-keyed maps encode in ascending key order.
use core.encoding.json as json

fn run() {
    values :: [String:Int]{
        "z": 26,
        "a": 1,
        "m": 13
    }
    print(json.to_string(values))
}
