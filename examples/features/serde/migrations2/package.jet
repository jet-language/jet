name: "migrations2"
version: "2.0.0"
authority: {
    holds: {
        allow: [Exec, FS, IO, Mem.Alloc]
    }
}
description: "D-MIGRATE2 example: every schema migration op on one record"
license: "MIT"
