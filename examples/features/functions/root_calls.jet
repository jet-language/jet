// D-CALLDUAL1=E: one marked receiver supports both call spellings.
fn scale(#Root value: Int, factor: Int) => Int {
    return value * factor
}

fn run() {
    total :: 3
    print(total.scale(4))
    print(scale(total, 4))
    total.print()
}
