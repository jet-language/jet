// D-JPK-TASKRUN1 (card #476) — project tasks are `#Task fn`s.
// Invoke: `jetpack run greet`  (or `jet run --task greet <this-file>`).
// Cross-task dependency = a plain call (`seed` calls `greet` below).
#Task
fn greet() {
    print("hello from task")
}

#Task
fn seed() {
    greet()
    print("seeded")
}

fn run() {
    print("use jetpack run greet")
}
// --- expected output --------------------------------------------------
// use jetpack run greet
