// 16 — Wordcount: THE M5 exit example.
fn main() {
    counts: [String, Int] = [:]
    loop word in "the quick the brown".split(" ") {
        counts[word] = ((counts.get(word) ?? 0) + 1)
    }
    loop key, count in counts {
        print("{key}: {count}")
    }
}
