// D-LOOPREAD1=A: pull one stdin line per turn and stop on the blank sentinel.
// Repeated input belongs in the source-loop `in` form, not a state-loop header.
use core.term as io

fn run() {
    loop line in io.stdin().lines() {
        if line == "" { break }
        print(line)
    }
}
