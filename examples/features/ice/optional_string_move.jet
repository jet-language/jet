struct Config {
    path: ?String
}

fn selected(config: Config) String -[]> {
    return config.path ?? "default.toml"
}

fn run() {
    print(selected(Config{path: None}))
    print(selected(Config{path: Val("settings.toml")}))
}
