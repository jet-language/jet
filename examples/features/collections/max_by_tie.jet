// Card #2298: min_by and max_by keep the last tied item.
fn run() {
    tied :: [10, 20, 30]
    min_tie :: tied.min_by((value: Int) -> 0) ?? -1
    max_tie :: tied.max_by((value: Int) -> 0) ?? -1
    print("min-by-tie={min_tie}")
    print("max-by-tie={max_tie}")
}
