// 16 — Wordcount: THE M5 exit example.
fn run() {
    counts: [String: Int] := []
    loop word; "the quick the brown".split(" ") {
        counts[word] = ((counts.get(word) ?? 0) + 1)
    }
    loop key, count; counts {
        print("{key}: {count}")
    }
}
