// Indexed struct fields are mutable places.
struct Point {
    x: Int
}

fn run() {
    points := [Point.{x: 1}, Point.{x: 2}]
    points[0].x = 11
    print(points[0].x)
    points[0].x += 1
    print(points[0].x)
}
