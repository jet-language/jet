// List predicate traversal and first-match replacement.
fn run() {
    values := [1, 2, 2, 3]
    print(values.count_where(item -> item % 2 == 0))
    changed :: values.update_first(item -> item == 2, 9)
    print(changed)
    print(values)
    unchanged :: values.update_first(item -> item == 8, 7)
    print(unchanged)
    print(values)
}
