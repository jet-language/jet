// Card #2296: release codegen must honor the sema-resolved collection shapes.
fn run() {
    values := [Float]{Float.NAN, 1.0}
    values.sort()
    print(values)
    print([1, 2].indexed().to_list())
    print([1, 2].zip([3, 4]).to_list())
    print([Int]{} == [Int]{})
}
