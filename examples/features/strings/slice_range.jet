// S40 / D-RANGE-VALUE1: String slices use character positions and accept
// both an inline Range and a stored Range value.
fn run() {
    text :: "hello"
    r :: 0..4
    print(text.slice(0..4))
    print(text.slice(0, 4))
    print(text.slice(r))
}
