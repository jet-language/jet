// D-SHAPE-MODULEINTERNAL1=A: this declaration is omitted from automatic
// project discovery until another source writes `use project._bench`.
module _bench { }

fn run() { print("internal project parts require explicit imports") }
