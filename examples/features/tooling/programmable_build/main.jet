fn build(b: BuildContext) => BuildPlan ? {
    // D-BUILDCTX-FLAGS1=A: declare shared defaults once; CLI --allow-* / --profile still win when passed.
    b.default_profile("release")
    b.default_allow(["Exec", "FS"])
    b.generate("build_message") {
        fn generated_build_message() => String = "generated source checked";
    }?
    app :: b.add_executable("programmable_build", ["main.jet", ".jet/generated/main/build_message.jet"], [])?
    return b.plan(app)
}

fn run() { print(generated_build_message()) }
