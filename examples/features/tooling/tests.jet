// 20 — top-level test blocks (M6/S43).
fn double(n: Int) Int :> (n * 2)

fn run() { print("{double(3)}") }

#Test("double returns twice the input") {
    assert_eq(double(3), 6)
    assert_eq(double(0), 0)
}

#Test("double is consistent") {
    assert((double(5) == 10))
}
