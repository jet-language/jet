// D-CLAIM-BENCH1=A: measurement is a mode of `#Test`, not a second claim
// family. `jet test --measure` selects this claim; `jet run` keeps the program
// output below as the executable golden.
module test_perf;

fn run() { print("sum_to(1000) = {test_perf.sum_to(1000)}") }

#Test("sum_to(1000)") {
    .measure {
        assert_eq(keep(test_perf.sum_to(1000)), 500500)
    }
}
