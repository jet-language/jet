// The member graph makes the build order visible: foundation is a dependency
// of app, and the root package build runs after both members. The app package
// has no fn build, so it uses the batteries fallback.
module workspace {
    members: find("./packages")
}

fn run() { print("nested build entry") }
