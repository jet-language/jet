#Meta(category: "Movement", tunable)
fn step_speed(speed: Int) => Int {
    next :: speed + 1
    return next
}

fn run() {
    #Meta(category: "Movement", tunable) speed :: 3
    print("{step_speed(speed)}")
}
