fn helper() => Int :: 1

fn square(n: Int) => Int :: n * n

fn summarize(limit: Int) => Int {
    if {
        true -> { print("branch") }
        else -> { print("else") }
    }
    total :: square(limit)
    return if total > 10 -> total else -> total + 1
}

fn run() { print(summarize(4)) }
