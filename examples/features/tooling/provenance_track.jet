fn run() {
    #Track speed :: Float{3.5}
    plain :: Float{3.5}
    print(speed.origin().starts_with("tracked `speed` at examples/features/tooling/provenance_track.jet:2:12"))
    print(plain.origin())
}
