// `jet inspect structure examples/features/tooling/structure_plane.jet`
// exposes the registered structure rows without changing this program.
use "structure_plane/support" as support

fn unused_structure_subject() {}

fn run() {
    support.old()
    print("structure fact plane")
}
