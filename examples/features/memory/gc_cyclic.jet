// D-OPTGC1 / D-DEP-GC1: opt-in traced `Gc<T>` handles + explicit collection.
use core.gc

fn main() {
    a :: gc.Gc.new<Int>(1)
    b :: gc.Gc.new<Int>(2)
    keep_a :: a.clone()
    keep_b :: b.clone()
    gc.collect()
    print(1)
}
