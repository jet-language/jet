// D-OPTGC1 / D-DEP-GC1: opt-in traced `Gc<T>` handles + explicit collection.
use core.gc

fn run() {
    a :: gc.Gc.new<Int>(1)
    b :: gc.Gc.new<Int>(2)
    keep_a :: ~a
    keep_b :: ~b
    gc.collect()
    print(1)
}
