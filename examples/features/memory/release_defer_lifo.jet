struct Resource {
    name: String
}

impl Resource.Close {
    fn close(^self) { print("close {self.name}") }
}

fn run() {
    first := Resource{name: "first"}
    defer close(^first)
    second := Resource{name: "second"}
    defer close(^second)
    print("body")
}
