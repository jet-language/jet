// D-REFSTRUCT1: `#Ref(owner)` stored-reference fields prove the owner relationship.
struct TokenView {
    #Ref(source) text: String
}

fn show(source: String) {
    t: TokenView #= TokenView.{ text: source }
    print(t.text)
}

fn main() {
    show("hello")
}
