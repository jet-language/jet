// D-BROWSER-AUTO1=A (#1193): cleanup, profile isolation, privacy, and redaction.
// Isolated BiDi user contexts, privacy defaults, and redacted normal receipts.
use core.browser as browser

fn run() {
    // Live endpoints come from the host. This example only documents the API.
    print("privacy:isolated_profiles+receipt.redacted+context.user_hash+cleanup")
}
