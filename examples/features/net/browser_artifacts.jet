// D-BROWSER-AUTO1=A (#1191): storage, files, screenshots, and PDF.
// Cookies and local/session storage, file upload, download folder, screenshot,
// and print-to-PDF on the native BiDi path.
use core.browser as browser

fn run() {
    // Live endpoints come from the host. This example only documents the API.
    print("artifacts:cookie+storage+set_files+allow_downloads+screenshot+pdf")
}
