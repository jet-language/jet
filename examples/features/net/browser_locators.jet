// D-BROWSER-AUTO1=A (#1189): semantic locators, actions, and deterministic waits.
// Connect to a BiDi endpoint, find controls by role/text/label/test id, wait,
// fill, press, hover, click, then wait_gone.
use core.browser as browser

fn run() {
    // Live endpoints come from the host. This example only documents the API.
    print("locators:get_by_role+text+label+placeholder+test_id+css")
    print("actions:wait+wait_gone+click+hover+fill+press")
}
