// D-BROWSER-AUTO1=A (#1192): checked expert CDP supplement.
// Capability-gated protocol("cdp") only — BiDi cannot smuggle goog:cdp.*.
use core.browser as browser

fn run() {
    // Live endpoints come from the host. This example only documents the API.
    print("cdp:capabilities.cdp+protocol(cdp)+Domain.command")
}
