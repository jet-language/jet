name: "backend_battery"
version: "0.1.0"
packages: {
    backend_battery: executable,
}
deps: {
    backend: "0.1.0",
}
authority: {
    holds: {
        allow: [DB, FS, IO, Log, Mem.Alloc, Mem.Rc, Net, Panic, Time],
    },
}
