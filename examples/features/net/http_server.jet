// core.http.server: function-first mux with path params.
use core.http.server as Server
use core.net as net
use core.tasks as tasks

fn run() {
    mux :: Server.mux()
    mux.get("/", (req: HttpSrvReq) => Server.response(200, "hello from mux"))
    mux.get("/ping", (req: HttpSrvReq) => Server.response(200, "pong"))
    mux.post("/echo", (req: HttpSrvReq) => Server.response(200, req.body()))
    print("mux ready")
}
