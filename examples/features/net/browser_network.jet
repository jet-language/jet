// D-BROWSER-AUTO1=A (#1190): events, network inspection, and interception.
// Subscribe to network events, inspect redacted request facts, add intercepts,
// then continue / fail / fulfill paused requests.
use core.browser as browser

fn run() {
    // Live endpoints come from the host. This example only documents the API.
    print("network:subscribe+inspect+intercept+continue|fail|fulfill")
}
