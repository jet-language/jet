// D-BROWSER-AUTO1=A (#1188): browser/context/page/tab/frame lifecycle.
// Connect to a BiDi endpoint, open tabs, walk the frame tree, close cleanly.
use core.browser as browser

fn run() {
    // Live endpoints come from the host. This example only documents the API.
    print("lifecycle:context.tab+page.frames+frame.close")
}
