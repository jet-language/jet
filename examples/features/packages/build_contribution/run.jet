// D-CONF-SPLIT1 / I9: the computed setting is folded before runtime codegen.
fn build(b: BuildContext) => BuildPlan ? {
    b.contribute("cache_slots", 4)
    return b.plan()
}

fn run() { print(@build.settings.cache_slots) }
