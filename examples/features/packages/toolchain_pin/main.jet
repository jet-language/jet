// This project pins its Jet toolchain in pkg.jet (`jet: 1.0`). Because the
// running `jet` is in that channel, it builds and runs the program directly —
// no download, no re-exec. Run `jet self toolchain` here to see the pin and its
// locked version.
fn run() { print("built by the pinned toolchain") }
