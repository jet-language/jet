// D-NAME-FENCE1=A / D-PUBPKG1: `pub(package)` opens a package fence.
// `helper` is inside this package, while `dep` is a separate path package.
use helper
use dep

fn run() {
    print(helper.inside())
    print(dep.public())
}
