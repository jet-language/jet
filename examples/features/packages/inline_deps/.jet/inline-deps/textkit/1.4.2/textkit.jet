// A tiny stand-in "published" library, staged locally under
// `.jet/inline-deps/textkit/1.4.2/` the same way a real resolved inline
// dependency (or a `jet store lock`-populated cache) would sit next to the script
// that depends on it.
pub fn shout(s: String) => String = "{s}!"