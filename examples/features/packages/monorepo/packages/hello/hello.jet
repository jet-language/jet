// The `hello` workspace member. Addressable as `repo.hello`, `packages/hello`,
// or the bare name `hello` once the workspace index is built.
module hello {
    pub fn greeting() => String = "hello from the monorepo"
}

fn run() { print(hello.greeting()) }
