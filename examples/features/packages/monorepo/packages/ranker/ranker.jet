// The `ranker` workspace member. Addressable as `repo.ranker`,
// `packages/ranker`, or the bare name `ranker`.
module ranker {
    pub fn top() -> String { return "ranker: #1 monorepo demo" }
}

fn run() { print(ranker.top()) }
