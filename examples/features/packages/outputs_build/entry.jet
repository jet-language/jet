// Not named `main.jet` or `run.jet` on purpose — see package.jet. This
// file's only job is the `service` alias that `entry: service.run` (in
// package.jet) follows to `service/module.jet`. The compiled program's
// actual entry is that file, not this one.
use "service/module" as service
