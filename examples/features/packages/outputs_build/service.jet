// Not named `main.jet` or `run.jet` on purpose — see package.jet. The
// package's `outputs: .{ demo: .Executable.{ entry: run } }` is what points
// `jet run`/`jet build` at this file's `fn run`.
fn run() {
    print("outputs: demo drives the build")
}
