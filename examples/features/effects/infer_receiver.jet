struct Player {
    hp: Int
}

impl Player {
    fn damage(&self, n: Int) { self.hp = self.hp - n }
}

fn hurt(p: &Player) { p.damage(5) }

fn run() {
    x := Player.{hp: 100}
    hurt(&x)
    print(x.hp)
}
