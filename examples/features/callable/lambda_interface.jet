// D-LAMBDA-IFACE1=A: a stored lambda may spell its full callable interface.
enum LambdaError {
    Invalid
}

fn run() {
    increment :: (n: Int) Int LambdaError! -[]> Ok(n + 1)
    print(increment(41) ?? 0)
}
