// D-TYPE2-DEFAULT1=A: decimal literals use exact base-10 arithmetic.
fn run() {
    price :: 19.99
    print((price * 3).to_string())
    print(0.1 + 0.2 == 0.3)

    // Explicit construction remains the named exact Decimal path.
    tax :: Decimal("0.08")
    print((price + tax).to_string())
}
