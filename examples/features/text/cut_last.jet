// D-GO127-STDLIB1=A: terminal-boundary string splitting, paired with the
// existing first-boundary `split_once` operation.
fn run() {
    pair :: "root/branch/leaf".cut_last("/") ?? panic("missing cut")
    print(pair.before)
    print(pair.after)
    print("no-separator".cut_last("/") == None)
}
