// D-TYPEALIAS1: transparent generic type aliases shorten fallible signatures.
enum AppError {
    Empty
}

alias Parsed<T> = T ? AppError

fn parse_count(raw: String) -> Parsed<Int> {
    if raw == "" { return Err(AppError.Empty) }
    return Ok(42)
}

fn run() {
    n :: parse_count("7") ?? 0
    print(n)
}
