use core.science.measurement as M

fn run() {
    g := M.from(9.8, 0.1)
    t := M.from(2.0, 0.05)
    t2 := t.mul(t)
    h := M.from(0.5, 0.0).mul(g).mul(t2)
    print(g)
    print(t)
    print(t2)
    print(h)
    // accessor methods
    print(h.value())
    print(h.uncertainty())
}
