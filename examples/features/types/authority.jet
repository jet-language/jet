// D-AUTHORITY-WORD2=E: Authority is the one rights value that crosses
// process, plugin, and session boundaries. It only narrows.
use core.process as process

struct Holder {
    grant: Authority
}

fn run() {
    #FX(grant: Exec, IO) {
        result :: process.run(["echo", "authority"], grant)
        print("authority")
    }
}
