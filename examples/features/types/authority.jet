// D-AUTHORITY-NAME1=A: Authority is the one rights value that crosses
// process, plugin, and session boundaries. It only narrows.
struct Holder {
    authority: Authority
}

fn run() {
    authority :: Authority.workspace()
    narrowed :: authority.with("FS.Read")
    released :: narrowed.without("FS.Read")
    print("authority")
}
