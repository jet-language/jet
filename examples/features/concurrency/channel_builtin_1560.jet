// D-CONC-CHAN1 / card #1560: channel<T>() is a builtin value with no import.
fn run() {
    (tx, rx) :: channel<Int>(capacity: 8)
    tx.send(42)
    print(rx.receive() ?? panic("channel closed"))
}
