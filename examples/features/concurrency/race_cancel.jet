// 167 — D-CONC-SPAWN1=D: race returns first success; losers cancel at runtime.
use core.time as time

fn slow_one() => Int {
    time.sleep(200)
    return 1
}

fn fast_seven() => Int = 7

fn run() {
    task.group g {
        winner :: task.race { slow_one(), fast_seven() } ?? panic("race failed")
        print(winner)
    }
}
