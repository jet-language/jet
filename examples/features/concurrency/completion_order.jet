// D-CONCCOMB1/A: race/any use task completion order, not task-list order.
use core.time as time

fn slow_one() => Int {
    time.sleep(100)
    return 1
}

fn fast_seven() => Int = 7

fn run() {
    task.group race_group {
        time.sleep(200)
        print(task.race { slow_one(), fast_seven() })
    }
    task.group any_group {
        time.sleep(200)
        print(task.any { slow_one(), fast_seven() })
    }
}
