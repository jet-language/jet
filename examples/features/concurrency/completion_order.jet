// D-CONCCOMB1/A: race/any use task completion order, not task-list order.
use core.time as time

fn slow_one() Int -> {
    time.sleep(100ms)
    return 1
}

fn fast_seven() Int -> 7

fn run() {
    time.sleep(200ms)
    print(task.race {slow_one(), fast_seven()} ?? panic("race failed"))
    time.sleep(200ms)
    print(task.any {slow_one(), fast_seven()} ?? panic("any failed"))
}
