// D-EVENT1: synchronous event policies keep listener dispatch deterministic.
use core.event as event

fn run() {
    policy :: event.policy_sync()
    events :: event.with_policy<Int>(policy)
    print("listeners:{events.listener_count()}")
}
