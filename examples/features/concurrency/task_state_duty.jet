// D-CONC-UNIT1: task state and join duty are compiler facts. They erase before codegen.
fn run() {
    joined :: task 40 + 2
    print(joined.join() ?? 0)

    detached :: task 0
    detached.detach()
    print("detached")
}
