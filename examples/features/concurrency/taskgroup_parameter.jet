// D-TASKGROUP-PARAM1=A: pass a taskgroup to a named helper.
fn print_next(group: TaskGroup, value: Int) {
    task :: group.task => value + 1
    print(task.join())
}

fn run() {
    taskgroup group {
        print_next(group, 41)
    }
}
