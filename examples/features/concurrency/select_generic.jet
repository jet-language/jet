// D-SELECT-GENERIC1=A: select carries a non-Int payload through one generic door.
use core.tasks as tasks

fn run() {
    task.group g {
        (sender, receiver) :: tasks.channel<String>()
        sender.send("generic")
        value :: g.select().recv(receiver).wait()
        print(value)
    }
}
