// 157 — D-COROUTINE1: coroutine-grade control via task handles (internal substrate).
use core.tasks as tasks

fn main() {
    t #= tasks.spawn(() => 7)
    t.pause()
    print(t.trace())
    t.resume()
    t.cancel()
    print(t.trace())
    print(t.join())
}
