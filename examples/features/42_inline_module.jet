// D-MOD2: inline code modules — group functions under a named namespace.
module math {
    pub fn double(n: Int) -> Int {
        return n * 2
    }

    pub fn add(a: Int, b: Int) -> Int {
        return a + b
    }
}
fn main() {
    print(math.double(5))
    print(math.add(3, 4))
}
