// D-MOD3: use alias.{A, B} brings multiple items into scope
module math {
    pub fn double(n: Int) -> Int {
        return n * 2;
    }

    pub fn triple(n: Int) -> Int {
        return n * 3;
    }
}

use math.{double, triple};

fn main() {
    print(double(5));
    print(triple(4));
}
