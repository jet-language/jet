// D-FAIL-ERROR1=A / D-FAIL-EDGE1=A: an unhandled default Err is rendered at
// the program edge through the shared Prelude report.
fn run() ? Err {
    return Err("unhandled default error", code: "RUNFAIL", cause: Err("disk offline"))
}
