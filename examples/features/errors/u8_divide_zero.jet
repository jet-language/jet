// #1484: fixed-width / by zero uses Prelude wording and exit 70 (I2),
// never a raw Rust "attempt to divide by zero" at exit 101.
fn score(n: U8) => U8 {
    return n
}

fn run() {
    print(score(U8.{10}) / score(U8.{0}))
}
