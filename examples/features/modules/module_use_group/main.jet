// D-CORE-USELIST1: use alias.[A, B] brings multiple items into scope
use scale.[double, triple]

module scale {
    pub fn double(n: Int) Int :> n * 2

    pub fn triple(n: Int) Int :> n * 3
}

fn run() {
    print(double(5))
    print(triple(4))
}
