pub fn clamp(n: Int, lo: Int, hi: Int) -> Int {
    if n < lo {
        return lo
    }
    if n > hi {
        return hi
    }
    return n
}

pub fn lo() -> Int {
    return 0
}

pub fn hi() -> Int {
    return 100
}
