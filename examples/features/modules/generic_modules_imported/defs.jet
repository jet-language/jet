const base = 40

struct Marker {
    value: Int
}

fn helper() -> Int { return 1 }

pub module box<T> {
    pub fn value(v: T) -> T { return v }

    pub fn marker() -> Marker {
        return Marker.{ value: base + helper() }
    }
}
