use defs.Box
module defs

module IntBox = Box<Int>

const BASE = 900

struct Marker {
    value: String
}

fn helper() -> Int {
    return 900
}

fn run() {
    print(IntBox.value(41))
    print(IntBox.marker().value)
}
