use defs.box

module defs

module int_box = box<Int>

const base = 900

struct Marker {
    value: String
}

fn helper() => Int = 900

fn run() {
    print(int_box.value(41))
    print(int_box.marker().value)
}
