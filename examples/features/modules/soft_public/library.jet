// D-STRUCT-LIFE1=A: a public retiring item carries one lifecycle marker and a
// named replacement. Consumers can migrate the member with `jet fix`.
#Deprecated(since: "2027", use: "replacement")
pub fn _legacy() { print("legacy") }

pub fn replacement() { print("replacement") }
