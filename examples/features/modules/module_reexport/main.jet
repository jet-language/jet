// D-MOD4: a directory module re-exports a submodule item with `pub use`.
// `text.wrap` resolves through text/module.jet to text/wrap.jet, while the
// non-re-exported helper stays internal.
module text
fn main() {
    print(text.wrap("hi"))
}
