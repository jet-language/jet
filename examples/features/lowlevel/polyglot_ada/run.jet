// Run `jet inspect bind ada geodesy.ads --pkg geodesy`, then `jet run main.jet`.
use ada.geodesy as geo

fn run() -[FFI.Ada, IO]> {
    print(geo.double_lat(95.0) ?? -1.0)
    print(geo.calls(0) ?? -1)
    print(geo.double_lat(21.0) ?? -1.0)
    print(geo.calls(0) ?? -1)
}
