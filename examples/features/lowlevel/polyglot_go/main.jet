// Run `jet inspect bind go handles.go --pkg handles`, then `jet run main.jet`.
use go.handles as handles

fn run() =[Go, Io]=> {
    handle :: handles.new_handle(42)
    print(handles.consume_handle(handle))
}
