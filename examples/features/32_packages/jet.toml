[package]
name = "wordapp"
version = "0.1.0"
jet = ">=0.1.0"
description = "Example multi-package project (M12.1)"
license = "MIT OR Apache-2.0"
repository = ""

[dependencies]
greeter = { path = "greeter" }
