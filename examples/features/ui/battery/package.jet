name: "gui-battery-example"
version: "0.1.0"
edition: "2026"
deps: {
    gui: "0.1.0",
}
authority: {
    holds: { allow: [FS] },
    needs: [UI.FileDialog, UI.Clipboard, UI.Ime, UI.DragDrop, UI.Shortcuts, UI.Accessibility, UI.FontShaping]
}
