fn converge(flag: Bool) {
    value :: 1
    done :: value
    if flag {
        print(value)
        finish(done)
    } else { print(value) }
}

fn finish(value: Int) { print(value) }

fn run() { converge(true) }
