// D-SG5: `"""…"""` spans many lines. The newline after the opening `"""` and
// the one before the closing `"""` are dropped; the closing `"""`'s indentation
// is stripped from every line. Escapes and `{interp}` stay active.
fn main() {
    who #= "Jet"
    banner #= """
    Welcome to {who}!
      - safe by default
      - friendly errors
    """
    print("{banner}")
}
