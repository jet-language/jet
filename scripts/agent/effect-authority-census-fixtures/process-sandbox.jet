use core.process as process

fn run() {
    #FX(authority: Exec, IO, Time.Wait) {
        restricted :: authority.without("Exec")
        process.run(["true"], restricted) ?? panic("process refusal fixture")
    }
}
