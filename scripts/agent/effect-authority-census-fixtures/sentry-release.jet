use core.mem

fn run() {
    arena :: mem.Arena.new()
    #Unsafe("pointer is used only after arena reset to prove quarantine") {
        stale :: *Int{*arena.alloc(7)}
        arena.reset()
        print(stale.*)
    }
}
