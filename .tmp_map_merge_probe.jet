fn run() {
    left := [String:Int]{"x": 10, "y": 1}
    right := [String:Int]{"x": 4, "z": 7}
    summed := left.merge(right, conflict: (key: String, a: Int, b: Int) -> a + b)
    print(summed.get("x") ?? 0)
    print(summed.get("y") ?? 0)
    print(summed.get("z") ?? 0)
}
