/* The package adapter includes the canonical checked body once. Keeping the
 * implementation path explicit prevents a source/header mismatch from being
 * mistaken for a proved binding. */
#include "../native/checked_native.c"
