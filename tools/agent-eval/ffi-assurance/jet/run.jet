// Safe caller fixture: the call carries a String value, not a raw pointer.
// The native body remains foreign and is never promoted to a universal safe
// guarantee by this facade.
#Import(c) fn fa_checked_probe(value: String) String = "fa_checked_probe"

fn run() {
    result :: fa_checked_probe("ffi-assurance")
    print(result)
}
