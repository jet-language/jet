// Candidate: the same ordered operations with a reusable first partial value.
fn run() {
    first :: Float{10000000000000000.0} + Float{-10000000000000000.0}
    float_value :: first + Float{1.0}
    checked := 100
    checked += 23
    print(float_value == Float{1.0})
    print(checked)
}
