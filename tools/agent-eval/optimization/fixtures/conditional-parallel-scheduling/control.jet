// Control: sequential independent kernel over the full floor-sized input.
fn run() {
    values := [Int]{}
    loop index in 0..<1024 {
        values.push(index)
    }
    results := [Int]{}
    loop value in values {
        results.push(value * value)
    }
    print("{results[0]}:{results[1023]}")
}
