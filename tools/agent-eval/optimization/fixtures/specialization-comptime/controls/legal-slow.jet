// Legal slow control: runtime fallback with an explicit checked helper.
fn multiply_once(value: Int, factor: Int) Int -> value * factor
fn multiply_again(value: Int, factor: Int) Int -> multiply_once(value, factor)

fn run() {
    value := 3
    factor := 4
    print(multiply_again(value, factor))
}
