// Control: runtime fallback keeps the factor as a consumed input.
fn multiply(value: Int, factor: Int) Int -> value * factor

fn run() {
    value := 3
    factor := 4
    print(multiply(value, factor))
}
