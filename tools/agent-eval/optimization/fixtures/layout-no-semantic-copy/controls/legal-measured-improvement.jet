// Legal measurement candidate: use the declared fields without an extra copy.
#Layout(c) struct Pair {
    left: U32
    right: U32
}

fn run() {
    pair :: Pair{left: 1, right: 2}
    print(pair.left + pair.right)
}
