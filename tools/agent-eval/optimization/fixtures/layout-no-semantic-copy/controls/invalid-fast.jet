// Negative control: field identity is changed at the foreign-visible boundary.
#Layout(c) struct Pair {
    left: U32
    right: U32
}

fn run() {
    pair :: Pair{left: 2, right: 1}
    print(pair.left)
}
