// Candidate: preserve the C layout while avoiding an unobservable semantic copy.
#Layout(c) struct Pair {
    left: U32
    right: U32
}

fn sum(pair: Pair) U32 -> pair.left + pair.right

fn run() {
    pair :: Pair{left: 1, right: 2}
    print(sum(pair))
}
