// Legal slow control: same disjoint range with an extra checked helper.
fn triple_then_subtract(value: Int) Int -> (value * 3) - value

fn run() {
    values :: [Int]{1, 2, 3, 4}
    total := 0
    loop index in 0..<values.len() {
        total += triple_then_subtract(values[index])
    }
    print(total)
}
