// Negative control: an alias-sensitive body is replaced with a different lane value.
fn run() {
    values :: [Int]{1, 2, 3, 4}
    total := 0
    loop value in values {
        total += value * 3
    }
    print(total)
}
