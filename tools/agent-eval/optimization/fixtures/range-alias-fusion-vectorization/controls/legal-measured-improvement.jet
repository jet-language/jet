// Legal measurement candidate: flat loop has no calls, aliases, or effects.
fn run() {
    values :: [Int]{1, 2, 3, 4}
    total := 0
    loop value in values {
        total += value * 2
    }
    print(total)
}
