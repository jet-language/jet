// Candidate: flat half-open range with a disjoint read and reduction.
fn run() {
    values :: [Int]{1, 2, 3, 4}
    total := 0
    loop index in 0..<values.len() {
        total += values[index] * 2
    }
    print(total)
}
