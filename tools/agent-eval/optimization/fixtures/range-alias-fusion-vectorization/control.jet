// Control: helper call and fresh output keep range and alias facts explicit.
fn double(value: Int) Int -> value * 2

fn run() {
    values :: [Int]{1, 2, 3, 4}
    total := 0
    loop value in values {
        total += double(value)
    }
    print(total)
}
