use core.data as data

#Codable
struct Row {
    team: String
    value: Float
}

fn run() {
    rows :: data.parquet<Row>("/home/nate/.cache/jet-luna/dx3/area-data/fixture.parquet")
    print(data.count(rows))
}
