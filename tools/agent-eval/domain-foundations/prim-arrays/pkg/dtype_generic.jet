use core.compute as compute

fn accept<T>(value: ^Tensor<T>) Tensor<T> -> value

fn run() {
    values := compute.zeros([2]) ?? panic("zeros")
    typed :: accept<Float>(^values)
    print("values:{compute.to_list(typed)}")
}
