use core.compute as compute
fn run() {
    p :: compute.zeros([2, 2]) ?? panic("p")
    g :: compute.ones([2, 2]) ?? panic("g")
    n :: compute.sgd_step(p, g, 0.2) ?? panic("step")
    print(compute.to_list(n))
}
