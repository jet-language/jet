use core.game.raylib as rl

fn run() {
    window :: rl.window_open(320, 240, "Music gap")
    music :: rl.load_music("assets/theme.ogg")
    rl.play_music_stream(music)
    rl.update_music_stream(music)
    rl.close_window(window)
}
