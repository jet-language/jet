use core.files as fs

fn ok_write(path: Path) Bool -> {
    fs.write(path, "x") ?? return false
    return true
}

fn run() {
    p :: Path.from("/home/nate/.cache/jet-luna/dx3/prim-storage/fall.txt")
    value :: ok_write(p) ?? false
    print(value)
}
