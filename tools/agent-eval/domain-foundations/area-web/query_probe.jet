use core.http.server as server

fn read_limit(req: HTTPRequest) String -> {
    return req.query("limit") ?? "missing"
}

fn run() {
    print("probe")
}
 
fn read_form(req: HTTPRequest) String -> {
    return req.form("item") ?? "missing"
}
