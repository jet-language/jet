// Deliberately probes whether Stream exposes restartable checkpoint state.
fn source() Stream<Int> -> {
    yield 1
}

fn run() {
    recovered :: source().checkpoint("state")
    print(recovered)
}
