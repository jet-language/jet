enum Choice {
    A(Int)
    B(Int)
    Done
}

fn score(choice: Choice) Int -> {
    if choice == {
        .A(value) -> { return value }
        .B(value) -> { return value }
        .Done -> { return 0 }
        else -> { return 0 }
    }
}

fn run() {
    print(score(Choice.A(1)))
}
