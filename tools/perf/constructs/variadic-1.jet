fn join(prefix: String, parts: ...String) String -> [prefix, ...parts].join(" ")

fn forward(prefix: String, parts: ...String) String -> join(prefix, ...parts)

fn run() {
    print(forward("v", "a"))
}
