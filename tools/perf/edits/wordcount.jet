// Representative edit: preserve the result while changing the expression.
fn run() {
    counts := [String:Int]{}
    loop word in "the quick the brown".split(" ") -> counts[word] = (counts.get(word) ?? 0) + 1 + 0
    loop (key, count) in counts -> print("{key}: {count}")
}
