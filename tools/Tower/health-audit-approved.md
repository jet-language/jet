# Health Audit — GUARD Verdicts

> Vetted 2026-06-21. Each finding independently verified against actual code at the cited
> line. Owner rule applied: no change-for-change; only legitimate improvements proceed.

## Verdict table

| Finding | Verdict | Risk | Implementation note |
|---------|---------|------|---------------------|
| SAFE-1 (`owner_type` field never read) | **APPROVE** | Low | Confirmed: `mod.rs:444` field set at `Bundle.rs:1328` / `Registration.rs:1024`, never read via `self.owner_type` (grep empty). The live value is the *local parameter* threaded through `check_params_and_body`. No body-check helper consumes the field today. **REMOVE the field** + its two struct-init assignments (keep the local param). The audit's "use it" branch is speculative; there is no current caller that needs it, and I8 says don't add surface for a hypothetical. If a future card needs enclosing-type in body-check, it adds the field back wired to a real reader. Clears the `owner_type is never read` warning. |
| SAFE-2 (80 codes lack ui snapshots) | **APPROVE (scoped subset only)** | Low | I4 is real. Scope to reachable, ratified, uncovered codes. **Verified lacking snapshots AND reachable from ordinary input:** E0420, E0421, E0422, E0423, E0424 (D-UNINIT1, ratified today); E0019 (legacy std import → `core`, teaching); E0039 (`os.environ` → `env.get`, teaching); E0048 (positional tuples → named members, teaching). **Correction to the audit:** E0425 is NOT a Jet diagnostic — it is a Rust compiler error string matched in `FFI.rs:435`. D-UNINIT1 is **5 codes (E0420–E0424), not 6.** Add one `.jet` + `.stderr` fixture per code, blessed via `UPDATE_EXPECT=1`. **DEFER the long tail** (E0061, E0277, E06xx, E08xx, E096x–E098x, E12xx, E20xx, E22xx, E24xx, E26xx, E29xx, E32xx, E34xx, E18xx): many are documented-as-unreachable in diagnostics.md (E2001/E2002, jetpack ModuleEval, C-FFI E3202 precedent) — those are registered+honest, not I4 breaches. Bulk-snapshotting them unattended is large and low-value; route through a roadmap card. |
| SAFE-3 (`remove().unwrap()` panic) | **APPROVE-WITH-CHANGES** | Medium | Confirmed at `Expression.rs:798`: non-Map `remove` emits `(list).remove(idx as usize).unwrap()`. `Vec::remove` panics on OOB before `.unwrap()` is even reached, so `.unwrap()` is dead and misleading. No sema bounds guard for growable-list `remove`. Per I2/I3 (codegen dumb; no raw Rust panic to users), the fix is a **safe codegen lowering that produces a Jet-style runtime bounds error (E3001 panic-report path), not a Rust `panic!`**. Emit a checked helper, e.g. `jet_list_remove(&mut v, idx)` returning the element or raising the E3001 report with Jet file/line — same as existing bounds checks. The audit's "sema lint on non-literal index" is wrong: it would false-negative on dynamic-but-valid indices and false-positive on literals, and can't prove bounds in general. Do NOT add a sema lint. Drop the `.unwrap()`; route through the runtime bounds-report helper. (Verify whether an existing index helper already does this for `get`/`[]` and reuse it.) |
| PERF-1 (`self.moved` clone per arm) | **DEFER** | n/a | Pattern confirmed: `check_switch` (CheckerCore.rs:1413–1415) and `check_if`/`check_for` clone `moved`/`uninit` per branch. But: (a) confidence is the audit's own "Med, no microbenchmark"; (b) the proposed Vec+watermark rewrite touches the move/uninit dataflow that D-UNINIT1 was ratified onto *today*; (c) no measured user-visible cost. Optimizing safety-critical dataflow on speculation, in an unattended pass, is exactly the risk this gate exists to stop. Needs a benchmark first and an owner/roadmap slot. Not change-for-change-worthy until the cost is real. |
| PERF-2 (400-line match arms) | **REJECT** | n/a | Self-classified "Low confidence, style as much as perf." The inlining/PGO argument is unsubstantiated (no profile). Splitting `infer_inner`/`infer_method_call` into per-variant fns is a large mechanical refactor of the hottest, most-tested sema code with zero correctness or measured-perf payoff — textbook change-for-change. If readability is the real goal, route as a roadmap card, not a health-fix. |
| MAINT-1 REMOVE: `type_known` | **APPROVE** | Low | Only self-recursive callers (CheckerCore.rs:259–267); no external entry. Superseded by `core_type_known`. Safe delete. |
| MAINT-1 REMOVE: `enum_lit_args`, `impl_def`, `module_path` | **APPROVE** | Low | All three confirmed zero callers (the `module_path` grep hits are the unrelated `def.module_path` struct *field* in LSP, not the `Parser/Items.rs:237` method). Build warns all three "never used". Safe delete. |
| MAINT-1 REMOVE: `ResolvedDep.tree_hash` field | **APPROVE** | Low | Confirmed write-only (Fetch.rs:115; no `.tree_hash` read on `ResolvedDep` — the `SHA256::tree_hash` fn and `rev.tree_hash` are unrelated). Build warns "field `tree_hash` is never read". Delete the field + its construction-site assignments. |
| MAINT-1 REMOVE: `check_branches` | **APPROVE-WITH-CHANGES → keep as a delete only, do NOT pair with MAINT-2** | Low | Confirmed zero callers (CheckerCore.rs:463). But it is the *unified* branch helper (handles both `moved` and `uninit`), while `check_if`/`check_switch` inline their own copies. MAINT-1 says delete it; MAINT-2 says delete it then re-add an equivalent helper — that is churn. Resolution: **delete `check_branches`** (it's genuinely dead and the inline versions diverge from it), and **REJECT the MAINT-2 re-add** (below). Clears `check_branches is never used`. |
| MAINT-1 KEEP+`#[allow]`: LSP `find_def`/`name_at_offset`/`all_refs`, `SemanticToken` constants | **APPROVE** | Low | Confirmed warned-but-planned: LSP go-to-def / rename infra for M13; SemanticToken type constants for c41 highlighting (map to LSP spec integers). `def_at_offset` is a false-positive (called by `name_at_offset` at SymbolDB.rs:106) — no annotation needed, just confirm it compiles clean once siblings are annotated. Add `#[allow(dead_code)]` with a one-line `// wired in M13 (LSP)` / `// wired in c41 (semantic tokens)` comment. Prefer annotation over deletion — these are real planned API. |
| MAINT-2 (unify branch-checking into a new helper) | **REJECT** | n/a | `check_if` (no-else fall-through merge, 1208–1236) and `check_switch` (exhaustiveness + per-arm coverage) have genuinely different post-branch merge semantics; they are not a clean shared abstraction. The "duplication" is shallow (a 3-line save/restore idiom) and the bodies diverge. Unifying them re-touches D-UNINIT1 dataflow ratified *today* with real soundness risk and no functional gain. This is refactor-for-tidiness on safety-critical code — exactly what I8 + the no-change-for-change rule reject. If pursued later, it needs its own card and review, not an unattended health pass. |
| CLARITY-1 (banned words in diagnostics) | **APPROVE-WITH-CHANGES** | Low | **The claim is correct** — diagnostics.md §Voice rules (line 45–46) literally bans these: *"Banned: token, expression, statement, identifier, parse, syntax error, illegal, invalid, lifetime, borrow checker."* So this is NOT change-for-change; the strings violate a pinned rule. All 9 sites verified verbatim (Parser/mod.rs:320–323, :180; Statements.rs:651/653, 667, 962, 1001). **Changes to the audit's rewrites:** keep them plain but precise — e.g. mod.rs:320 → `"something unexpected appeared after this line"`; Statements.rs:667 → `"expected a call, binding, assignment, or \`return\`, found …"` (audit's wording is fine). Where the existing string already avoids the banned word (Statements.rs:651 `what` is clean; only the `why` at :653 says "a statement must have an effect") rewrite **only the offending line**, not the whole diagnostic. `Items.rs:993` is an internal `expect(...)` message, not user-facing — leave it or reword without blessing. Re-bless all touched ui snapshots. |
| CLARITY-2 (REPL "parse error" leak) | **APPROVE** | Low | Confirmed REPL.rs:450: `"error: materialization parse error: {}"` — "parse error" is banned voice and user-visible. Reword to `"error: could not build this value: {}\n"`. Note this string is likely under a REPL snapshot — re-bless if so. |

### Out-of-scope warnings noted (no action this pass)
Build also warns `field name_span is never read` (3×) and `private item shadows public glob re-export` (2×). Not in the audit; not classified here. Flag for a future sweep.

---

## Approved implementation list (serial; safety first, smallest blast radius first)

Run `nix develop -c cargo build && nix develop -c cargo test` after each item; commit per item.

1. **CLARITY-2** — REPL.rs:450 reword `"parse error"` → `"could not build this value"`. Re-bless any REPL snapshot. (1 string, lowest risk.)

2. **CLARITY-1** — Rewrite the user-facing banned-word strings per diagnostics.md voice (Parser/mod.rs:320,321,323,180; Statements.rs:653,667,962,1001). Rewrite only the offending line in each diagnostic; skip the internal `Items.rs:993` expect. Re-bless touched ui snapshots via `UPDATE_EXPECT=1`. Verify diffs are voice-only, no code numbers changed.

3. **MAINT-1 KEEP set** — Add `#[allow(dead_code)]` + one-line "wired in M13/c41" comment to LSP `find_def`, `name_at_offset`, `all_refs` (SymbolDB.rs) and the `SemanticToken` impl block (Features.rs). Confirm `def_at_offset` warning clears without annotation. Build must drop those warnings.

4. **MAINT-1 REMOVE set** — Delete, one symbol per commit (or one commit, build-verified): `type_known` (CheckerCore.rs:256), `check_branches` (CheckerCore.rs:463), `enum_lit_args` (Parser/Expressions.rs:1269), `impl_def` (Parser/Items.rs:1209), `module_path` (Parser/Items.rs:237), and the `ResolvedDep.tree_hash` field + its assignments (Fetch.rs:115, 287, 367, 383). Each deletion must leave build green.

5. **SAFE-1** — Remove the `owner_type: Option<String>` field from `Checker` (mod.rs:444) and its two init assignments (Bundle.rs:1328, Registration.rs:1024). Keep the `owner_type` *local parameter* on `check_params_and_body`. Build must drop the `owner_type is never read` warning; sema tests stay green.

6. **SAFE-2 (scoped)** — Add ui fixtures (`.jet` + blessed `.stderr`) for E0420, E0421, E0422, E0423, E0424, E0019, E0039, E0048 only. One example per code, minimal reproducer, blessed via `UPDATE_EXPECT=1`. Verify each rendered output matches diagnostics.md format. Do not touch the deferred long tail.

7. **SAFE-3** — Replace the `remove().unwrap()` codegen (Expression.rs:798) with a checked lowering that raises the Jet runtime bounds report (E3001 path) instead of a raw Rust panic. Reuse any existing index/`get` bounds helper if present; otherwise add `jet_list_remove`. No sema lint. Add a runtime example proving an OOB `remove` yields a Jet E3001 report, not a Rust panic.

### Not proceeding (gated)
- **PERF-1** — DEFER: needs a microbenchmark + roadmap slot before touching D-UNINIT1 dataflow.
- **PERF-2** — REJECT: change-for-change; no measured payoff.
- **MAINT-2** — REJECT: would re-touch just-ratified D-UNINIT1 dataflow with soundness risk and no functional gain; `check_if`/`check_switch` merge semantics genuinely differ.
