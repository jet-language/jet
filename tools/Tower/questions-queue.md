# Owner questions queue

Questions the owner asked from the dashboard. Claude answers each — either replying in board.json (shown back on the card) or updating the ballot card itself — then marks it done.

- [x] **D-TOOL-SPLIT**: How do other languages handle this? What are the tradeoffs for each approach?  _(asked 2026-06-19 12:30, id cmqkwpztf51k)_ — answered in board.json + prior-art table added to ballot card
- [x] **D-CT-L2NAME**: I still have absolutely no clue what this means. Give me a user story with example code so I can learn what you mean  _(asked 2026-06-20 10:16, id cmqm7d1nj17e)_ — answered: card rewritten with a plain-language intro + a concrete "which Layer 2?" user story (the compile-time `embed_file` CSV example) showing it's a spec-doc naming clash, not a language feature; identical code under every option. Decision stays open.
- [x] **D-DEFER1**: I am leaning B but want to understand the practical tradeoofs of going with B & C respectively  _(asked 2026-06-20 10:20, id cmqm7ilar46i)_ — answered: added a B-vs-C tradeoff table to the card (ship-cost, mental model, failure mode, readability, `errdefer`) + a crux paragraph. Bottom line: B gets ~95% of C's value with zero new syntax/ratification and keeps one cleanup story; C's only unique win is `errdefer`, unproven, and B doesn't foreclose adding C later. Decision stays open.
- [x] **D-CAP2**: 1) Isnt sharing not supportable with the borrow checker? Wouldnt that be a mutable borrow which can only have 1, but immutable can have many? I dont get the point, refresh me on the memory capability model. How does it play into the borrow checker system? What is auto inferred for beginners but can be specified for experts?  _(asked 2026-06-21 11:31, id cmqnpharzw2)_ — answered in the D-CAP2 card: `share` = shared *ownership* (Rc/Arc, read-only), not a shared mutable borrow, so "mutable XOR shared" holds (view=&T many, edit=&mut T one, take=move, share=Rc); added the capability↔borrow-checker table and the inferred-for-beginners / explicit-for-experts rule.
- [x] **D-REGION1**: How is a supportive of the experts? That is one of the major points for jet -> a beginner friendly automagical side & an expert exposed full control side  _(asked 2026-06-21 11:57, id cmqnqf3pq2ke)_ — answered: reframed to **A (inferred) as the beginner default + B (`region r { … }`) as the explicit expert tier**, both ship; added a worked expert example (two arenas, one named region). Revised rec: ratify A+B together.
- [x] **D-MIGRATE1**: I'm leaning towards A, but how is bloat/buildup addressed? For large codebases, how will users be able to avoid 3k lines of code that are just migrations?  _(asked 2026-06-21 11:59, id cmqnqhfm46if)_ — answered: 4 bounds (only `#PublishedSchema` types accrue; migrations live in `migrations/`, build-tier, zero runtime cost; keep only back to the support floor; `jet schema squash` collapses old chains into a baseline). Bounded by published API × support window, not codebase size. Rec: ratify A with the squash/baseline tooling named in the #11 library.
- [x] **D-CTX1**: Please reform the ballot using a2 & c beta (#context) as the baseline. Give me grammar options to pick from using those two choices. That is what I want to pick but need to ensure I like the syntax/grammar  _(asked 2026-06-21 12:00, id cmqnqj53z5lc)_ — reformed: A2+Cβ fixed as the baseline; now a focused grammar pick G1 (`field = value`) / **G2 (`field: value`, rec)** / G3 (type-inferred), plus sub-points on single-field shorthand and a deferred spread-bundle form. Original Q1/Q2 analysis kept for reference.
- [x] **D-JIT1**: Option D but I dont want to defer the full implementation if avoidable. We are close to epoch 3 anyway, so what should we do to get this working? Propose a new approach in this decision.  _(asked 2026-06-21 12:03, id cmqnqn3mf15b)_ — proposed **Option D+**: interpreter seam now (Phase 1) + **Cranelift JIT this Epoch-3** (Phase 2, tiered: interpreter tier-0, Cranelift tier-1), not deferred. I6 holds (Cranelift is runtime-side, not in `Source/`); needs your approval of the runtime-side Cranelift dep (like regex). Rec D+.
- [x] **D-CAP2**: How does this interplay with experts that will want to use low level memory controls like references and pointers?  _(asked 2026-06-21 12:12, id cmqnqxvijn6)_ — answered in the D-CAP2 card (2nd owner-Q): different tiers that compose — `view`/`edit` ARE the safe references (`&T`/`&mut T`); raw `Ptr<T>` is the expert drop below the capability layer via `use core.mem` + `@unsafe`/`@audit`, where guarantees are suspended; they bridge at the fn boundary (safe capability params public, localized `@unsafe` raw-pointer inside). Worked `checksum(buf: view [U8])` example added.
- [x] **D-CAP3**: How is this affected when no type is written (inferred type)?  _(asked 2026-06-21 12:12, id cmqnqyta74mv)_ — answered in the D-CAP3 card: capabilities are a parameter concept (params carry types); omitted capability ⇒ inferred (no token to place); rule = capability rides the type, so no type written ⇒ capability inferred too — never a dangling capability. This reinforces A (type + capability in one place); the only inferred-type param spot is a lambda, where you write the type alongside to pin a capability.
- [x] **D-BIND2**: I think the $ is fine for me, but what about people trying to use the language globally? Is having such a commonly needed symbol an issue? What if people dont have that symbol? How likely is that? Is it a reasonable concern or not a big deal  _(asked 2026-06-22 10:30, id cmqp2r2n64a)_ — answered in the D-BIND2 card (Owner Q block): `$` is plain ASCII, universal on physical layouts, and Jet already left the ISO-646-invariant subset behind by depending on `# @ { } [ ]`; programmers type `$` constantly (shell/PHP/JS) with no conflicting binding-operator habit. Not a blocker; rec stands at A (`$=`). Decision stays open.
- [x] **D-TERM1**: Please update this card so it isnt called "raw mode" AND we will have a separate library for TUI. This gives experts control but also provides batteries included TUI development features out of the box. Please update this question accordingly & give me alternatives to "raw mode"  _(asked 2026-06-22 10:58, id cmqp3rfij6ck)_ — reformed the card: (1) "raw mode" dropped — naming sub-question with a menu (`live` rec, plus `direct_input`/`live_keys`/`keys`/`interactive`/`immediate`/`cockpit`/`takeover`); (2) the full TUI module (old Option D) is confirmed as a separate batteries-included `jet.tui` library, NOT core — so experts get the low-level primitive and beginners get widgets/screens out of the box on top; (3) the card now decides only the low-level primitive's name + surface shape (A scoped `live { }` block rec / B handle / C free fns; D removed). Decision stays open.
- [x] **D-MIGRATE2**: Break this into multiple cards, I cant choose multiple on one card  _(asked 2026-06-22 13:06, id cmqp8benw44t)_ — done: split into D-MIGRATE2A–F, all answered + ratified 2026-06-22 (in syntax-decisions.md decision log).
- [x] **D-CTCORE1**: I dont understand the downside. Please explain more precisely why we shouldn't just allow all core calls.  _(asked 2026-06-22 13:16, id cmqp8p8lx6x8)_ — answered in-card; ratified 2026-06-22 = B (curated pure comptime-Core whitelist), in the decision log.
- [x] **D-BIND2**: Please update the options to have a large number of sigils that include the = operator and are only two characters long. I want to see a wide array of options that may be more physically ergonomic & logical as an immutable operator before finalizing such a fundamental change  _(asked 2026-06-22 13:42, id cmqp9lvoh7fj)_ — answered in-card; ratified 2026-06-22 = A (`@=`), in the decision log.
- [x] **D-BIND2**: I like the three @, #, $ prefixes. Clean up the rest of the card to just have these options, then give a thorough analysis of logic, psychology, & the tradeoffs associated with each option  _(asked 2026-06-22 13:48, id cmqp9ttj2w6)_ — answered in-card; ratified 2026-06-22 = A (`@=`), in the decision log.
- [x] **D-SOA2**: Break this into multiple questions, I cant choose multiple answers to one question  _(asked 2026-06-22 14:04, id cmqpaenj97mn)_ — done: split into D-SOA2A–D, all answered + ratified 2026-06-22 (in the decision log).
- [x] **D-CAP10**: How is this handled in Jai & Odin?  _(asked 2026-06-23 22:18, id cmqr7hhqr5gc)_ — answered in-card (Odin explicit groups; neither treats capability as an overload axis); ratified 2026-06-23 = A, in the decision log.
- [x] **D-CAP9**: How is this handled in odin & jai?  _(asked 2026-06-23 22:19, id cmqr7iz9icy)_ — answered in-card (both make deref postfix; Jai `*x` raw-of precedent); ratified 2026-06-23 = D (postfix `p.*`, `*T`), in the decision log.
- [x] **D-PKGSIGN1**: What are the downsides of recommendation A? In practice does this cause friction? How do new users know what to do? Will this make it harder to use the system for simpler projects or beginner projects?  _(asked 2026-06-24 10:01, id cmqrwlrfj6rq)_ — answered in board.json + Owner Q&A block on the card: A's only real cost is publisher key custody — consumers/beginners/simple projects see zero friction (silent auto-verify; signing only on the registry-publish path; `jet publish` auto-keygens on first use). Genuine hard part = key loss/rotation (cf. PyPI dropped GPG 2023; npm/Go/crates.io moved authenticity off author keys). Argues for A as opt-in/non-blocking with checksum (B) as the always-on floor; `require_signed` OFF by default. **Owner ratified 2026-06-24 = B + A opt-in** — recorded in syntax-decisions.md, card stripped, plan unblocked.
- [x] **D-SERDE2**: I want this to be easily translatable -> one core lib to support csv, json, toml, yaml, etc. Is that what serde is? I don't want the "SerdeError" - because that engrains a rust package name into our new language. Show me how this is handled in rust & how other languages that are lauded for their handling of this are done  _(asked 2026-06-24 15:46, id cmqs8xldn5mu)_ — answered + card reworked: YES, serde's architecture = one model + per-format adapters (already ratified D-SERDE1); only the Rust *name* leaks. No lauded language names its error after the framework (even Rust uses `serde_json::Error`, never global `SerdeError`). Card now follows Swift `Codable` (the most-praised): rec `encode`/`decode` + `DataTree` + `EncodeError`/`DecodeError`; `SerdeError` dropped. Stays open for the pick.
- [x] **D-SERDE3**: Please analyze community sentiment on different cases. I STRONGLY prefer PascalCase for everything in general, but want to know what most people think  _(asked 2026-06-24 15:48, id cmqs8zavw1iz)_ — answered + card reworked: honest data — on the wire camelCase (~71% of 500 REST APIs; Google/JSON:API/Kubernetes mandate it) + snake_case (~21%) dominate; PascalCase is a .NET-interop minority. So a `rename_all` menu must cover camel+snake; pascal/kebab/screaming each unlock a real wire. Crucially this is the WIRE axis, separate from Jet's own identifiers (tags stay `#PascalCase`, D-CASING1) — your PascalCase preference is preserved there regardless. Rec: typed-keyword full menu (C). Stays open.
- [x] **D-NOSTD1**: I'm not entirely clear on what you are saying. I want to support embedded ssystems. I may have intended to defer this until later or not understood the context... If someone wants to opt out, then I'm fine with that, but we need to match syntax for jet not presume outdated syntax  _(asked 2026-06-24 15:50, id cmqs91z03bz)_ — answered + card reworked: embedded IS a committed goal; the only fix was dropping the borrowed `no_std: true` for Jet's own manifest grammar (ratified `targets:`/typed-platform model). Rec A = platform-implied (`target: bare.*` ⇒ no std), zero new boolean; user surface can ride the embedded-platform milestone (D-OS1, post-E3) while the demo ships as an internal build mode. B (`std: core` field) is the v1-now choice if you want it usable sooner. Stays open.
- [x] **D-JSONVERB1**: Please update the options to use other options with string in the name. I want it to be clear on its face that it is a to string conversion, but I want to keep the pretty printing as an option. Give multiple examples - maybe even based on other langs that have popular, lauded versions, just translate to jet syntax  _(asked 2026-06-24 15:54, id cmqs97t5s3jg)_ — answered + card reworked: all options now have "string" in the name + a paired pretty form. Cross-lang: the lauded shape is two named verbs (compact default + pretty sibling); serde_json's `to_string`/`to_string_pretty` is praised for naming the return type; JS `stringify(v, null, 2)` + Python `dumps` are the criticized ones. Rec A = `json.to_string`/`json.to_string_pretty` (replaces `json.render`). Stays open.
- [x] **D-SIMD2**: What language handles this the best per community sentiment? Show me what that looks like. I want the best version for everything in jet -> if there's already a "perfect" system, lets not reinvent the wheel, just ensure the syntax lets us put it on our axel  _(asked 2026-06-24 15:59, id cmqs9e9e4kl)_ — answered + card reworked: the community has converged — Zig `@Vector` ("vastly more ergonomic") and Swift `SIMD4` (cleanest high-level) agree on the SAME four ergonomics: operators-just-work (`a+b`), splat constructor, `v[i]` indexing, one-call reduce (`v.sum()`). Card now adopts that model wholesale (rec A); options only vary reduce spelling. The one Jet-specific crux flagged: operator overloading on built-in lane types (rec: bless for the compiler-provided `F32x4` family only). Stays open.
- [x] **D-JSONVERB1**: Why not just "JSON.string(...)" or JSON.string_pretty(...)"? Is the "to_  _(asked 2026-06-24 16:29, id cmqsaglqo3dl)_ — answered: `json.string(...)` is a real, terser option. The case for keeping `to_`: (1) it matches Jet's one ratified conversion idiom — `to_int`/`to_float`/`to_string` (S42) — so "turn X into a string" always reads the same way; (2) bare `json.string` reads ambiguously as "the string *node* of this JSON value" (an accessor) once you're walking a `JSON` tree; (3) `to_string` names the return type on its face. The case for `string`/`string_pretty`: shorter, reads fine aloud. Net: `to_string` is the consistency-preserving pick (rec stands), but it's pure preference — if you want the terser `json.string`/`json.string_pretty`, say so and I'll re-bless D-JSONVERB1 + the migrated examples. Your call; D-JSONVERB1 stays open.
- [x] **D-STATE-DECL**: What is the fundamental difference between being able to just use an enum for this??? Could we enhance enums rather than messing around with a similar but entirely new additional tag system?  _(asked 2026-06-25 10:00, id cmqtbzlic586)_ — answered + card reframed: (1) typestate is NOT a new tag system — a "state" is just an ordinary `tag` (D-QUAL2), the markers point at existing tags; (2) the real difference is enum=runtime value ("what state now?", costs memory, you match on it) vs typestate=compile-time-erased PROOF ("prove this op can't run in the wrong state", E0150, zero runtime cost) — a plain enum lets `r.room_key()` compile on a Pending reservation, typestate makes it a compile error; (3) your instinct is right that the enum should be the vocabulary — reframed Option B to "an `enum` IS the state set" (one named, grouped, typo-checked set, exhaustiveness for free, NO new `states { }` keyword), dropping the bespoke block. Rec now B. Decision stays open with one follow-up Owner Q (should the enum also be a storable runtime field, or keep the gate purely compile-time?).
- [x] **D-JIT2**: This question formatting is broken, cant actually chose & it looks very confusing  _(asked 2026-06-25 10:10, id cmqtcdd7b3bx)_ — fixed: the options were bold paragraphs, not list items, so Tower couldn't render them as choices, and the heading had no `(rec X)` and the recommendation was "lay out, do not decide". Rewrote to clean house format — heading now `(rec A)`, the three options are proper `- **Option A/B/C —**` list items each with a short shell example + one consequence line, a clear `**Recommendation: A**` (workspace member `jet-jit/`, keeps I6 machine-checkable), and an Owner Q. Decision stays open and is now pickable.
- [x] **D-ENC-DYN1**: Can we keep it consistent, like to have json, toml, yaml, and csv all share the same data value?  _(asked 2026-06-25 16:42, id cmqtqd4nf71k)_ — answered: **yes**. Reframed the D-ENC-DYN1 card with **Option A+** (one shared `Data` value for all four formats) as the recommendation; the internal `DataTree` is already the universal model, so A+ just exposes it as the single dynamic value every `parse` returns. Only cost = migrating JSON's shipped `JSON` enum to `Data` (bounded clean break, cheapest pre-1.0). Answer also recorded on the board question card; naming Owner Q (`Data` vs `Value`) left open.
