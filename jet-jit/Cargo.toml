[package]
name = "jet-jit"
version = "1.0.0"
edition = "2021"
description = "Jet Cranelift JIT backend (D-JITDEP1 / D-JIT2=A) — tier-1 dev-loop accelerator"

[lib]
name = "jet_jit"
path = "src/lib.rs"

[dependencies]
jet-foundation = { path = "../crates/jet-foundation" }

# D-JITDEP1 / D-JIT2=A: owner-approved runtime-side dep.
# M0 (seam wiring): declared but not yet used — M1 adds the CLIF emitter.
# Carries native-ize obligation: own JIT replaces Cranelift at end of
# the frozen c140→c141 progression.
# cranelift-jit = { version = "0.112", optional = true }   # uncomment in M1
