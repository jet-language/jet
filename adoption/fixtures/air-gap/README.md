# Air-gap fixture data

This tree is test data, not a release archive. The expected SHA-256 values and
state transitions live in [`fixture.json`](fixture.json). The two payloads are
deliberately plain text so a review can inspect every byte. They carry no
private or production trust material.
